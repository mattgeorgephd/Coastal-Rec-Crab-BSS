diff --git a/01_BSS_models/BSS-GH-gear-type-CPUE-model.Rmd b/01_BSS_models/BSS-GH-gear-type-CPUE-model.Rmd
index d88cb69..0f16f33 100644
--- a/01_BSS_models/BSS-GH-gear-type-CPUE-model.Rmd
+++ b/01_BSS_models/BSS-GH-gear-type-CPUE-model.Rmd
@@ -24,8 +24,8 @@ knitr::opts_chunk$set(echo = TRUE)
 # Estimates total recreational Dungeness crab harvest at Westport / Grays Harbor
 # with a per-gear-type CPUE process (shared AR(1) dynamics), so gear-type catch can
 # carry posterior uncertainty. Three population components (shore, private boat, and
-# commercial/charter census) across two gear-regime sub-seasons: ring-net only
-# (Sep 16 - Nov 30, no pots, biweekly BSS periods) and all-gear (Dec 1 - Sep 15,
+# commercial/charter census) across two gear-regime sub-seasons: the pot closure
+# (Sep 16 - Nov 30, no pots, non-pot gear only, biweekly BSS periods) and all-gear (Dec 1 - Sep 15,
 # monthly BSS periods).
 #
 # CURRENT STATE (v5.6): both shore and boat run on the gear-DEPLOYMENT effort unit
@@ -89,7 +89,7 @@ params <- list(
   # --- Stan model file ---
   bss_model_file    = "crab_bss_gear_resolved.stan",
 
-  # --- Default BSS period (overridden per sub-season: biweekly ring-net, monthly all-gear) ---
+  # --- Default BSS period (overridden per sub-season: biweekly pot-closure, monthly all-gear) ---
   period_bss        = "month",
 
   # --- BSS sampler settings ---
@@ -103,7 +103,7 @@ params <- list(
   # Per-fit overrides. The shore all-gear fit has 4 gear types across 10 monthly
   # periods with 1,600+ interviews (the most complex posterior geometry): more
   # iterations, higher adapt_delta, and a deeper treedepth to avoid trajectory
-  # truncation (93.9% saturation at max_treedepth=10). Ring-net fits are data-sparse
+  # truncation (93.9% saturation at max_treedepth=10). Pot-closure fits are data-sparse
   # and benefit from tighter adaptation.
   bss_iter_shore_allgear    = 4000,
   bss_warmup_shore_allgear  = 2000,
@@ -182,41 +182,64 @@ catch_groups <- if(params$estimate_red_rock) {
   "Dungeness_Kept"
 }
 
-# --- Derived: sub-season date ranges ---
-# v5.1: Each sub-season specifies its own BSS period type.
-# Ring-net sub-season uses biweekly periods (fewer months, sparse data).
-# All-gear sub-season uses monthly periods (longer, more data).
-subseason_1 <- list(
-  name = "ring_net_only",
-  start = as.Date(params$est_date_start),
-  end   = as.Date(params$pot_open_date) - 1,
-  period_bss = "biweekly",
-  # Gear types prohibited by regulation during this sub-season.
-  # Pots are illegal Sep 16 - Nov 30. Any "Pot" mentions in interviews
-  # during this period are recording errors or crabbers naming gear they
-  # own rather than what they deployed. Forcing the model to fit a Pot
-  # CPUE process with <6 fractional interviews causes catastrophic
-  # convergence failure (divergences, overflow in C_sum_gear).
-  gear_exclude = c("Pot")
-)
-subseason_2 <- list(
-  name = "all_gear",
-  start = as.Date(params$pot_open_date),
-  end   = as.Date(params$est_date_end),
-  period_bss = "month",
-  gear_exclude = character(0)  # All gear types legal
-)
-
-cat(sprintf("Sub-season 1 (ring-net): %s to %s (%d days, periods=%s)\n",
-            subseason_1$start, subseason_1$end,
-            as.integer(subseason_1$end - subseason_1$start + 1),
-            subseason_1$period_bss))
-cat(sprintf("Sub-season 2 (all-gear): %s to %s (%d days, periods=%s)\n",
-            subseason_2$start, subseason_2$end,
-            as.integer(subseason_2$end - subseason_2$start + 1),
-            subseason_2$period_bss))
+# --- Derived: sub-season date ranges (03_R_functions/build_subseasons.R) --------
+# Split by the pot-closure window (params$pot_closure_start / pot_closure_end, added
+# 2026-07-13). Each sub-season carries its own BSS period type (biweekly for the pot
+# closure: fewer months, sparse data; monthly for all-gear) and the gear types excluded
+# by regulation. During the closure Pot is excluded: pots are illegal Sep 16 - Nov 30,
+# and stray "Pot" mentions in that window are recording errors (crabbers naming gear
+# they own, not what they deployed); fitting a Pot CPUE process with <6 fractional
+# interviews causes catastrophic convergence failure (divergences, overflow in
+# C_sum_gear). For the current config (closure at the season start) this returns the
+# historical two sub-seasons with unchanged internal names ("ring_net_only" is the pot-
+# closure key, kept for output-filename continuity; display_name is "Pot closure"), so
+# keys and filenames do not change. The name "ring-net only" was a misnomer: non-pot
+# gear other than ring nets is also legal in the closure. Model logic keys off
+# ss$gear_regime ("pot_closure" | "all_gear"), never ss$name.
+subseasons <- build_subseasons(params)
+for (ss in subseasons) {
+  cat(sprintf("Sub-season: %-28s %s to %s (%d days, periods=%s)\n",
+              paste0(ss$display_name, " [", ss$name, "]"),
+              ss$start, ss$end, as.integer(ss$end - ss$start + 1), ss$period_bss))
+}
 cat(sprintf("Catch groups: %s\n", paste(catch_groups, collapse = ", ")))
 
+# GUARD (2026-07-13): build_subseasons is fully general (it emits pre/post all-gear
+# periods for a mid-season pot closure), but this report's downstream AGGREGATION is
+# still hardcoded to the historical two sub-seasons per population: the PE port summary
+# (component_keys / component_names), the catch-by-mode / final harvest tables, and the
+# harvest summary all enumerate exactly {ring_net_only, all_gear}. Extra sub-season keys
+# (all_gear_pre / all_gear_post) would be silently dropped there, under-reporting the
+# port total. Until those aggregators are generalized to sum over ALL sub-seasons per
+# population, stop with a clear message rather than under-report.
+ss_name_set <- vapply(subseasons, function(z) z$name, character(1))
+if (!setequal(ss_name_set, c("ring_net_only", "all_gear"))) {
+  stop(sprintf(paste0(
+    "build_subseasons produced sub-seasons {%s}, but this report's aggregation tables ",
+    "are hardcoded to {ring_net_only, all_gear}. A mid-season pot closure needs those ",
+    "tables generalized to sum over all sub-seasons per population first. Aborting to ",
+    "avoid silently under-reporting the port total."),
+    paste(ss_name_set, collapse = ", ")), call. = FALSE)
+}
+
+# Pot-closure boundary markers for date-axis plots (see the pooled driver for the
+# rationale). Dotted line at the closure start, dashed line at the day pots open
+# (closure end + 1), so every season plot brackets the pot-closure window. Returns a
+# list of ggplot layers added with `+`; labels = FALSE draws the lines only.
+pot_closure_markers <- function(params, labels = TRUE) {
+  pc_start  <- as.Date(params$pot_closure_start %||% params$est_date_start)
+  pots_open <- as.Date(params$pot_open_date     %||% (as.Date(params$pot_closure_end) + 1))
+  layers <- list(
+    geom_vline(xintercept = pc_start,  linetype = "dotted", color = "darkgreen", linewidth = 0.5),
+    geom_vline(xintercept = pots_open, linetype = "dashed", color = "red",       linewidth = 0.5))
+  if (labels) layers <- c(layers, list(
+    annotate("text", x = pc_start + 3,  y = Inf, label = "Pot closure start",
+             hjust = 0, vjust = 1.5, size = 3, color = "darkgreen"),
+    annotate("text", x = pots_open + 3, y = Inf, label = "Pots open",
+             hjust = 0, vjust = 1.5, size = 3, color = "red")))
+  layers
+}
+
 
 # ===========================================================================
 # 1. DATA INGESTION - Read CSVs and classify interviews by population
@@ -270,7 +293,7 @@ cat(sprintf("Catch groups: %s\n", paste(catch_groups, collapse = ", ")))
 # v5.1 CHANGES:
 #   - Weighted gear assignment: gear_weights matrix replaces gear_type_IntC
 #   - Empirical pi_gear_data[P_n, 3, G_gear] with Laplace smoothing
-#   - Per-sub-season period type (biweekly for ring-net, monthly for all-gear)
+#   - Per-sub-season period type (biweekly for pot closure, monthly for all-gear)
 #   - estimate_R_G flag and R_G_fixed for boat fits
 #   - Cauchy prior scales tightened to 2 (was 5)
 #   - Word-boundary regex for robust gear-type detection
@@ -329,9 +352,7 @@ p_shore_effort <- dwg$shore_effort |>
   left_join(days_full |> select(event_date, day_type), by = "event_date") |>
   ggplot(aes(x = event_date, y = total_gear, color = day_type, size = n_counts)) +
   geom_point(alpha = 0.5) +
-  geom_vline(xintercept = as.Date(params$pot_open_date), linetype = "dashed", color = "red", linewidth = 0.5) +
-  annotate("text", x = as.Date(params$pot_open_date) + 5, y = Inf, label = "Pots open",
-           hjust = 0, vjust = 1.5, size = 3, color = "red") +
+  pot_closure_markers(params, labels = TRUE) +
   scale_size_continuous(range = c(1,3), breaks = 1:3, name = "Counts/day") +
   labs(title = "Shore Crabbers: Daily Gear Count (Docks F20 + F17-21)",
        subtitle = "Point size = count sequences per day",
@@ -347,7 +368,7 @@ p_boat_effort <- dwg$boat_effort |>
   left_join(days_full |> select(event_date, day_type), by = "event_date") |>
   ggplot(aes(x = event_date, y = total_trailers, color = day_type)) +
   geom_point(alpha = 0.5) +
-  geom_vline(xintercept = as.Date(params$pot_open_date), linetype = "dashed", color = "red", linewidth = 0.5) +
+  pot_closure_markers(params, labels = FALSE) +
   labs(title = "Private Boat Crabbers: Daily Trailer Count (Boat Launch)",
        x = "Date", y = "Total trailer count", color = "Day type") +
   theme_bw()
@@ -367,7 +388,7 @@ p_cpue <- cpue_data |>
   ggplot(aes(x = event_date, y = mean_cpue, color = day_type)) +
   geom_point(alpha = 0.4, size = 1.5) +
   geom_smooth(method = "loess", span = 0.3, se = TRUE, color = "grey30") +
-  geom_vline(xintercept = as.Date(params$pot_open_date), linetype = "dashed", color = "red", linewidth = 0.5) +
+  pot_closure_markers(params, labels = FALSE) +
   facet_wrap(~population, ncol = 1, scales = "free_y",
              labeller = labeller(population = c(shore = "Shore Crabbers", private_boat = "Private Boat Crabbers"))) +
   labs(title = "Dungeness CPUE by Population",
@@ -419,7 +440,7 @@ cat("\n", strrep("=",60), "\n PE ESTIMATES\n", strrep("=",60), "\n")
 
 pe_all <- list()
 for(pop in c("shore","private_boat")) {
-  for(ss in list(subseason_1, subseason_2)) {
+  for(ss in subseasons) {
     label <- paste0(pop, "_", ss$name)
     days_ss <- prep_days_crab(ss$start, ss$end, params, L_eff_model)
     summ_ss <- prep_population_summary(dwg, pop, ss$start, ss$end, params)
@@ -433,7 +454,7 @@ cat("\n  Generating PE plots...\n")
 
 pe_daily_all <- list()
 for(pop in c("shore","private_boat")) {
-  for(ss in list(subseason_1, subseason_2)) {
+  for(ss in subseasons) {
     label <- paste0(pop, "_", ss$name)
     days_ss <- prep_days_crab(ss$start, ss$end, params, L_eff_model)
     summ_ss <- prep_population_summary(dwg, pop, ss$start, ss$end, params)
@@ -477,7 +498,7 @@ p_pe_effort <- pe_daily_df |>
   ggplot(aes(x = event_date, y = est_daily_crabber_hours, color = day_type)) +
   geom_point(alpha = 0.4) +
   geom_smooth(method = "loess", span = 0.3, se = TRUE, color = "grey30") +
-  geom_vline(xintercept = as.Date(params$pot_open_date), linetype = "dashed", color = "red", linewidth = 0.5) +
+  pot_closure_markers(params, labels = FALSE) +
   facet_wrap(~pop_label, ncol = 1, scales = "free_y") +
   labs(title = "PE: Estimated Daily Crabber-Hours by Population",
        x = "Date", y = "Estimated crabber-hours", color = "Day type") +
@@ -496,7 +517,7 @@ timer_stop("4_commercial_charter")
 # ---- 7.5 PE PORT TOTALS ----
 cat("\n", strrep("=",60), "\n PE PORT TOTALS\n", strrep("=",60), "\n")
 
-component_names <- c("Shore (ring-net)","Shore (all-gear)","Private Boat (ring-net)",
+component_names <- c("Shore (pot-closure)","Shore (all-gear)","Private Boat (pot-closure)",
                      "Private Boat (all-gear)","Commercial/Charter","PORT TOTAL")
 component_keys <- c("shore_ring_net_only","shore_all_gear",
                     "private_boat_ring_net_only","private_boat_all_gear","comm_charter")
@@ -530,7 +551,7 @@ bss_all <- list()
 ar_resolutions <- list()   # AR resolution actually used, per fit (for the gate report)
 for(cg in bss_catch_groups) {
   for(pop in c("shore","private_boat")) {
-    for(ss in list(subseason_1, subseason_2)) {
+    for(ss in subseasons) {
       label <- paste0(pop, "_", ss$name, "_", cg)
       cat(sprintf("\n--- BSS: %s ---\n", label))
 
@@ -555,11 +576,11 @@ for(cg in bss_catch_groups) {
       ar_resolutions[[label]] <- bss_data$.ar_resolution
 
       is_shore <- (pop == "shore")
-      is_allgear <- (ss$name == "all_gear")
-      is_ringnet <- (ss$name == "ring_net_only")
+      is_allgear <- (ss$gear_regime == "all_gear")
+      is_ringnet <- (ss$gear_regime == "pot_closure")
 
       # Per-fit sampler tuning: shore all-gear is the most complex posterior
-      # (4 gear types, 10 monthly periods); ring-net fits are data-sparse
+      # (4 gear types, 10 monthly periods); pot-closure fits are data-sparse
       # and benefit from tighter adaptation. All other fits use defaults.
       if(is_shore && is_allgear) {
         fit_iter    <- params$bss_iter_shore_allgear
@@ -728,7 +749,7 @@ for(cg in bss_catch_groups) {
   bss_E_total <- rep(0, n_draws_max)
 
   for(pop in c("shore","private_boat")) {
-    for(ss in list(subseason_1, subseason_2)) {
+    for(ss in subseasons) {
       label <- paste0(pop, "_", ss$name, "_", cg)
       pe_label <- paste0(pop, "_", ss$name)
       b <- bss_all[[label]]
@@ -792,7 +813,7 @@ monthly_E_draws <- matrix(0, nrow = n_draws_max, ncol = length(all_months),
 
 for(cg in bss_catch_groups) {
   for(pop in c("shore","private_boat")) {
-    for(ss in list(subseason_1, subseason_2)) {
+    for(ss in subseasons) {
       label <- paste0(pop, "_", ss$name, "_", cg)
       pe_label <- paste0(pop, "_", ss$name)
       b <- bss_all[[label]]
@@ -898,6 +919,8 @@ p_monthly <- monthly_summary |>
   ggplot(aes(x = Month)) +
   geom_col(aes(y = BSS_Catch_median), fill = "steelblue", alpha = 0.7) +
   geom_errorbar(aes(ymin = BSS_Catch_lo95, ymax = BSS_Catch_hi95), width = 0.3) +
+  geom_vline(xintercept = which(all_months == format(as.Date(params$pot_closure_start %||% params$est_date_start), "%Y-%m")) - 0.5,
+             linetype = "dotted", color = "darkgreen") +
   geom_vline(xintercept = which(all_months == format(as.Date(params$pot_open_date), "%Y-%m")) - 0.5,
              linetype = "dashed", color = "red") +
   labs(title = "Monthly Dungeness Catch Estimate (BSS + PE combined)",
@@ -914,6 +937,8 @@ p_monthly_eff <- monthly_summary |>
   ggplot(aes(x = Month)) +
   geom_col(aes(y = BSS_Effort_median), fill = "darkgreen", alpha = 0.7) +
   geom_errorbar(aes(ymin = BSS_Effort_lo95, ymax = BSS_Effort_hi95), width = 0.3) +
+  geom_vline(xintercept = which(all_months == format(as.Date(params$pot_closure_start %||% params$est_date_start), "%Y-%m")) - 0.5,
+             linetype = "dotted", color = "darkgreen") +
   geom_vline(xintercept = which(all_months == format(as.Date(params$pot_open_date), "%Y-%m")) - 0.5,
              linetype = "dashed", color = "red") +
   labs(title = "Monthly Effort Estimate (BSS + PE combined)",
@@ -967,7 +992,7 @@ cat("\n", strrep("=",60), "\n BSS CATCH BY GEAR TYPE\n", strrep("=",60), "\n")
 gear_results <- list()
 
 for(pop in c("shore","private_boat")) {
-  for(ss in list(subseason_1, subseason_2)) {
+  for(ss in subseasons) {
     label_cg <- paste0(pop, "_", ss$name, "_Dungeness_Kept")
     pe_label <- paste0(pop, "_", ss$name)
     b <- bss_all[[label_cg]]
@@ -1253,7 +1278,7 @@ cat("\n", strrep("=",60), "\n DAILY COMBINED ESTIMATE\n", strrep("=",60), "\n")
 daily_combined <- list()
 
 for(pop in c("shore","private_boat")) {
-  for(ss in list(subseason_1, subseason_2)) {
+  for(ss in subseasons) {
     label_cg <- paste0(pop, "_", ss$name, "_Dungeness_Kept")
     pe_label <- paste0(pop, "_", ss$name)
     b <- bss_all[[label_cg]]
@@ -1336,7 +1361,7 @@ cat("\n", strrep("=",60), "\n DATA COVERAGE\n", strrep("=",60), "\n")
 
 coverage <- list()
 for(pop in c("shore","private_boat")) {
-  for(ss in list(subseason_1, subseason_2)) {
+  for(ss in subseasons) {
     days_ss <- prep_days_crab(ss$start, ss$end, params, L_eff_model)
     summ_ss <- prep_population_summary(dwg, pop, ss$start, ss$end, params)
 
@@ -1529,7 +1554,7 @@ cat("\n", strrep("=",60), "\n PE VS BSS COMPARISON (by component)\n", strrep("="
 
 comparison <- list()
 for(pop in c("shore","private_boat")) {
-  for(ss in list(subseason_1, subseason_2)) {
+  for(ss in subseasons) {
     label_cg <- paste0(pop, "_", ss$name, "_Dungeness_Kept")
     pe_label <- paste0(pop, "_", ss$name)
     b <- bss_all[[label_cg]]
@@ -1552,7 +1577,7 @@ for(pop in c("shore","private_boat")) {
     }
 
     comparison[[length(comparison)+1]] <- tibble(
-      component = paste0(pop, " (", ss$name, ")"),
+      component = paste0(pop, " (", ss$display_name, ")"),
       PE_effort = round(pe_effort),
       BSS_effort = round(bss_effort),
       effort_diff_pct = effort_diff_pct,
@@ -1686,7 +1711,7 @@ cat("\n", strrep("=",60), "\n SENSITIVITY: INCOMPLETE TRIP EFFECT\n", strrep("="
 sensitivity_results <- list()
 
 for(pop in c("shore","private_boat")) {
-  for(ss in list(subseason_1, subseason_2)) {
+  for(ss in subseasons) {
     label <- paste0(pop, "_", ss$name)
     days_ss <- prep_days_crab(ss$start, ss$end, params, L_eff_model)
     summ_ss <- prep_population_summary(dwg, pop, ss$start, ss$end, params)
diff --git a/01_BSS_models/BSS-GH-pooled-CPUE-model.Rmd b/01_BSS_models/BSS-GH-pooled-CPUE-model.Rmd
index 7d41275..95e5eea 100644
--- a/01_BSS_models/BSS-GH-pooled-CPUE-model.Rmd
+++ b/01_BSS_models/BSS-GH-pooled-CPUE-model.Rmd
@@ -33,9 +33,29 @@ report_table <- function(df, caption = NULL, digits = 2) {
 # Short, HTML-friendly fit labels for display tables (e.g. "boat all-gear").
 short_fit <- function(x) gsub("_Dungeness_Kept$", "",
                          gsub("private_boat", "boat",
-                           gsub("_ring_net_only", " ring-net",
+                           gsub("_ring_net_only", " pot-closure",
                              gsub("_all_gear", " all-gear", x))))
 
+# Pot-closure boundary markers for date-axis plots. Draws a dotted line at the pot-
+# closure start and a dashed line at the day pots open (closure end + 1), so every
+# season plot brackets the pot-closure window explicitly (added 2026-07-13 with the
+# explicit pot_closure_start/end config). Returns a list of ggplot layers, added to a
+# plot with `+`. When the closure starts at the season start the left line sits at the
+# left edge (correct). labels = FALSE draws the lines only (for secondary panels).
+pot_closure_markers <- function(params, labels = TRUE) {
+  pc_start  <- as.Date(params$pot_closure_start %||% params$est_date_start)
+  pots_open <- as.Date(params$pot_open_date     %||% (as.Date(params$pot_closure_end) + 1))
+  layers <- list(
+    geom_vline(xintercept = pc_start,  linetype = "dotted", color = "darkgreen", linewidth = 0.5),
+    geom_vline(xintercept = pots_open, linetype = "dashed", color = "red",       linewidth = 0.5))
+  if (labels) layers <- c(layers, list(
+    annotate("text", x = pc_start + 3,  y = Inf, label = "Pot closure start",
+             hjust = 0, vjust = 1.5, size = 3, color = "darkgreen"),
+    annotate("text", x = pots_open + 3, y = Inf, label = "Pots open",
+             hjust = 0, vjust = 1.5, size = 3, color = "red")))
+  layers
+}
+
 # Read the per-fit CSVs a section wrote to output_dir (files named prefix_<label>.csv)
 # and row-bind them for on-page display, tagging each row with a short fit_lbl label from
 # the filename. Display-only and defensive: returns NULL if output_dir or the files are
@@ -63,7 +83,7 @@ read_fit_outputs <- function(prefix, dir = NULL) {
 
 ## 1. Setup and configuration
 
-Loads packages, sources the helper functions in `03_R_functions/`, applies the run configuration from `run_config.R`, and defines the two sub-seasons (ring-net-only and all-gear). Every user-selectable toggle for this run lives in `run_config.R`.
+Loads packages, sources the helper functions in `03_R_functions/`, applies the run configuration from `run_config.R`, and defines the two sub-seasons (pot-closure and all-gear). Every user-selectable toggle for this run lives in `run_config.R`.
 
 ```{r config, results='hide'}
 ###############################################################################
@@ -76,8 +96,10 @@ Loads packages, sources the helper functions in `03_R_functions/`, applies the r
 #   1. Shore crabbers (dock + jetty + beach)   BSS + PE
 #   2. Private boat crabbers                    BSS + PE
 #   3. Commercial/charter boats                 census/tally
-# Two gear-regime sub-seasons: ring-net only (Sep 16 - Nov 30, no pots) and
-# all-gear (Dec 1 - Sep 15, pots allowed).
+# Two gear-regime sub-seasons: pot closure (Sep 16 - Nov 30, non-pot gear only) and
+# all-gear (Dec 1 - Sep 15, pots allowed). "Pot closure" was formerly labeled
+# "ring-net only"; non-pot gear other than ring nets is also legal, so the name was
+# a misnomer. The internal key stays "ring_net_only" for output-filename continuity.
 #
 # CURRENT STATE (v7.9): both fitted components run on the gear-DEPLOYMENT effort
 # unit (h = number_of_gear, L = tau), via the shared 03_R_functions/bss_effort_spec.R.
@@ -237,25 +259,40 @@ catch_groups <- if(params$estimate_red_rock) {
   "Dungeness_Kept"
 }
 
-subseason_1 <- list(
-  name = "ring_net_only",
-  start = as.Date(params$est_date_start),
-  end   = as.Date(params$pot_open_date) - 1
-)
-subseason_2 <- list(
-  name = "all_gear",
-  start = as.Date(params$pot_open_date),
-  end   = as.Date(params$est_date_end)
-)
-
-cat(sprintf("Sub-season 1 (ring-net): %s to %s (%d days)\n",
-            subseason_1$start, subseason_1$end,
-            as.integer(subseason_1$end - subseason_1$start + 1)))
-cat(sprintf("Sub-season 2 (all-gear): %s to %s (%d days)\n",
-            subseason_2$start, subseason_2$end,
-            as.integer(subseason_2$end - subseason_2$start + 1)))
+# Sub-seasons split by the pot-closure window (03_R_functions/build_subseasons.R).
+# The pot closure is when pots are not legal (non-pot gear only); its window is set
+# explicitly in run_config (pot_closure_start / pot_closure_end). For the current
+# config (closure at the season start) this returns the historical two sub-seasons
+# with unchanged internal names ("ring_net_only" is the pot-closure key, kept for
+# output-filename continuity; display_name is "Pot closure"), so keys and filenames
+# do not change. A mid-season closure adds pre/post all-gear periods automatically.
+# Model logic keys off ss$gear_regime ("pot_closure" | "all_gear"), never ss$name.
+subseasons <- build_subseasons(params)
+for (ss in subseasons) {
+  cat(sprintf("Sub-season: %-26s %s to %s (%d days)\n",
+              paste0(ss$display_name, " [", ss$name, "]"),
+              ss$start, ss$end, as.integer(ss$end - ss$start + 1)))
+}
 cat(sprintf("Catch groups: %s\n", paste(catch_groups, collapse = ", ")))
 
+# GUARD (2026-07-13): build_subseasons is fully general (it emits pre/post all-gear
+# periods for a mid-season pot closure), but this report's downstream AGGREGATION is
+# still hardcoded to the historical two sub-seasons per population: the PE port summary
+# (component_keys / component_names), the catch-by-mode table, and the PE-fallback rows
+# of the final harvest table all enumerate exactly {ring_net_only, all_gear}. Extra
+# sub-season keys (all_gear_pre / all_gear_post) would be silently dropped there,
+# under-reporting the port total. Until those aggregators are generalized to sum over
+# ALL sub-seasons per population, stop with a clear message rather than under-report.
+ss_name_set <- vapply(subseasons, function(z) z$name, character(1))
+if (!setequal(ss_name_set, c("ring_net_only", "all_gear"))) {
+  stop(sprintf(paste0(
+    "build_subseasons produced sub-seasons {%s}, but this report's aggregation tables ",
+    "are hardcoded to {ring_net_only, all_gear}. A mid-season pot closure needs those ",
+    "tables generalized to sum over all sub-seasons per population first. Aborting to ",
+    "avoid silently under-reporting the port total."),
+    paste(ss_name_set, collapse = ", ")), call. = FALSE)
+}
+
 
 # ===========================================================================
 # 0.2 STANDALONE DAY-TYPE CLASSIFIER
@@ -417,9 +454,7 @@ p_shore_effort <- dwg$shore_effort |>
   mutate(day_type = classify_day_type(event_date, params)) |>
   ggplot(aes(x = event_date, y = total_gear, color = day_type, size = n_counts)) +
   geom_point(alpha = 0.5) +
-  geom_vline(xintercept = as.Date(params$pot_open_date), linetype = "dashed", color = "red", linewidth = 0.5) +
-  annotate("text", x = as.Date(params$pot_open_date) + 5, y = Inf, label = "Pots open",
-           hjust = 0, vjust = 1.5, size = 3, color = "red") +
+  pot_closure_markers(params, labels = TRUE) +
   scale_size_continuous(range = c(1,3), breaks = 1:3, name = "Counts/day") +
   labs(title = "Shore: Daily Gear Count (Docks F20 + F17-21)",
        x = "Date", y = "Total gear count", color = "Day type") +
@@ -433,7 +468,7 @@ p_boat_effort <- dwg$boat_effort |>
   mutate(day_type = classify_day_type(event_date, params)) |>
   ggplot(aes(x = event_date, y = total_trailers, color = day_type)) +
   geom_point(alpha = 0.5) +
-  geom_vline(xintercept = as.Date(params$pot_open_date), linetype = "dashed", color = "red", linewidth = 0.5) +
+  pot_closure_markers(params, labels = FALSE) +
   labs(title = "Private Boat: Daily Trailer Count", x = "Date", y = "Trailers", color = "Day type") +
   theme_bw()
 ggsave(file.path(output_dir, "plot_boat_effort_timeseries.png"), p_boat_effort, width = 10, height = 6)
@@ -451,7 +486,7 @@ p_cpue <- cpue_data |>
   ggplot(aes(x = event_date, y = mean_cpue, color = day_type)) +
   geom_point(alpha = 0.4, size = 1.5) +
   geom_smooth(method = "loess", span = 0.3, se = TRUE, color = "grey30") +
-  geom_vline(xintercept = as.Date(params$pot_open_date), linetype = "dashed", color = "red") +
+  pot_closure_markers(params, labels = FALSE) +
   facet_wrap(~population, ncol = 1, scales = "free_y") +
   labs(title = "Dungeness CPUE by Population", x = "Date", y = "Mean CPUE", color = "Day type") +
   theme_bw()
@@ -589,7 +624,7 @@ cat("\n", strrep("=",60), "\n PE ESTIMATES\n", strrep("=",60), "\n")
 
 pe_all <- list()
 for(pop in c("shore","private_boat")) {
-  for(ss in list(subseason_1, subseason_2)) {
+  for(ss in subseasons) {
     label <- paste0(pop, "_", ss$name)
     days_ss <- prep_days_crab(ss$start, ss$end, params, L_eff_model = L_eff_model)
     summ_ss <- prep_population_summary(dwg, pop, ss$start, ss$end, params)
@@ -619,7 +654,7 @@ sens_df <- NULL   # persists to the display chunk below even if the tryCatch err
 tryCatch({
   sens_rows <- list()
   for(pop in c("shore","private_boat")) {
-    for(ss in list(subseason_1, subseason_2)) {
+    for(ss in subseasons) {
       s_label <- paste0(pop, "_", ss$name)
       days_ss <- prep_days_crab(ss$start, ss$end, params, L_eff_model = L_eff_model)
       summ_ss <- prep_population_summary(dwg, pop, ss$start, ss$end, params)
@@ -629,6 +664,7 @@ tryCatch({
       d_on    <- run_pe_pooled(summ_ss, days_ss, p_on,  paste0(s_label, " [complete only]"))$Dungeness_Kept %||% 0
       sens_rows[[s_label]] <- tibble(
         component                  = s_label,
+        component_disp             = paste0(gsub("_", " ", pop), " ", ss$display_name),
         PE_Dungeness_all_trips     = round(d_off),
         PE_Dungeness_complete_only = round(d_on),
         diff_pct                   = if(d_off > 0) round((d_on - d_off) / d_off * 100, 1) else NA_real_
@@ -650,7 +686,7 @@ tryCatch({
 if (!is.null(sens_df) && nrow(sens_df) > 0) {
   report_table(
     dplyr::transmute(sens_df,
-      Component                 = gsub("_", " ", component),
+      Component                 = component_disp,
       `PE catch, all trips`     = PE_Dungeness_all_trips,
       `PE catch, complete only` = PE_Dungeness_complete_only,
       `Change (%)`              = diff_pct,
@@ -666,7 +702,7 @@ if (!is.null(sens_df) && nrow(sens_df) > 0) {
 
 ### 4.2 Commercial and charter census
 
-The commercial and charter harvest is a day-type-stratified census expansion of the daily vessel tally, not a modeled quantity.
+The commercial and charter harvest is a day-type-stratified census expansion of the daily vessel tally, not a modeled quantity. As of the T1.4 change it uses a **separate mean catch per vessel for commercial vs charter vessels**, each applied to its own tally column, rather than one mean across the pooled tally; the two classes have different catch profiles, so a single mean biased the total whenever the commercial-to-charter mix in the tally differed from the mix in the interviews.
 
 ```{r pe-comm, results='hide'}
 # ---- 7.4 COMMERCIAL/CHARTER ----
@@ -677,6 +713,29 @@ timer_stop("4_commercial_charter")
 
 ```
 
+```{r pe-comm-show, echo=FALSE}
+# T1.4: show the commercial-vs-charter per-vessel split behind the census. Each class
+# gets its own mean Dungeness per vessel applied to its own tally column (Guide folded
+# into Charter); a class with no interviews falls back to the pooled mean.
+.vt <- tryCatch(pe_all$comm_charter$vessel_type_detail, error = function(e) NULL)
+if (!is.null(.vt) && nrow(.vt) > 0) {
+  report_table(
+    dplyr::transmute(.vt,
+      `Vessel type`            = vessel_type,
+      Interviews               = n_interviews,
+      `Mean Dungeness/vessel`  = round(mean_dung_per_vessel, 1),
+      `Tally vessels (window)` = round(tally_vessels),
+      `Sampled-day catch`      = round(sampled_catch)),
+    caption = paste("Commercial vs charter split behind the census (T1.4). Each class's",
+                    "mean catch per vessel is applied to its own tally column instead of",
+                    "one pooled mean. 'Sampled-day catch' is the class census before the",
+                    "day-type expansion that produces the reported commercial/charter",
+                    "total, so it shows the split, not the final stratified figure."))
+} else {
+  cat("_Commercial/charter vessel-type split unavailable this run._")
+}
+```
+
 ### 4.3 PE port totals
 
 The Point Estimator port total, summed across the three population components. Compare it against the Bayesian total below.
@@ -685,7 +744,7 @@ The Point Estimator port total, summed across the three population components. C
 # ---- 7.5 PE PORT TOTALS ----
 cat("\n", strrep("=",60), "\n PE PORT TOTALS\n", strrep("=",60), "\n")
 
-component_names <- c("Shore (ring-net)","Shore (all-gear)","Private Boat (ring-net)",
+component_names <- c("Shore (pot-closure)","Shore (all-gear)","Private Boat (pot-closure)",
                      "Private Boat (all-gear)","Commercial/Charter","PORT TOTAL")
 component_keys <- c("shore_ring_net_only","shore_all_gear",
                     "private_boat_ring_net_only","private_boat_all_gear","comm_charter")
@@ -714,6 +773,37 @@ write.csv(pe_summary, file.path(output_dir, "pe_port_summary.csv"), row.names=FA
 report_table(pe_summary, 'Point Estimator (PE) port totals by component.')
 ```
 
+### 4.4 PE internal consistency (ratio-of-sums)
+
+Each component's PE stratum CPUE is a ratio-of-sums (`sum(catch)/sum(hrs)` within each week-by-day-type stratum), not a mean of per-day ratios (backlog P0). The mean-of-ratios was unstable in thin strata, a day with very little sampled effort produced an extreme daily ratio that was then multiplied by the full stratum effort, and it made the shore PE's implied CPUE about 2.9x its own interview ratio-of-sums. Ratio-of-sums removes that and matches the gear-resolved track. The check below is the guardrail: the PE's implied CPUE (its catch divided by its effort) must agree with the ratio-of-sums over the same interviews. A ratio far from 1 means catch and effort are on different scales, and the run stops if any component exceeds 2x, so a clean table here is itself the confirmation that the PE is internally consistent.
+
+```{r pe-consistency-show, echo=FALSE}
+# P0: render the PE internal-consistency check returned by run_pe_pooled per component
+# (component_keys is defined in the pe-port chunk above). comm_charter is a census, not
+# run_pe_pooled, so it has no check and is skipped.
+pe_chk <- tryCatch(
+  dplyr::bind_rows(lapply(component_keys, function(k) {
+    d <- pe_all[[k]]$pe_cpue_check
+    if (is.null(d)) NULL else dplyr::mutate(d, Component = k)
+  })),
+  error = function(e) NULL)
+if (!is.null(pe_chk) && nrow(pe_chk) > 0) {
+  report_table(
+    dplyr::transmute(pe_chk,
+      Component         = short_fit(Component),
+      Unit              = effort_unit,
+      `PE implied CPUE` = round(pe_implied_cpue, 3),
+      `Interview RoS`   = round(interview_ros, 3),
+      Ratio             = round(ratio, 2)),
+    caption = paste("PE internal-consistency check (P0). 'PE implied CPUE' is the",
+                    "component's PE catch / PE effort; 'Interview RoS' is the ratio-of-sums",
+                    "over the same interviews. Healthy: Ratio near 1 (the run stops if any",
+                    "component is below 0.5 or above 2.0)."))
+} else {
+  cat("_PE consistency check unavailable (no fitted components produced it)._")
+}
+```
+
 ## 5. Bayesian state-space model fits
 
 Each population by sub-season is fit independently with a Bayesian state-space (BSS) model that treats daily effort and catch rate as smoothly evolving latent series, fills the gaps between sampled days, and returns full credible intervals. **What a healthy fit looks like:** R-hat below 1.01, effective sample size above 400, and few or no divergent transitions on every monitored quantity. Fits are checked against these criteria in the next section.
@@ -729,7 +819,7 @@ ar_resolutions <- list()  # track AR resolution per fit
 
 for(cg in bss_catch_groups) {
   for(pop in c("shore","private_boat")) {
-    for(ss in list(subseason_1, subseason_2)) {
+    for(ss in subseasons) {
       label <- paste0(pop, "_", ss$name, "_", cg)
       cat(sprintf("\n--- BSS: %s ---\n", label))
 
@@ -759,8 +849,8 @@ for(cg in bss_catch_groups) {
 
       is_shore   <- (pop == "shore")
       is_boat    <- (pop == "private_boat")
-      is_allgear <- (ss$name == "all_gear")
-      is_ringnet <- (ss$name == "ring_net_only")
+      is_allgear <- (ss$gear_regime == "all_gear")
+      is_ringnet <- (ss$gear_regime == "pot_closure")
 
       # v6.1/v6.2: per-fit sampler tuning. Each branch sets iter, warmup,
       # max_treedepth, and adapt_delta; any fit without a specific override
@@ -987,7 +1077,7 @@ for(cg in bss_catch_groups) {
   bss_E_total <- rep(0, n_draws_max)
 
   for(pop in c("shore","private_boat")) {
-    for(ss in list(subseason_1, subseason_2)) {
+    for(ss in subseasons) {
       label <- paste0(pop, "_", ss$name, "_", cg)
       pe_label <- paste0(pop, "_", ss$name)
       b <- bss_all[[label]]
@@ -1055,7 +1145,7 @@ monthly_E_draws <- matrix(0, nrow = n_draws_max, ncol = length(all_months),
 
 for(cg in bss_catch_groups) {
   for(pop in c("shore","private_boat")) {
-    for(ss in list(subseason_1, subseason_2)) {
+    for(ss in subseasons) {
       label <- paste0(pop, "_", ss$name, "_", cg)
       pe_label <- paste0(pop, "_", ss$name)
       b <- bss_all[[label]]
@@ -1191,7 +1281,7 @@ The monthly trajectory split by mode (shore, private boat, commercial/charter).
 #          from the daily C_expected draws, then 2.5/50/97.5 quantiles.
 #   Private Boat: method is per sub-season (v7.0 scale-aware gate, B1.8). On the
 #          2024-25 data the all-gear sub-season passes the gate and uses the BSS
-#          posterior (full daily C_expected draws, CV ~27%), while the ring-net
+#          posterior (full daily C_expected draws, CV ~27%), while the pot-closure
 #          sub-season has too few interviews (< bss_min_interviews) and uses PE.
 #          The PE branch distributes the selected PE point across months by monthly
 #          effort share, then applies a design-based relative SE (stratified effort
@@ -1229,7 +1319,7 @@ rln_scale <- function(n, rel_se) {
 for(cg in bss_catch_groups) {
   for(pop in c("shore","private_boat")) {
     mode_nm <- if(pop == "shore") "Shore" else "Private Boat"
-    for(ss in list(subseason_1, subseason_2)) {
+    for(ss in subseasons) {
       label    <- paste0(pop, "_", ss$name, "_", cg)
       pe_label <- paste0(pop, "_", ss$name)
       b <- bss_all[[label]]
@@ -1342,7 +1432,7 @@ p_bymode_dodge <- monthly_bymode |>
                 position = position_dodge(width = 0.8), width = 0.3) +
   scale_fill_manual(values = mode_cols) +
   labs(title = "Monthly Dungeness Catch by Crabbing Mode",
-       subtitle = "Median with 95% interval. Shore: BSS posterior. Private Boat: BSS posterior (all-gear), PE point with design-based effort SE (ring-net). Commercial/Charter: census, catch-per-vessel CV.",
+       subtitle = "Median with 95% interval. Shore: BSS posterior. Private Boat: BSS posterior (all-gear), PE point with design-based effort SE (pot closure). Commercial/Charter: census, catch-per-vessel CV.",
        x = "Month", y = "Dungeness crab caught", fill = "Mode") +
   theme_bw() +
   theme(axis.text.x = element_text(angle = 45, hjust = 1), legend.position = "top")
@@ -1844,7 +1934,7 @@ if (exists('multiplier_df')) report_table(multiplier_df, 'Fitted effort and CPUE
 
 ### 12.1 Expansion ratios (R_G and R_G_boat)
 
-The gear-per-crabber ratio (shore) and gear-per-boat-group ratio (boat) that carry effort onto the gear-deployment scale.
+The gear-per-crabber ratio (shore) and gear-per-boat-group ratio (boat) that carry effort onto the gear-deployment scale. The R_G posterior below is under the model's data-driven R_G prior by default. For the T1.3 prior-sensitivity analysis the prior is overridable from `run_config.R` (`R_G_prior_mu` / `R_G_prior_sigma`); the console log records whether the prior was data-driven or an override, and this report reflects whichever prior was active for the run. A full sensitivity is a small grid of runs (R_G at 1.0, the empirical value, and 1.5) compared on the port total, not a single knit.
 
 ```{r expansion, results='hide'}
 # ---- 7.16 R_G AND R_G_boat POSTERIORS ----
@@ -1895,7 +1985,7 @@ cat("\n", strrep("=",60), "\n PE VS BSS COMPARISON\n", strrep("=",60), "\n")
 
 comparison <- list()
 for(pop in c("shore","private_boat")) {
-  for(ss in list(subseason_1, subseason_2)) {
+  for(ss in subseasons) {
     label_cg <- paste0(pop, "_", ss$name, "_Dungeness_Kept")
     pe_label <- paste0(pop, "_", ss$name)
     b <- bss_all[[label_cg]]
@@ -1917,7 +2007,7 @@ for(pop in c("shore","private_boat")) {
     }
 
     comparison[[length(comparison)+1]] <- tibble(
-      component = paste0(pop, " (", ss$name, ")"),
+      component = paste0(pop, " (", ss$display_name, ")"),
       PE_effort = round(pe_effort), BSS_effort = round(bss_effort),
       effort_diff_pct = effort_diff_pct,
       PE_catch = round(pe_catch), BSS_catch = round(bss_catch),
@@ -2026,9 +2116,16 @@ These are run by hand after a fit to localize any divergences; they are not part
 # load('output/20260619/pooled-CPUE/BSS-GH-pooled-CPUE-model_workspace.RData')
 
 source(here::here("03_R_functions/divergence_diagnostic.R"))
-diagnose_divergences(bss_all[["shore_ring_net_only_Dungeness_Kept"]]$fit)
-diagnose_divergences(bss_all[["shore_all_gear_Dungeness_Kept"]]$fit)
-diagnose_divergences(bss_all[["private_boat_all_gear_Dungeness_Kept"]]$fit)
+# Robust to the sub-season set (build_subseasons): iterate over every fitted BSS
+# component and skip PE-fallback components that have no Stan fit, rather than
+# hardcoding fit labels (which omitted private_boat_ring_net_only and would miss the
+# pre/post all-gear keys of a mid-season closure).
+for (nm in names(bss_all)) {
+  b <- bss_all[[nm]]
+  if (is.null(b) || isTRUE(b$pe_fallback) || is.null(b$fit)) next
+  cat(sprintf("\n=== Divergence diagnostics: %s ===\n", nm))
+  diagnose_divergences(b$fit)
+}
 ```
 
 ### 11.3 Divergence localization
diff --git a/03_R_functions/build_subseasons.R b/03_R_functions/build_subseasons.R
new file mode 100644
index 0000000..b8dd3f0
--- /dev/null
+++ b/03_R_functions/build_subseasons.R
@@ -0,0 +1,67 @@
+###############################################################################
+# build_subseasons.R  (shared by the pooled and gear-resolved drivers)
+#
+# Build the list of within-season sub-seasons, split by the pot-closure window.
+#
+# The "pot closure" is the period when pots are not legal, so only non-pot gear
+# (ring nets, snares, traps) may be used. It is NOT ring-net-only, so the internal
+# label "ring_net_only" is kept only for output-filename continuity; the display_name
+# is "Pot closure" and user-facing strings should use it. Outside the closure window,
+# pots are allowed (all-gear).
+#
+# The closure window is given explicitly by params$pot_closure_start /
+# params$pot_closure_end (added 2026-07-13). It is no longer assumed to start at the
+# season start. This builder therefore handles the general case:
+#   - an optional pre-closure all-gear period  [est_date_start, pot_closure_start - 1]
+#   - the pot closure                          [pot_closure_start, pot_closure_end]
+#   - an optional post-closure all-gear period [pot_closure_end + 1, est_date_end]
+# A zero-length period is dropped.
+#
+# Backward compatibility: when pot_closure_start == est_date_start (the historical
+# assumption, and the 2024-25 config), there is no pre-closure period and the result
+# is the historical two-element list with the SAME internal names ("ring_net_only",
+# "all_gear"), so keys, Stan fit labels, and output filenames are unchanged. Only
+# when a mid-season closure is configured do the two all-gear periods take distinct
+# names ("all_gear_pre" / "all_gear_post") to keep the population keys unique.
+#
+# Defaults: if params$pot_closure_start / _end are absent, they fall back to the old
+# derivation (season start, and the day before params$pot_open_date), so an old config
+# still runs. Each element also carries gear-model fields (period_bss, gear_exclude)
+# that are inert for the pooled model.
+#
+# The model logic keys off gear_regime ("pot_closure" | "all_gear"), never off name.
+###############################################################################
+
+build_subseasons <- function(params) {
+  est_start <- as.Date(params$est_date_start)
+  est_end   <- as.Date(params$est_date_end)
+  pc_start  <- as.Date(params$pot_closure_start %||% params$est_date_start)
+  pc_end    <- as.Date(params$pot_closure_end   %||% (as.Date(params$pot_open_date) - 1))
+
+  if (pc_start < est_start) pc_start <- est_start
+  if (pc_end   > est_end)   pc_end   <- est_end
+  if (pc_end < pc_start)
+    stop("build_subseasons(): pot_closure_end is before pot_closure_start.", call. = FALSE)
+
+  has_pre  <- pc_start > est_start
+  has_post <- pc_end   < est_end
+
+  allgear <- function(nm, s, e, disp) list(
+    name = nm, display_name = disp, gear_regime = "all_gear",
+    start = s, end = e, period_bss = "month", gear_exclude = character(0))
+  closure <- list(
+    name = "ring_net_only", display_name = "Pot closure", gear_regime = "pot_closure",
+    start = pc_start, end = pc_end, period_bss = "biweekly", gear_exclude = c("Pot"))
+
+  ss <- list()
+  if (has_pre)
+    ss <- c(ss, list(allgear(if (has_post) "all_gear_pre" else "all_gear",
+                             est_start, pc_start - 1,
+                             if (has_post) "All gear (pre-closure)" else "All gear")))
+  ss <- c(ss, list(closure))
+  if (has_post)
+    ss <- c(ss, list(allgear(if (has_pre) "all_gear_post" else "all_gear",
+                             pc_end + 1, est_end,
+                             if (has_pre) "All gear (post-closure)" else "All gear")))
+  ss
+}
diff --git a/03_R_functions/estimate_comm_charter.R b/03_R_functions/estimate_comm_charter.R
index 69304ca..6767970 100644
--- a/03_R_functions/estimate_comm_charter.R
+++ b/03_R_functions/estimate_comm_charter.R
@@ -131,11 +131,24 @@ estimate_comm_charter <- function(dwg, params) {
   total_dung <- sum(strat$est_total_dung)
   total_vessels <- sum(strat$est_total_vessels)
 
+  # T1.4: per-vessel-type split summary for the report (a sampled-day census, i.e.
+  # before the day-type expansion that produces the headline total_dung).
+  comm_tally_n <- sum(tally$commercial_tally, na.rm = TRUE)
+  char_tally_n <- sum(tally$charter_tally,    na.rm = TRUE)
+  vessel_type_detail <- tibble(
+    vessel_type          = c("Commercial", "Charter"),
+    n_interviews         = c(n_comm, n_char),
+    mean_dung_per_vessel = c(md_comm, md_char),
+    tally_vessels        = c(comm_tally_n, char_tally_n),
+    sampled_catch        = c(comm_tally_n * md_comm, char_tally_n * md_char)
+  )
+
   result <- list(
     Dungeness_Kept = total_dung,
     effort_total = total_vessels,
     daily_est = daily_est,
-    strat_detail = strat
+    strat_detail = strat,
+    vessel_type_detail = vessel_type_detail
   )
 
   if(params$estimate_red_rock) {
diff --git a/03_R_functions/run_pe_pooled.R b/03_R_functions/run_pe_pooled.R
index 6d2cd8b..dd64bc8 100644
--- a/03_R_functions/run_pe_pooled.R
+++ b/03_R_functions/run_pe_pooled.R
@@ -83,6 +83,9 @@ run_pe_pooled <- function(summ, days, params, population_name) {
   # in lockstep with the shore effort unit and the shore BSS.
   hrs_col <- if(is_boat) "number_of_gear" else bss_effort_spec(TRUE, days, params)$h_col
 
+  # P0: accumulate the per-catch-group PE internal-consistency check (implied CPUE vs
+  # interview ratio-of-sums) so the driver can render it in the report (Section 4).
+  pe_check_rows <- list()
   for(cg in catch_groups) {
     if(!cg %in% names(summ$interview)) { results[[cg]] <- 0; next }
     # POOL-2: drop incomplete trips from the PE CPUE too, so the PE and BSS CPUE
@@ -129,6 +132,8 @@ run_pe_pooled <- function(summ, days, params, population_name) {
       rel <- implied / ros
       cat(sprintf("  PE check [%s / %s]: implied CPUE %.4f vs interview ratio-of-sums %.4f (%.2fx) [%s]\n",
                   population_name, cg, implied, ros, rel, effort_unit_pe))
+      pe_check_rows[[cg]] <- tibble(catch_group = cg, effort_unit = effort_unit_pe,
+                                    pe_implied_cpue = implied, interview_ros = ros, ratio = rel)
       if (rel < 0.5 || rel > 2.0) {
         stop(sprintf(paste0("run_pe_pooled(): PE implied CPUE (%.4f) is %.2fx the interview ",
                             "ratio-of-sums (%.4f) for %s / %s. Catch and effort are not on ",
@@ -139,6 +144,7 @@ run_pe_pooled <- function(summ, days, params, population_name) {
   }
 
   results$effort_unit <- effort_unit_pe
+  results$pe_cpue_check <- if (length(pe_check_rows)) bind_rows(pe_check_rows) else NULL
   rr_str <- if(params$estimate_red_rock) sprintf(", RR=%s", format(round(results$Red_Rock_Kept),big.mark=",")) else ""
   cat(sprintf("  PE %s: Effort=%s %s, Dung=%s%s\n", population_name,
               format(round(results$effort_total),big.mark=","), effort_unit_pe,
diff --git a/07_documentation/BSS-GH-gear-type-CPUE-model-documentation.md b/07_documentation/BSS-GH-gear-type-CPUE-model-documentation.md
index 5276954..663a80b 100644
--- a/07_documentation/BSS-GH-gear-type-CPUE-model-documentation.md
+++ b/07_documentation/BSS-GH-gear-type-CPUE-model-documentation.md
@@ -144,16 +144,18 @@ The season is split into two **sub-seasons**, defined by the pot closure, and ea
 
 | Sub-season | Typical dates | Gear allowed | Gear types modeled | BSS period type |
 |---|---|---|---|---|
-| Ring-net only | Sep 16 to Nov 30 | Ring nets, snares, foldable traps | 3 (Ring Net, Trap, Snare) | Biweekly |
+| Pot closure | Sep 16 to Nov 30 | Ring nets, snares, foldable traps | 3 (Ring Net, Trap, Snare) | Biweekly |
 | All-gear | Dec 1 to Sep 15 | All gear including crab pots | 4 (Pot, Ring Net, Trap, Snare) | Monthly |
 
+The "pot closure" sub-season was formerly labeled "ring-net only", a misnomer since non-pot gear other than ring nets is also legal in the closure; the internal key stays `ring_net_only` for output-filename continuity and the reports display "Pot closure". The closure window is set explicitly in `run_config.R` via `pot_closure_start` / `pot_closure_end` (added 2026-07-13) rather than assumed to run from the season start to `pot_open_date - 1`; the shared builder `03_R_functions/build_subseasons.R` derives the sub-seasons and adds pre/post all-gear periods automatically if the closure falls mid-season. Season plots mark the closure start and the pots-open date with vertical lines.
+
 The number of gear types set up for modeling in a sub-season is determined by two filters applied in sequence.
 
-**Regulatory exclusion.** Each sub-season carries a `gear_exclude` list of gear prohibited by regulation during that period. For the ring-net sub-season, pots are excluded. This is enforced structurally: regardless of interview contents, the model will not set up a CPUE process for a prohibited gear type. Any interview that mentions an excluded gear type (typically a multi-gear crabber naming gear they own rather than what they deployed) has its fractional weight redistributed to the remaining reported types. An interview recorded as "Pot, Ring Net" during the ring-net sub-season contributes 100% of its weight to Ring Net rather than 50% to each. This matters because even a handful of phantom gear mentions (5 to 6 fractional interviews in a 76-day sub-season) can, with near-zero data to constrain a CPUE process, drive hundreds of divergent transitions and overflow in the generated catch. Encoding the known fishery structure prevents that.
+**Regulatory exclusion.** Each sub-season carries a `gear_exclude` list of gear prohibited by regulation during that period. For the pot-closure sub-season, pots are excluded. This is enforced structurally: regardless of interview contents, the model will not set up a CPUE process for a prohibited gear type. Any interview that mentions an excluded gear type (typically a multi-gear crabber naming gear they own rather than what they deployed) has its fractional weight redistributed to the remaining reported types. An interview recorded as "Pot, Ring Net" during the pot-closure sub-season contributes 100% of its weight to Ring Net rather than 50% to each. This matters because even a handful of phantom gear mentions (5 to 6 fractional interviews in a 76-day sub-season) can, with near-zero data to constrain a CPUE process, drive hundreds of divergent transitions and overflow in the generated catch. Encoding the known fishery structure prevents that.
 
 **Minimum effective-N threshold.** After regulatory exclusions, each remaining gear type must have at least `bss_min_gear_effective_n` effective interviews (default 15), computed as the sum of fractional gear weights, to be set up independently. The threshold is conservative because fractional weights from multi-gear interviews can inflate the effective count. If fewer than two gear types qualify, the model falls back to a single "All" category.
 
-The ring-net sub-season uses biweekly BSS periods rather than monthly: with only about 76 days and 2.5 calendar months, monthly periods give too few AR(1) transitions for effective temporal smoothing, while biweekly periods yield about 5 to 6 periods.
+The pot-closure sub-season uses biweekly BSS periods rather than monthly: with only about 76 days and 2.5 calendar months, monthly periods give too few AR(1) transitions for effective temporal smoothing, while biweekly periods yield about 5 to 6 periods.
 
 **Day type and the holiday effect.** Each day is classified as weekday (Mon-Thu), weekend (Fri-Sun), or holiday. The effort process includes two separate effects: a weekend boost (B1) and an additional holiday boost beyond the weekend (B2), because holidays generate substantially higher effort than regular weekends. Gear-type proportions vary by day type as well as by period. The single season list of crabbing holidays lives in `run_config.R`.
 
diff --git a/07_documentation/BSS-GH-pooled-CPUE-model-documentation.md b/07_documentation/BSS-GH-pooled-CPUE-model-documentation.md
index 04ddc81..b3756d6 100644
--- a/07_documentation/BSS-GH-pooled-CPUE-model-documentation.md
+++ b/07_documentation/BSS-GH-pooled-CPUE-model-documentation.md
@@ -93,9 +93,11 @@ The harvest is built from three components, estimated separately and summed:
 
 The season is split into two **sub-seasons**, defined by the pot closure, and each is estimated independently:
 
-- **Ring-net only** (Sep 16 to Nov 30): pots prohibited.
+- **Pot closure** (Sep 16 to Nov 30): pots prohibited; non-pot gear (ring nets, snares, traps) allowed. Formerly labeled "ring-net only", a misnomer since gear other than ring nets is also legal during the closure. The internal key stays `ring_net_only` for output-filename continuity; the reports display "Pot closure".
 - **All-gear** (Dec 1 to Sep 15): pots allowed.
 
+The closure window is set explicitly in `run_config.R` via `pot_closure_start` and `pot_closure_end` (added 2026-07-13), so a future season whose start does not coincide with the closure start is supported. The shared builder `03_R_functions/build_subseasons.R` derives the sub-seasons from that window and adds pre/post all-gear periods automatically if the closure falls mid-season. Season plots mark the closure start and the pots-open date with vertical lines.
+
 ------------------------------------------------------------------------
 
 ## 6. Where this method is valid
@@ -138,7 +140,7 @@ Method v1.0 is calibrated to the Westport / Grays Harbor fishery as sampled in t
 1. **Place the new season's data** in `04_input_files/`, keeping the four filenames and their column schemas unchanged. Honor the schema quirks in the input-folder README (the interview gear column maps from column N; re-export the effort CSV with full quoting; dates are M/D/YYYY; the "Commerical" boat-type spelling is matched by regex).
 2. **Edit `run_config.R` (repo root). This is the ONE file you edit.** As of the 2026-07-11 consolidation it is the single source of truth for every user-selectable toggle; the driver's `params` chunk holds only this model's internal tuning (the Stan file, per-fit sampler settings, gate thresholds, AR-selector thresholds), which rarely changes and legitimately differs from the gear-resolved model. The keys you normally set:
    - `est_date_start`, `est_date_end`: the season window. The driver fits each sub-season inside this window.
-   - `pot_open_date`, `commercial_opener`, `census_start_date`, `census_end_date`: the regulatory / structural dates.
+   - `pot_open_date`, `pot_closure_start`, `pot_closure_end`, `commercial_opener`, `census_start_date`, `census_end_date`: the regulatory / structural dates. `pot_closure_start` / `pot_closure_end` bound the pot-closure sub-season explicitly (keep `pot_open_date` = `pot_closure_end` + 1).
    - `shore_effort_unit` (default `gear-deployments`), `filter_incomplete_trips` (default on), and the `tau_shore` / `tau_boat` turnover priors: the effort-unit and CPUE-denominator controls (Sections 11, 14, 15).
    - `ie_data_file`, `ie_sheet`: the I/E workbook and sheet (defaults `ingress_egress.xlsx`, `data`).
    - `crabbing_holiday_dates`: update this one list each season.
@@ -270,7 +272,7 @@ Method v1.0 is built for one fishery under one sampling design. It can be re-run
 - **Same location.** Westport / Grays Harbor access points (docks Floats 17-21, the jetty, beaches, the boat launch, the marina). The gear-per-crabber prior `R_G`, the gear-per-boat-group prior `R_G_boat`, and the effective-day-length regression are all calibrated to this site.
 - **Same input streams, same schema.** The four input files in the same form (Section 7).
 - **Same sampling design.** Instantaneous effort counts, dockside interviews, the commercial tally, and I/E surveys, collected as in 2024-25. The 2024-25 protocol of three randomized effort counts per day is the design the effort expansion assumes (it measures mean daily effort).
-- **Same sub-season structure.** Ring-net only Sep 16 to Nov 30; all-gear Dec 1 to Sep 15, tied to the pot closure.
+- **Same sub-season structure.** Pot closure Sep 16 to Nov 30 (non-pot gear only); all-gear Dec 1 to Sep 15, tied to the pot closure. The closure window is set explicitly via `pot_closure_start` / `pot_closure_end`.
 
 **Conditions that require re-derivation, not just new data:**
 
diff --git a/07_documentation/development_notes/PIPELINE_STATUS.md b/07_documentation/development_notes/PIPELINE_STATUS.md
index eb50732..658bc04 100644
--- a/07_documentation/development_notes/PIPELINE_STATUS.md
+++ b/07_documentation/development_notes/PIPELINE_STATUS.md
@@ -1,6 +1,6 @@
 # Coastal Rec Crab BSS: Pipeline Status and Backlog
 
-**Last updated:** 2026-07-12
+**Last updated:** 2026-07-13
 **Maintainer note:** this is the single living status document for the pipeline. It replaces the six scattered development notes listed in Section 8, reconciling their issue IDs so nothing is lost. Update this file as work lands; do not re-fork it into per-session notes.
 
 **Repo:** `Coastal-Rec-Crab-BSS`, `main`. **Method of record:** Method v1.0 (frozen against pooled code v7.4); the code has advanced to v7.9 with effort-unit, filtering, and config corrections that move published totals, so the 2024-25 reference numbers in the method documents are pre-refresh until regenerated.
@@ -22,14 +22,14 @@
 
 | Component | PE | BSS (median) | BSS vs PE | Method used |
 |---|---:|---:|---:|---|
-| Shore ring-net (Sep 16 - Nov 30) | 5,884 | 6,586 | +11.9% | BSS |
+| Shore pot-closure (Sep 16 - Nov 30) | 5,884 | 6,586 | +11.9% | BSS |
 | Shore all-gear (Dec 1 - Sep 15) | 15,732 | 20,654 | +31.3% | BSS |
 | Private boat all-gear | 22,070 | 43,475 | +97% | BSS |
-| Private boat ring-net | 971 | n/a | n/a | PE (insufficient data) |
+| Private boat pot-closure | 971 | n/a | n/a | PE (insufficient data) |
 | Commercial/charter | 12,007 | n/a | n/a | Census |
 | **Port total** | **56,665** | **83,914** | **+48%** | gated combination |
 
-**Convergence:** all three fitted components pass (shore ring-net daily 2.5% divergences; shore all-gear daily 2.23%; boat all-gear monthly 0.63%); R-hat near 1.00, n_eff well above 400. Boat ring-net is the only PE fallback and it is data-limited (~17 interviews), not a geometry failure.
+**Convergence:** all three fitted components pass (shore pot-closure daily 2.5% divergences; shore all-gear daily 2.23%; boat all-gear monthly 0.63%); R-hat near 1.00, n_eff well above 400. Boat pot-closure is the only PE fallback and it is data-limited (~17 interviews), not a geometry failure.
 
 **The three facts that most shape how to read the numbers:**
 1. **The boat is now cross-model consistent.** On monthly AR the pooled boat (catch 43,475, effort 14,716, CPUE 2.95) matches the independent gear-resolved boat (43,314 / 14,805 / 2.93). The weekly-vs-monthly AR resolution, not the effort unit, was the entire source of the earlier ~26% pooled-vs-gear-resolved boat gap.
@@ -82,6 +82,7 @@ Historical IDs are preserved in parentheses so the older notes remain traceable.
 
 **Refactor and config.**
 - Behavior-preserving function extraction into `03_R_functions/` and centralization of all user toggles in `run_config.R` (v7.8). Config restructured so `run_config` is the base parameter set and each model layers its tuning (P5/v7.9); the per-model AR resolution map lives in `run_config.R`.
+- **Pot-closure sub-season made explicit and renamed (2026-07-13).** The pot-closure window (when pots are illegal and only non-pot gear is legal) is now set explicitly in `run_config.R` via `pot_closure_start` / `pot_closure_end`, instead of being assumed to run from the season start to `pot_open_date - 1`, so a future season whose start does not coincide with the closure is supported. A shared builder `03_R_functions/build_subseasons.R` derives the sub-seasons for both drivers and handles the general case (optional pre/post all-gear periods for a mid-season closure, keyed off `gear_regime` rather than the sub-season name). The sub-season was renamed "ring-net only" to "pot closure": a **display-only** rename, since non-pot gear other than ring nets is legal, so the old name was a misnomer; the internal key stays `ring_net_only` for output-filename continuity, so keys and filenames are unchanged and the 2024-25 config produces bit-identical sub-seasons. Season plots in both drivers now draw vertical lines at the closure start and the pots-open date. **Known limitation:** the builder is general, but the report's downstream aggregation (PE port summary `component_keys` / `component_names`, catch-by-mode, and the final-table PE-fallback rows) is still hardcoded to the two historical sub-seasons per population; a fail-fast guard in each driver stops the run if a mid-season closure is configured (sub-season name set != {ring_net_only, all_gear}), rather than silently under-reporting the port total. Generalizing those aggregators to sum over all sub-seasons per population is the follow-up (relates to T3.1).
 
 **PE (partial).**
 - Shore PE unit alignment done (v7.8, above). Ratio-of-sums adoption (P0) is NOT done and is the top open PE item (Section 4).
@@ -97,11 +98,11 @@ Historical IDs are preserved in parentheses so the older notes remain traceable.
 
 Status note: the four Tier 1 items were worked on 2026-07-12. Three are implemented in code (P0, T1.1a, T1.4) and need a confirming run; the prior-sensitivity harness (T1.3) is in place but the sweep runs are pending; external validation (T1.1b) still needs benchmark data.
 
-- **P0 [DONE 2026-07-12, confirm on next run]: pooled PE on ratio-of-sums.** `run_pe_pooled` now computes stratum CPUE as within-stratum `sum(catch)/sum(hrs)`, replacing the unstable `weighted.mean(daily_ratios, w = n_int)`, and carries an implied-CPUE-vs-ratio-of-sums guard that fails fast (in the PE section, before the multi-hour BSS fits) if catch and effort drift onto different scales. This mirrors `run_pe_gear`, so the two tracks' PE cannot diverge. Expected effect on the next run: the boat PE moves from 22,070 toward ~40k (near the BSS and the gear-resolved boat), and the shore PE implied CPUE drops from ~2.9x its ratio-of-sums toward 1x, making the PE a fair cross-check.
+- **P0 [DONE 2026-07-12, confirm on next run]: pooled PE on ratio-of-sums.** `run_pe_pooled` now computes stratum CPUE as within-stratum `sum(catch)/sum(hrs)`, replacing the unstable `weighted.mean(daily_ratios, w = n_int)`, and carries an implied-CPUE-vs-ratio-of-sums guard that fails fast (in the PE section, before the multi-hour BSS fits) if catch and effort drift onto different scales. This mirrors `run_pe_gear`, so the two tracks' PE cannot diverge. Expected effect on the next run: the boat PE moves from 22,070 toward ~40k (near the BSS and the gear-resolved boat), and the shore PE implied CPUE drops from ~2.9x its ratio-of-sums toward 1x, making the PE a fair cross-check. The estimator change and its per-component internal-consistency check now render in the knitted report (Section 4.4).
 - **T1.1(a) [DONE 2026-07-12]: summer-extrapolation transparency.** A new report section (`extrapolation_transparency.csv` plus an on-page table, RMD Section 11.4) decomposes each fitted component's BSS catch into interview-days vs extrapolated (no-interview) days, and reports the share of the summed BSS catch that rests on extrapolated time. Turns the summer-dominance caveat into a number.
 - **T1.1(b) [OPEN, needs external data]: external validation (B4).** Still requires a benchmark the repo does not contain: dockside census totals, a known-total year, or the legacy estimator. The strongest internal cross-checks now in place are the two independent BSS pipelines agreeing on the boat (43.5k) and the per-component PE-vs-BSS comparison (fair once P0's run lands); neither is a true external check. Pursue a benchmark before publication.
 - **T1.3 [HARNESS DONE 2026-07-12, sweep runs pending]: R_G prior sensitivity.** The R_G prior is now overridable via `params$R_G_prior_mu` / `R_G_prior_sigma` (data-driven when unset), with a commented sweep block in `run_config.R`. Remaining work is the runs: set `R_G_prior_mu` to 1.0, ~1.28 (empirical), and 1.5 in turn and tabulate the port totals, then extend to the `L_effective` prior (`tau_shore`/`tau_boat` are already live toggles). `prior_vs_posterior_*.csv` (O9) shows the prior pull. **Effort: the runs (multi-hour each).**
-- **T1.4 [DONE 2026-07-12, confirm on next run]: commercial/charter vessel split.** `estimate_comm_charter` now applies separate per-vessel Dungeness (and red-rock) means for commercial vs charter vessels (from `boat_type_clean`, Guide folded into Charter) to the matching tally columns, with a fallback to the pooled mean for a class with no interviews. In the 2024-25 census window commercial is ~35 and charter ~51 crab/vessel, so the split corrects a real bias. Applies to both tracks (shared estimator). Pair with a census uncertainty interval (T3.5).
+- **T1.4 [DONE 2026-07-12, confirm on next run]: commercial/charter vessel split.** `estimate_comm_charter` now applies separate per-vessel Dungeness (and red-rock) means for commercial vs charter vessels (from `boat_type_clean`, Guide folded into Charter) to the matching tally columns, with a fallback to the pooled mean for a class with no interviews. In the 2024-25 census window commercial is ~35 and charter ~51 crab/vessel, so the split corrects a real bias. Applies to both tracks (shared estimator). The per-vessel-type split renders in the knitted report (Section 4.2). Pair with a census uncertainty interval (T3.5).
 
 ### Tier 2: accuracy, cross-track parity, or credibility
 
diff --git a/run_config.R b/run_config.R
index b7a3de2..3aceab7 100644
--- a/run_config.R
+++ b/run_config.R
@@ -68,7 +68,17 @@ run_config <- list(
   season_filter     = "2024-25",
 
   # --- Regulatory / structural dates ---------------------------------------
-  pot_open_date     = "2024-12-01",   # pots legal from this date (sub-season split)
+  pot_open_date     = "2024-12-01",   # pots legal from this date (used for L_effective
+                                      #   and as the default pot_closure_end + 1 day)
+  # Pot-closure window: the period when pots are NOT legal (only non-pot gear, ring
+  # nets/snares/traps). Given explicitly here rather than assumed to start at the
+  # season start, so a future season whose start does not coincide with the closure
+  # start is supported. Outside this window pots are allowed (all-gear). If a closure
+  # starts after est_date_start or ends before est_date_end, the driver adds the
+  # corresponding all-gear period(s) automatically (see 03_R_functions/build_subseasons.R).
+  # Keep pot_open_date = pot_closure_end + 1.
+  pot_closure_start = "2024-09-16",   # first day of the pot closure (here = season start)
+  pot_closure_end   = "2024-11-30",   # last day pots are illegal (day before pots open)
   commercial_opener = "2025-01-01",   # (was malformed "2025-01-1" in gear-resolved)
   census_start_date = "2024-12-01",
   census_end_date   = "2025-02-08",
