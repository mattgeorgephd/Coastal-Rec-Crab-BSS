# 06_diagnostics

**The validation harness and the dated batch runners**, plus the experimental weather-tide covariate module. Nothing in this folder is a production estimator; the estimate comes from the drivers in `01_BSS_models/`. What lives here is the machinery that decides whether a change to those drivers is safe and worth keeping:

- `test_improvements_2026-08-25.R`, the dependency-light regression harness. Run it before committing to any long fit.
- `run_*.R`, one dated batch runner per validation campaign. Each ships `DRY_RUN <- TRUE` (asserted by the harness), resolves every stage's config and runs its pre-flight without fitting, is resumable, and writes its pass/fail verdicts to a `*_verdicts.csv` at the `05_output/` root. The convention that grew out of hard experience: pre-set the pass criteria in code BEFORE the run; compare per-fit summaries for exactness, never port totals (permuted draws move a port total ~0.2% between bit-identical fits); expect a config delta of exactly the keys the stage changes (`config_delta`); wrap every verdict block so a defect in the READING of a run can never destroy the run's output.
- The weather-tide covariate driver, experimental and currently stale.

Each runner's row below records whether it has run and where its results were reviewed. **For where the model currently stands**, read the box at the top of `07_documentation/development_notes/PIPELINE_STATUS.md` and the change register `07_documentation/development_notes/CHANGE_REGISTER.md`.

For the production models, see [`01_BSS_models/`](../01_BSS_models/README.md); for the project overview, the [root README](../README.md).

## Files

| File | Role |
|---|---|
| `BSS-GH-pooled-CPUE-weather-tide-covariates.Rmd` | The covariate module driver (module v0.2.x). Layered on the **pooled** model only. Screens candidate tide/weather covariates with daily GAMs, fits a covariate-augmented BSS alongside the baseline, and compares them with PSIS-LOO (with a leave-one-week-out block-CV fallback for true sampling gaps). |
| `run_rg_sweep.R` | The T1.3 `R_G` prior-sensitivity sweep runner (three pooled runs at `R_G_prior_mu` = 1.0/1.28/1.5). |
| `run_osp_validation.R` | Batches the OSP validation ladder across both production models and appends to `05_output/osp_validation_summary.csv`. A validation/diagnostic runner, not a production estimator. |
| `run_patch_validation_2026-08-25.R` | The validation ladder for the 2026-08-25 improvement batch: one change per rung, with the pass criteria written down in code BEFORE the runs. It ships `DRY_RUN <- TRUE` (it was left FALSE from 2026-08-25 until 2026-09-03, so a source started real fits); that prints every rung's resolved config and self-tests the extractor and the criteria against the two reference runs without fitting anything. Six model-runs, roughly 16-32 h; resumable. |
| `gear_coverage_audit.R` | Standalone audit of per-gear interview coverage against the gear-model's effective-N threshold. NOTE it sources `run_config.R` only, while `bss_min_gear_effective_n` lives in the gear driver's `params_model`, so its threshold always falls back to the literal 15; it agrees with production today but would not follow a change. Writes `gear_coverage_audit.csv` to the REPO ROOT, which `.gitignore` does not cover. |
| `run_improvement_plan_2026-08-27.R` | The 2026-08-27 improvement batch (stages A-F). Reviewed in `improvement-batch-review-2026-08-29.md`. |
| `run_tau_sweep.R` | Three-point `tau_boat` prior sweep. **Superseded**: `diagnose_tau_sensitivity` reproduces it to ~0.2% on every run, and `shared_tau = TRUE` (2026-09-01) replaces the per-day draws it perturbs. Its header baselines are stale; see the warning at the top of the file. |
| `test_improvements_2026-08-25.R` | Dependency-light regression harness (no rstan, seconds to run) for the functions the 2026-08-25 batch added or changed, plus a guard on the shipped defaults in `run_config.R`, on the sampler-override whitelist, on the retro audit tables, and on the Stage 5 runner's hard-coded baselines. **452 assertions** as of 2026-09-08. Run it before committing to any long fit. Sections 35-44 target the shape most defects here have taken, a change validated by the mechanism it was about to break: they read the driver SOURCE and assert the property that failed, rather than a helper's return value, and they pin the shipped `run_config` defaults (including the 2026-09-07 adoption: weekly shore AR, ZINB on, no experiment lever active). |
| `run_stage5_2026-08-30.R` | Unattended batch for Stage 5 of `improvement-plan-2026-08-27.md`: S1 PE-fill re-run (minutes, no MCMC), S2 the `shared_tau` informed-day floor as an exact per-population bit-identity GATE, S3 the gear-track cross-check with the pooled track's sampler settings, S4a/S4b the tau x AR 2x2 (two runs, not one; stage F is not a matched control), S5 the uncontaminated boat pot-closure AR re-run. Ships with `DRY_RUN <- TRUE`, which resolves every config, runs the R-to-Stan data-contract pre-flight and a 22-assertion extractor self-test against the archived runs, and fits nothing. Roughly 18-24 h with `DRY_RUN <- FALSE`; resumable. **Ran 2026-08-30** (24 h; 11 of 12 criteria passing, the one FAIL being a criterion compared against a reference that differed in two ways); results reviewed in `07_documentation/development_notes/stage5-batch-review-2026-08-31.md`. |
| `run_validation_2026-09-01.R` | The outstanding validation and diagnosis work after the 2026-09-01 adoption. FOUR DESK STAGES (D1-D4) that run during the dry run, need no Stan toolchain, and score backlog items against data already on disk: the Tier-1 shore I/E unit fix against its four pre-set criteria, the `gear_only` arm decision and the PE-vs-BSS arm disagreement, the PPC coverage and zero-bin reads, and a repository-hygiene audit. Then FIVE FITTED STAGES: V1 the adoption gate (production as shipped must reproduce S2 exactly), V2/V3 the never-sampled GR-7 Phase 2 Dirichlet gear shares with its control, V4 the untested shared-turnover x biweekly-pot-closure corner, V5 a `shared_tau_min_obs = 20` sensitivity. Ships `DRY_RUN <- TRUE`; roughly 15-17 h with it FALSE; resumable. **Ran 2026-09-01** (14.1 h; 17 of 19 criteria resolving, one FAIL a real production defect and one a criterion using the wrong reference); reviewed in `07_documentation/development_notes/validation-batch-review-2026-09-02.md`. |
| `run_shore_ar_zi_2026-09-03.R` | The four next steps from the 2026-09-02 review. P1 aligns the PE and BSS incomplete-trip arms and measures it (PE only, seconds, runs during the dry run). G1 re-runs the gear track with the 2026-09-02 boat all-gear sampler fix to restore the cross-check. Z0 is the ZINB behaviour-neutrality gate AND the AR ladder's daily rung. A1-A3 force the shore all-gear AR to weekly, biweekly and monthly, the largest open modelling question at 29% of the port total. Z1 turns the ZINB on for the shore, with the boat fits as a free negative control. Ships `DRY_RUN <- TRUE`; roughly 17-20 h with it FALSE; resumable; one Stan recompile. **RAN 2026-09-03** (23.6 h). **Read the review before reading its outputs**: P1, G1 and Z0 are sound, but three one-line defects invalidated five of eleven verdicts and stage A1 refit `daily` four times instead of laddering, burning 14.7 h. See Section 1i of `PIPELINE_STATUS.md` and `07_documentation/development_notes/shore-ar-zi-review-2026-09-04.md`; the committed verdicts file carries corrected columns beside the originals. Superseded by `run_ladder_zinb_2026-09-04.R`. |
| `run_ladder_zinb_2026-09-04.R` | Corrects the 2026-09-03 batch. **D0** is a desk stage that runs during the dry run and re-derives, from committed files alone, every number that batch got wrong: the ZINB elpd against the PAIRED difference SE, the zero bin under the MIXTURE likelihood, and what the ladder actually did. **L1** runs the shore all-gear ladder for real, through the production `ar_escalate` toggle now that `force_resolution` honours its scoped form; `LADDER_INCLUDE_DAILY` (default FALSE) reuses the daily rung from `20260901/pooled-CPUE-SZ-Z0-zi-off`, saving 3.7 h, with the tradeoff written out in the control block. **Z2** (switchable, `ZINB_RERENDER`) re-renders the ZINB shore fit under the corrected PPC so adoption rests on rendered output rather than an offline mean-product approximation, and confirms the boat negative control reproduces bit-for-bit. Its PRE-FLIGHT is not decoration: each check asserts one of the four defects is fixed, at the source, and refuses to run otherwise. Ships `DRY_RUN <- TRUE`; **RAN 2026-09-04, 9.1 h** (L1 5.4 h, not the 2-3 h estimated: coarsening the AR barely reduces the cost, so budget one full fit per rung). **The ladder worked**: four distinct resolutions, all four passing the gate, and the adequacy statistics say daily is overfitted (p_loo 35.2% -> 9.6% of `n_obs`, 41 -> 1 bad Pareto k). Reviewed in `07_documentation/development_notes/ladder-zinb-review-2026-09-06.md` and Section 1j of `PIPELINE_STATUS.md`. Three defects found in the run and fixed: the rendered HTML was not moved so stage Z2 overwrote stage L1's report; `fmt()` was scalar-only and would abort the verdict block on R >= 4.3 after the fitting was done; `DRY_RUN <- FALSE` was committed. Resumable; no Stan recompile. **Stages C1 and C2 ran 2026-09-04** (9.3 h) and complete the 2x2: **C1 (shore all-gear at weekly + ZINB on shore catch) is the candidate production configuration** and the only fit in the project with zero Pareto k above 0.7. Reviewed in `07_documentation/development_notes/candidate-config-review-2026-09-07.md` and Section 1k of `PIPELINE_STATUS.md`. Four defects found in that run, all in the helper the previous patch added: see the review. |
| `run_adoption_2026-09-07.R` | The adoption render for the 2026-09-07 configuration (weekly shore all-gear AR + ZINB shore catch), successor to `run_ladder_zinb_2026-09-04.R` whose five stages have all run. **A1 carries NO config delta**: the authoritative run must come out of `run_config` as shipped, or its provenance is a footnote. Its gate is that the four fits be **bit-identical to `20260904/pooled-CPUE-LZ-C1-weekly-zi`**, because C1 reached weekly through `ar_force` (which bypasses the cap) and production reaches it through the cap: same resolution, different routing, and a routing change that silently moved a number would be the worst way to adopt this. A DESK stage proves the two routes build identical Stan data before the render starts, in seconds and with no Stan toolchain. **A2** is the gear-resolved cross-check, not re-measured since the shore component moved; expect the gap to narrow, and note that the gear Stan has no zero-inflation block so the two tracks now differ in the shore catch likelihood as well. Ships `DRY_RUN <- TRUE`; about 4.5 h with it FALSE; resumable; no Stan recompile. **RAN 2026-09-07** (4.1 h). **The gate PASSED**: A1 reproduces C1 across 10,253 parameter rows, so `05_output/20260904/pooled-CPUE-AD-A1-adopted` is the authoritative run (port 72,027). Reviewed in `07_documentation/development_notes/adoption-review-2026-09-08.md` and Section 1m of `PIPELINE_STATUS.md`. One defect: `verdict_A2` read the pooled port-row label against the gear file, got `numeric(0)`, and `is.finite(numeric(0)) && ...` errors in R, so the verdicts were lost after the fitting succeeded. Fixed by matching the row by pattern AND by wrapping every verdict block so a defect can never cost a completed run its output. |
| `README.md` | This file. |

The augmented Stan model it fits, `crab_bss_pooled_weather_adjusted.stan`, lives in [`02_stan_models/`](../02_stan_models/README.md); it adds covariate blocks (`gamma_E` on `mu_E`, `gamma_C` on `mu_C`) and ****no longer collapses to `crab_bss_pooled.stan`** (it did when written; as of 2026-09-02 the fork is missing about 40 data variables the production model declares, including the entire `shared_tau` block that production adopted, the OSP stream, the `crab_fraction_*` block and the ZINB flags), and when `K_E = K_C = 0` it collapses only to the v6.9-era model it was forked from**, so one file serves both the baseline and augmented fits.

## How it runs

- As part of a full run: set `run_weather <- TRUE` in `run_config.R` (or pass `--weather`) with `model = "pooled"`, and `run_estimation.R` renders this module **after** the pooled model. It is only valid with the pooled model, the module reuses the pooled run's in-memory objects (`dwg`, `ie_data`, `L_eff_model`, …) via the shared render environment ("Option A" hand-off), so it cannot run without a preceding pooled run in the same session.
- Standalone: knit `BSS-GH-pooled-CPUE-weather-tide-covariates.Rmd` directly; its setup chunk auto-sources `run_config.R` when `run_config` is not already present.

Outputs land in `05_output/<YYYYMMDD>/pooled-CPUE-covariates/` (paired `*_baseline` / `*_covariates` files, GAM smooths, and the covariate-vs-baseline LOO comparison). At runtime the module reaches NOAA CO-OPS, NDBC, and Iowa State IEM/GSOD endpoints for tide and weather series; results are cached under `cache/weather_tide/` at the repo root (regenerable and git-ignored).

## Status

**Experimental and currently stale.** Per `07_documentation/development_notes/PIPELINE_STATUS.md`, the module tracks an older pooled engine (~v6.9 parity, pre-deployment-scale) and has not been re-run against the current Stan models, so its boat-magnitude outputs must not be cited. Its committed conclusion is that weather/tide covariates are **excluded**, see the decision record in `07_documentation/WEATHER_COVARIATE_ANALYSIS.md` and Section 17 of the pooled-model documentation. The intent is that any covariate later shown to help is folded directly into the production pooled and gear-resolved models, rather than maintained as a separate track.

## Documentation

Technical write-up: `07_documentation/BSS-GH-pooled-CPUE-weather-tide-covariates-documentation.md`. Decision record (exclusion finding): `07_documentation/WEATHER_COVARIATE_ANALYSIS.md`.
