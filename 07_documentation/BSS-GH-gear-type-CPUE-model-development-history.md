# Grays Harbor Crab Harvest Estimation: Gear-Resolved CPUE Model

## Development history

- **Companion to:** `BSS-GH-gear-type-CPUE-model-documentation.md` (the published gear-resolved method reference).
- **Scope:** the full version-by-version change log of the gear-resolved-CPUE pipeline and its Stan model `crab_bss_gear_resolved.stan`, plus the detailed working notes from the two largest episodes: the boat effort-scale correction (fix-markers F1 and F2) and the point-estimator population-and-estimator fix (P0). The current production state is framework **v5.6** (2026-07-12): both the shore and boat components run on the gear-deployment effort unit and `loo_effort_unit_comparison` is turned off for production (v5.5), and `run_config.R` is now the base parameter set in parity with the pooled track's v7.9 (v5.6).
- **Convention:** no em dashes.

This file is the provenance record for the gear-resolved model. The published method document summarizes this history in one screen and refers here for the detail. Entries are newest-first. The version log traces the framework from the initial gear-resolved release (v5.0) through the empirical-proportion and data-alignment work (v5.1 to v5.4) to the shore effort-unit resolution (v5.5). The run-driven Stan fixes that do not carry a framework tag are recorded in their own section below the version log, and the outstanding backlog closes the log.

The gear-resolved track branched from the shared pooled/gear-resolved sequence at v5: versions v1 through v4 are one shared milestone line (documented in `README.md`), and since v5 the two tracks carry independent change logs. The gear-resolved track imports the pooled convergence work as parity ports rather than re-deriving it (the B1.x markers in the Stan file below), and adds its own hard-won equivalents, F1 and F2, which the pooled track then ported back as POOL-1 and POOL-3. The two tracks share the effort-specification module `03_R_functions/bss_effort_spec.R` and the convergence-gate and AR-selection helpers, so they cannot drift onto different scales or gate policies.

**A note on the two numbering systems.** The gear-resolved track carries a v5.x "framework" series in the RMD header (`BSS-GH-gear-type-CPUE-model.Rmd`) and in the documentation change log. That series is distinct from the Stan model's own history: `crab_bss_gear_resolved.stan` carries no vX.Y tag. The documentation's "Stan v3.2" label (attached to framework v5.2) is stale; the Stan file is materially past v3.2. It now carries the pooled-track parity ports B1.3, B1.5, B1.6, B1.8, and B1.9, plus the run-driven fixes F1, F2, P1, and P2. Read the framework tag for what the R pipeline does and the fix-marker section below for what the Stan model does; the "Stan vX.Y" labels in the version log are historical and should not be trusted as the current model state.

**Header status.** As of v5.6 (2026-07-12) the RMD banner, the method-document header, and this history are all in sync at v5.6. Earlier releases carried a header lag (the RMD banner and method-doc header read v5.4 while the body was v5.5); that is resolved.

------------------------------------------------------------------------

## Version log

### 2026-09-02, The validation batch: the adoption is confirmed, and the gear track turns out never to have been able to fit its boat component (branch `OSP-boat-count-incorporation`)

Nineteen criteria, 14.1 h of fitting. Harness **233 assertions**, 0 failing. Full review in `development_notes/validation-batch-review-2026-09-02.md`.

**ADOPTION CONFIRMED.** V1 reproduces S2 across 11,223 shared parameter rows, identical at full double precision, with all four components matching. The shipped configuration is the one the Stage 5 batch measured. Authoritative run `05_output/20260831/pooled-CPUE-VAL-1-adopted`, port total 71,513 [52,346, 100,742]. The boat `prior_vs_posterior` files exist again, closing the 2026-08-31 regression; `tau_bar` contraction is 0.216 with `prior_influential` TRUE, which is the recorded open item about that flag's definition rather than a finding about `tau_bar`.

**A PRODUCTION DEFECT THAT HAD BEEN THERE ALL ALONG.** The gear driver carried explicit per-fit sampler settings for shore all-gear and for both pot-closure fits and NONE for boat all-gear, so that fit alone used the track defaults (2,000/1,000 draws, adapt_delta 0.90, treedepth 10) while the pooled track fits the same component on the same data at 5,000/2,500, 0.99 and 13. At the defaults it produces 554 divergences, `tau_bar` n_eff 49 and R-hat 1.0813, the gate rejects it, and the component falls back to PE: the gear-resolved port total reads **51,385 instead of about 70,900, a 27% understatement of the cross-check that validates the pooled headline**. The 2026-08-30 Stage 5 batch had already found the fix and left it in an EXPERIMENT-ONLY escape hatch, so production never got it, and the validation batch reproduced the failure bit-identically. The four settings now live in the gear driver's `params_model` behind their own `else if (!is_shore && is_allgear)` branch; `bss_sampler_override` stays NULL in production. A fix that lives only in an experiment flag is not a fix.

**THE FINDING THE BATCH WAS NOT LOOKING FOR.** The adequacy table on the production run flags the SHORE all-gear fit, not the boat: p_loo 35.2% of n_obs against the boat's 8.0%, 41 Pareto k above 0.7 against 1, and `coverage_50` 0.701 against a nominal 0.500, which is +7.1 sampling SDs. That component runs at a DAILY AR in production and shows the same signature the daily boat AR was rejected for, on 29% of the port total. The gear track fits it at monthly and is not better but differently wrong (`coverage_50` 0.035, -16.4 SDs), so the answer is between them and weekly and biweekly have never been run. The cross-track table differs in three things at once and is suggestive only; the pooled daily figure alone is sound. Now a Tier-1 backlog item.

**GR-7 PHASE 2 SAMPLED FOR THE FIRST TIME, and it does exactly what it was designed to do.** Coded 2026-07-21 and never run: propagating Dirichlet gear-share uncertainty widens every per-gear shore interval by 1.12x to 1.23x, moves no median by as much as 1%, and leaves the per-gear medians summing to the component total. Phase 2 is validated; whether `gear_resolved_G` becomes the gear-model default is now a decision rather than an unknown.

**OTHER RESULTS.** The boat pot-closure 2x2 completes at 849 / 1,018 / 735 / 939 with an interaction of +35 crab, additive like the all-gear one, and V4 re-confirms the `ar_force` leak is closed. Raising `shared_tau_min_obs` from 15 to 20 drops the boat pot closure out of the feature and costs 169 crab, 0.24% of the port: the threshold is defensible and nearly inconsequential. The Tier-1 "TOP OF THE LIST" shore I/E unit fix is CLOSED on all four of its own pre-set criteria, taking the Tier-2 GR-9 tension with it. The Tier-3 zero-inflation precondition is met and targets ONE stream: every boat stream sits inside |z| = 2.3 while the two shore CATCH streams under-predict zeros at z = +3.8 and +2.9, so a prototype should target that likelihood rather than the whole model.

**A PROCESS FAILURE, recorded because it repeated.** Two of the last three batches contained a criterion comparing against a reference that differed in TWO things, both times producing a FAIL that looked like a code defect and needed manual diagnosis. `fit_exactness()` now takes `expect_delta`, parses both runs' `run_parameters.txt`, and reports any unexpected config difference inside the verdict. Also fixed: the aggregate PPC now computes the PIT in closed form rather than by simulation, so it agrees with `ppc_byobs_*.csv` to floating point instead of to Monte Carlo noise, and D3 reads the newest production run so its zero-bin criterion is scorable rather than permanently pinned to archived folders.

------------------------------------------------------------------------

### 2026-09-01, The shared turnover is adopted, and a PPC coverage defect retracts an open item from the day before (branch `OSP-boat-count-incorporation`)

Harness **214 assertions**, 0 failing. Full desk evidence in `development_notes/PIPELINE_STATUS.md` Section 1d.

**ADOPTED: `shared_tau = TRUE`, with `shared_tau_min_obs = 15` stated explicitly in `run_config.R` rather than left to the helper default.** The feature is boat-only by the informed-day floor and not by a population switch: shore all-gear (4 informed days of 289) and shore pot closure (0) degrade to per-day draws with a printed reason, boat all-gear (130) and boat pot closure (18) take the shared level. Recommended authoritative run S2, port total 71,521 [52,350, 100,759], cross-checked by the gear track at 70,886. Confirmation outstanding: stage V1 of the new `run_validation_2026-09-01.R` re-runs production as shipped and must reproduce S2 bit-for-bit.

**A DIAGNOSTIC DEFECT, and it corrects the review written the day before.** `ppc_calibration_*.csv` computed `coverage_50` by testing the observation against a QUANTILE INTERVAL of simulated draws. For small counts that interval cannot carry 50% of the mass, so it over-covers by construction. `ppc_byobs_*.csv` had always computed the same quantity correctly from the randomized PIT, and the two had never been compared: they disagree by up to **0.154** on the private-boat trailer stream, whose fitted means are 1-2 boats, and by 0.000 on the catch stream, whose counts are an order of magnitude larger. The 2026-08-31 Stage 5 review recorded "the trailer stream is over-covered in every configuration, 0.667-0.692 against a nominal 0.500, 4.6-5.3 sampling SDs" as a new open modelling item. On the correct statistic the same runs give 0.523 and 0.538, which is 0.6 and 1.1 sampling SDs. **The open item is retracted and no change should be made to the trailer observation model on account of it.** `model_diagnostics.R` now uses the randomized statistic so the two files agree; `bss_model_adequacy.R` prefers `ppc_byobs` where it exists and records which source it used, so archived runs stay comparable.

**The Stage 5 conclusion survives and is sharper.** The daily-AR cells sit at 0.713 and 0.744 under the randomized statistic, 5.9 and 6.8 sampling SDs from nominal, against 0.523 for production. `flag_miscalibrated` now fires on the two daily cells and stays quiet on the two monthly ones; before the fix it fired on all five, which at the time was rationalised as informative rather than treated as the tell it was. A flag that fires on everything carries no information.

**Two smaller PPC fixes rode along.** The OSP stream now gets per-observation rows: it was the one stream whose calibration could not be cross-checked against the randomized statistic, and it is where the daily-AR pathology is most extreme. And `p_zero` (the model's own per-day zero probability) is now written, so the Tier-3 zero-inflation precondition can be scored as observed zero COUNT against expected zero count over ALL days. The first attempt read it off the PIT at the observed zeros, which is a different question: those days are selected for being zero, so they are the low-mean days whose P(Y=0) is high by construction, and it read a calibrated trailer stream as 20% observed against 45% implied.

**NEW: `06_diagnostics/run_validation_2026-09-01.R`,** four desk stages plus five fitted ones, about 15-17 h. The desk stages run during the dry run, need no Stan toolchain, and retired three backlog items on their first execution: the Tier-1 "TOP OF THE LIST" shore I/E unit fix is CLOSED on all four of its own pre-set criteria, using an isolating run that had existed since 2026-08-26 and had never been scored; the Tier-2 GR-9 `sigma_IE` tension closes with it; and the `gear_only` arm turns out to be worth at most 0.7% on any component, so the live defect there is that the PE and the BSS use different arms on both boat components rather than which arm is right. The fitted stages are the adoption gate, the never-sampled GR-7 Phase 2 Dirichlet gear shares with its control, the untested shared-turnover x biweekly-pot-closure corner, and a `shared_tau_min_obs = 20` sensitivity on a threshold set just below the component it keeps.

------------------------------------------------------------------------

### 2026-08-31, The Stage 5 batch: the tau x AR 2x2 is settled, and the adequacy table it shipped with was wrong (branch `OSP-boat-count-incorporation`)

Six stages, 24 h of fitting, 11 of 12 criteria passing. Full review in `development_notes/stage5-batch-review-2026-08-31.md`. Harness at **199 assertions**.

**The 2x2.** Private-boat all-gear across `shared_tau` x boat AR: 25,868 (off/monthly), 31,008 (on/monthly), 37,359 (off/daily), 42,344 (on/daily). The interaction is **-155** against main effects of +5,140 and +11,491, so the two levers are additive: they act on different parts of the model and either could be adopted without the other. Additivity settles the arithmetic and adjudicates nothing.

**What adjudicates.** The shared turnover buys 14.7 nats of trailer elpd for 3.1 effective parameters (4.7 nats each), costs the catch stream 0.1 nats, adds no Pareto k above 0.7, and moves PIT mean, PIT sd and coverage all toward nominal. The daily boat AR buys elpd by spending 27 to 48 effective parameters on 195 observations, makes the CATCH stream 3.8 nats worse in both tau conditions, produces 2 then 16 bad Pareto k, collapses both observation-error scales (`sigma_r_OSP` 0.806 to 0.086; `sigma_r_E` 0.934 to 0.306), drives `r_OSP` to 133.7 with a 95% upper bound of 43,875, and pushes `phi_E` to 0.886 at daily resolution. Identification versus absorption, no longer a hypothesis. **Recommendation: adopt the shared turnover boat-only; do not adopt the daily boat AR.**

**Cross-track corroboration, the strongest the project has produced.** Raising the gear track's boat all-gear fit to the pooled track's adaptation as well as its draw count moved `tau_bar` from n_eff 49 / R-hat 1.081 / 554 divergences / gate REJECTED to n_eff 19,292 / R-hat 0.9997 / 2 divergences / BSS. Draws rose 2.5x and n_eff rose 394x, so draw count was never the binding constraint; the gear driver simply had no per-fit override for that component and ran it at `adapt_delta` 0.90 where pooled runs 0.99. The two tracks now agree on `tau_bar` to **0.03%** (2.5969 vs 2.5962), the boat component to 0.80% and the port total to 0.89%. The independent OSP/trailer overlap calibration puts the turnover at 2.01-3.03; the monthly posterior straddles it at 2.597 [2.06, 3.25], while the daily cell's 3.232 [2.87, 3.63] sits at or above its top.

**The adequacy table added on 2026-08-30 was inadequate, and this batch is what showed it.** `pit_worst_bias` ranked the (tau ON, daily) cell as the best-calibrated of the four when it is the worst: a latent process with one state per observation interpolates the data and piles every PIT value at 0.5, so the mean reads 0.495 while 98% of OSP observations fall inside a nominal 50% predictive interval. `cov50_worst_dev`, `pit_sd_worst_dev` and `flag_miscalibrated` are now in the table; both underlying statistics were already being written to `ppc_calibration_*.csv` and neither reached it. A second gap: `disp_neff_min` asks whether a dispersion parameter SAMPLED well, not whether it COLLAPSED, and `sigma_r_OSP` fell to 0.086 while holding n_eff 657, above the gate's floor. `disp_scale_min` is now reported and deliberately NOT flagged, because the half-Cauchy priors have no finite SD so contraction is undefined for them and any cut-off would be a number chosen after seeing these runs.

**One regression, found and fixed.** `tau_bar` was registered in the prior-vs-posterior table under its bare name while rstan summarises it as `tau_bar[1]` (declared `vector<lower=0>[shared_tau]`). `has_par()` strips the index and selected it, `post[pn, ]` then threw, and the writer's enclosing `tryCatch` swallowed the error, silently dropping the ENTIRE boat `prior_vs_posterior_*.csv` from S2, S3 and S4b. The entry is now named `tau_bar[1]` like `mu_mu_E[1]`, the row lookup tolerates a scalar/vector mismatch in either direction, and an unresolvable parameter is dropped rather than taking the file with it. The generalisable lesson: a `tryCatch` around a whole writer turns a one-row bug into a missing file.

**Also settled.** The empty-stratum re-run reproduces the driver exactly (zeroed days 44/0/47/9, PE catch to the crab), and `day_type` raises the PE 22.9-28.5% per component, +14.0% at the port; the decision is still open. The boat pot-closure AR re-run gives 735 at biweekly against the gear track's 743, with the all-gear component back at rung 4's 25,868, confirming the 2026-08-28 `ar_force` leak, which was worth 3,025 crab on that component, is closed.

**Two open items this batch created.** The trailer stream is over-covered in EVERY configuration (`coverage_50` 0.667-0.692 at monthly against a nominal 0.500, which is 4.6-5.3 sampling SDs at n = 195); the shared turnover improves it and nothing fixes it. And `prior_influential` measures variance reduction only: `tau_bar` contracts 0.216 and would be flagged prior-influential while its posterior mean sits 3.5 prior SDs from the prior mean.

------------------------------------------------------------------------

### 2026-08-30, Stage 5 prerequisites: an informed-day floor, adequacy beside the gate, four reporting fixes, and a sanctioned way past the config merge (branch `OSP-boat-count-incorporation`)

No estimate changed. Everything here is code and reporting, applied before the Stage 5 batch (`06_diagnostics/run_stage5_2026-08-30.R`) so the batch has something exact to gate on. Harness: **178 assertions**, 0 failing.

**1. The informed-day floor on `shared_tau` (plan 5.2).** `bss_shared_tau_data()` gained `n_informed` and refuses the feature below `shared_tau_min_obs` (default **15**), falling back to per-day draws with a printed reason, exactly as the existing turnover guard does. Informed days are the I/E days plus, when `osp_scale_is_tau = 1`, the OSP days, because that is what puts `L` into a likelihood. Both preps compute the count and expose it on `stan_data`; `fit_data_summary.csv` records it with the resulting `shared_tau` so the decision is auditable from the output folder. The counts are 4 / 0 / 130 / 18 across the four fits, so any threshold in 5..18 separates the shore from the boat; 15 sits just below 18 on purpose, to keep the boat pot closure whose `tau_bar` (1.873 pooled, 2.050 gear) is corroborated across tracks. `shared_tau` stays **off in production**.

**2. Model adequacy, reported and deliberately not gating (plan 5.5).** New `03_R_functions/bss_model_adequacy.R` writes `model_adequacy.csv`: `p_loo` as a fraction of `n_obs` (worst stream), Pareto k > 0.7 count, worst PIT bias, and the smallest `n_eff` among the observation-model dispersion parameters, which the gate never inspects. Decoupled dispersion parameters are excluded from that minimum. `pass_convergence` is untouched: six configurations already passed every gate criterion while spanning 44% on the boat, and the point is to make "sampled" and "adequate" visibly different questions, not to quietly add a fifth gate.

**3. A retro path for both audit tables.** `annotate_model_adequacy_run()` and `annotate_decoupled_run()` rebuild adequacy and decoupling for an archived run folder from its own committed CSVs, writing `model_adequacy_reconstructed.csv` and `decoupled_audit.csv` beside the originals. Committed outputs are never rewritten and every reconstructed row says so. Both live and retro adequacy go through one shared body (`.bma_core`), because a reconstruction that computed its numbers slightly differently from the live writer would be worse than none, given the two get quoted side by side.

**4. Four reporting defects.** `pe_vs_bss_comparison.csv` now carries `bss_reported`, so a rejected fit's BSS column cannot read as a result (the 2026-08-29 gear run printed `BSS_catch = 32,689` beside `method_selected = "PE"`). `structural_params_*.csv` gained `fit_method` and an `estimate` column that is `median` with decoupled rows blanked, which covers `kappa_OSP` (prior-only in every fit under `osp_scale_is_tau`) and shore `r_OSP` (posterior mean 2.5 million). The pooled `expansion_ratios.csv` gained `R_G_boat_decoupled` and a `[PRIOR ONLY: no boat count stream in this fit]` suffix. The 2026-08-26 baselines, which have no `decoupled` column at all, are covered by the retro annotator, which marks window-dependent rules `unknown` rather than guessing them. A fifth, caught in review: the new writers had invented `"PE (gate fail)"` where `convergence_report.csv` says `"PE (convergence fail)"`, so one fit would have read two ways in one folder; all three now quote `gate_info$method_selected`, the gate being the single authority on method selection.

**5. `bss_sampler_override`.** Each driver merges `params_model` on top of `run_config`, so `params_model` wins every sampler key and a `run_config` delta on `bss_iter_default` does nothing while looking as though it complied. That is the `bss_min_interviews` trap again, and it would have silently voided plan 5.2's request to give the gear track more draws. The new list is applied after the merge, accepts sampler keys only, prints every change, and **errors** on anything else. Production value `NULL`.

**One thing measured rather than argued.** Extra elements in `rstan::stan(data = )` are inert: a two-chain fit with and without three added elements, including the new `.n_L_informed`, returned bit-identical draw matrices. The dotted metadata the preps attach to `stan_data` therefore cannot perturb a bit-identity gate, which the Stage 5 S2 gate depends on.

------------------------------------------------------------------------

### 2026-08-29, The improvement-plan batch ran: six stages, and a 44% spread on the boat that every gate accepted (branch `OSP-boat-count-incorporation`)

`06_diagnostics/run_improvement_plan_2026-08-27.R` completed all six stages (~22 h). Every question it asked got an answer, and the answers together exposed a larger one. Full review in `development_notes/improvement-batch-review-2026-08-29.md`; follow-up work in Stage 5 of `improvement-plan-2026-08-27.md`.

-   **THE HEADLINE. The private-boat all-gear estimate ranges over 25,883 to 37,392 across six configurations that ALL pass the convergence gate**, an 18% spread at the port total (66,237 to 78,250). Every one of those fits has R-hat under 1.01, n_eff over 400, a divergence fraction under 5% and impact under 0.10 SD. For scale, the entire 2026-08-25 improvement batch moved the port total 1.6%. The gate tests whether a fit sampled, not whether the model is right, and was never designed to do the latter. Any external presentation of the boat number now has to name the configuration that produced it.
-   **Stage D, the gate: PASSED.** `shared_tau = FALSE` reproduces rung 4 with 11,217 shared parameter rows identical at full double precision and a byte-identical `convergence_report.csv`, so stage E is attributable to the feature.
-   **Stage E, shared turnover: the boat turnover moves, with unusually good corroboration.** `tau_bar` = 2.597 [2.064, 3.249] against a prior centred on 1.20. Four independent lines agree: the OSP/trailer overlap calibration implies 2.01-3.03; the trailer and OSP PIT means converge on 0.50 simultaneously and in opposite directions (bias down 81% and 73%); boat trailer `elpd_loo` gains +14.67 at paired z = +2.66 with `p_loo` staying low; and convergence improved (divergences 97 -> 39). Boat all-gear 25,883 -> 31,012. Qualifications: the posterior sits above the prior's 95% envelope so 2.597 is shrunk, and `n_ie_obs = 0` for this fit, so the turnover is identified indirectly through the count streams and must never be called measured.
-   **Stage E's SHORE result must not be carried forward.** Shore all-gear moved +17.9% on its own `tau_bar` (1.700 -> 2.080), backed by 4 of 289 informed days, with an interval that still contains the 1.7 prior centre, no measurable improvement in fit, and no replication in the gear track (which put the same parameter at 1.681). The toggle is global and needs an informed-day floor.
-   **Stage B, reseeded rung 2: seed-dependent, but the configuration is marginal.** The fit survived (488 divergences, 4.88% against a 5% backstop, worst chain 350, `E_sum` n_eff 924 against the shipped config's 5,914). Two seeds, two struggling chains. The I/E unit fix alone does strain the shore all-gear daily fit; the weekend fix is what makes the shipped configuration comfortable. The clean isolation now confirms the I/E result three ways: `sigma_IE` 0.238 / 0.300 / 0.272 against 1.068 before the fix.
-   **Stage C: the 13% cross-track gap on the boat pot closure was a configuration artefact.** Forced to biweekly the pooled fit gives 735 against the gear track's 743. The run was contaminated by an `ar_force` defect found in this review (see below), so its port total is not interpretable; the pot-closure conclusion stands.
-   **Stage F, the escalation ladder: every component passes at DAILY on the first attempt, and the daily boat fit is absorbing rather than identifying.** Shore pot closure at daily gives 111 divergences where Run 6 recorded 1,165, so the funnel that justified the biweekly cap is gone and `ar_max_resolution` is now the only thing holding production coarse. But the boat at daily raises catch 44% while `p_loo` on the trailer stream goes 4.9 -> 31.8 with 2 bad Pareto k, the CATCH stream's elpd gets WORSE, and `r_OSP` collapses 1.62 -> 10.61 with a 95% upper bound of 1,405 (its parent `sigma_r_OSP` at n_eff 307, below the gate's own floor on a parameter the gate does not check). The entire increase lands in the extrapolated portion.
-   **Stage A, empty-stratum fill: `day_type` raises every affected component's PE 20-27%**, about 13% at the port level. The `zero` fill has been biasing the PE down by roughly an eighth, which matters for the PE-vs-BSS cross-check and for any component that falls back to PE.
-   **Two defects found and fixed.** `ar_force` was per-population only while `ar_max_resolution` had gained the nested per-sub-season form, and `as.character()` on a one-element list returns its element, so a nested `ar_force` silently forced every sub-season of that population; that is what contaminated stage C, moving boat all-gear 25,883 -> 28,902 as a side effect. It now takes the same shapes as `ar_max_resolution` and errors on an unnamed list. Separately, stage A did not load the crabbing-holiday workbook the way the driver does, so its stratification (85 cells) and zeroed-day counts (41/44/8/0) did not match the driver's (92 cells, 44/47/9/0); the comparison was still fair because both arms shared the stratification.
-   **No configuration change is adopted.** The shipped rung-4 configuration stands as authoritative. Stage E is the strongest candidate to change it and should not be adopted on one pooled run with a failed gear cross-check and a shore side effect that has to be suppressed first.
-   Harness at **128 assertions**. Files changed: `03_R_functions/bss_ar_resolution.R`; `06_diagnostics/run_improvement_plan_2026-08-27.R`, `test_improvements_2026-08-25.R`; `development_notes/PIPELINE_STATUS.md`, `improvement-plan-2026-08-27.md`, new `improvement-batch-review-2026-08-29.md`; this history.

### 2026-08-27b, Shared-turnover parameterization, the OSP predictive check, and the improvement-plan batch (branch `OSP-boat-count-incorporation`)

Makes the improvement plan runnable. The model change is the substantive part; everything else is plumbing for it.

-   **Improvement 2.1, a SHARED turnover.** Both Stan models gain a `shared_tau` data flag and a `vector<lower=0>[shared_tau] tau_bar` parameter. With it on, `L[d] = tau_bar * exp(shared_tau_sigma * L_raw[d])`, one estimated level informed by every observed day, replacing the `D` independent per-day anchors that left 148 days of OSP data unable to move the boat turnover off its 1.2 prior. `shared_tau_sigma` is data, not a parameter: the question is where the LEVEL goes, and estimating the spread alongside it lets the two trade off on a series where most days are unobserved.
-   **Ships FALSE, and the OFF path is verified bit-identical on real data.** `tau_bar` is zero-size when the feature is off. A fixed-seed fit of the shore pot-closure component against the pre-change model reproduces `C_sum`, `C_expected_sum`, `E_sum`, `B1`, `B2`, `B1_C`, `R_G`, `sigma_IE` and `L_out` identically at full double precision, with zero `tau_bar` draws and `tau_bar_out` exactly 0. With the feature on it samples cleanly and the per-day `L` medians collapse onto the shared level. `bss_shared_tau_data()` refuses the toggle, warning rather than erroring, whenever `L` is not a constant turnover.
-   **Improvement 2.2, the OSP posterior-predictive check**, which was the one observation stream without one. `ppc_calibration_*.csv` gains an `osp` row per boat fit, on the same mean the likelihood uses. This is the direct read on the scale conflict that previously had to be inferred from the trailer PIT sitting at 0.42.
-   **Plan stage 4 hygiene.** The per-fit diagnostic subsampling is seeded from `params$bss_seed`, so `bss_draws_summed_*` and `ppc_*` stop shuffling between runs of bit-identical fits; the port total stays RNG-sensitive through `rstan::extract(permuted = TRUE)` and that is now documented. The commercial/charter label becomes "Census (day-type stratified)".
-   **`06_diagnostics/run_improvement_plan_2026-08-27.R`**, six stages, isolated rather than cumulative against the rung-4 baseline, resumable, ~25-35 h. Stage A does no fitting (the empty-stratum fill is a PE-only question). Stage D is a hard gate on stage E.
-   **Harness at 121 assertions**; the batch's dry run adds 10 self-tests against the rung-4 outputs.
-   Files changed: both `.stan` models; `03_R_functions/bss_effort_spec.R` (new `bss_shared_tau_data()`), `prep_bss_crab_pooled.R`, `prep_bss_crab_gear.R`, `model_diagnostics.R`, `save_run_diagnostics.R`; both drivers; `run_config.R`; new `06_diagnostics/run_improvement_plan_2026-08-27.R`; `06_diagnostics/test_improvements_2026-08-25.R`; `development_notes/PIPELINE_STATUS.md` and `improvement-plan-2026-08-27.md`; this history. Removed the stray `postladderfixes20260827.patch` committed at the repo root.

### 2026-08-27, The 2026-08-25 batch is validated, and six reporting defects the review of it found (branch `OSP-boat-count-incorporation`)

`06_diagnostics/run_patch_validation_2026-08-25.R` completed all six model-runs (~15 h). The batch is behaviour-neutral where it claims to be, every change moved for its predicted reason, and the two independent tracks reconcile to 0.73%. **Rung 4 is the shipped configuration and the new authoritative run: port total 66,237 (95% CI 50,037 - 91,210), effort 40,187, 4 of 4 components fitted.** Full numbers in `development_notes/PIPELINE_STATUS.md`; the review is `development_notes/ladder-validation-review-2026-08-27.md`; the sequenced follow-up work is `development_notes/improvement-plan-2026-08-27.md`.

-   **The shipped config moves the port total DOWN 1.6% (pooled) and 1.1% (gear).** Decomposition: boat all-gear -1,882 at the weekend rung, boat pot closure +744 at the interview-floor rung, shore roughly +190 net, commercial census -165. The weekend redefinition, acting through the boat, is the whole story.
-   **Baseline reproduction passes on both tracks, and the gear track is BIT-identical at the fit level.** All 1,508 / 4,935 / 3,431 parameter rows shared with the reference match at full double precision across the three fits; `convergence_report.csv` is byte-identical. The only differences are three new inert reporters (`f_lower[1]`, `f_lower_out[1]`, `osp_f_kappa_out`, all exactly 0 with 0 SD) and one pure rename, `f_crab_param[1]` -> `f_theta[1]`, identical to every digit. Pooled sits 11 crab (0.1 MCSE) from its reference, exactly as the retired `B3` dimension predicts.
-   **The shore I/E unit fix is confirmed and large.** `sigma_IE` falls 1.068 -> 0.300 pooled and 0.871 -> 0.598 gear. It also checks out arithmetically: the geometric-mean ratio of crabber-hours to crabber-trips across the four in-window I/E days is 3.44, and `log(3.44) = 1.24` against a pre-fix `sigma_IE` of 1.03 to 1.07 -- a single lognormal scale absorbing a systematic unit offset of exactly that size. **Correction: the mismatch measures ~3.2x, not the ~4x estimated on 2026-08-25.**
-   **One fit was lost, to one stalled chain.** Rung 2's shore all-gear was rejected by the gate (2,957 divergences, `E_sum` n_eff 30, R-hat 1.049), and that PE substitution is the entire rung-2 port drop. Per-chain, chains 1/2/4 recorded 163/140/154 divergences in line with rung 1's 385 across four, while chain 3 recorded 2,500 of 2,500 with mean accept-stat 0.053. It never moved. Re-running rung 2 on a different `bss_seed` is plan item 1.2.
-   **Weekend = Sat/Sun does what was predicted, slightly past the forecast.** `B1` 1.67x -> 2.48x on shore all-gear against a predicted ~2.34x; boat all-gear 1.56x -> 1.90x. Boat all-gear catch falls 6.8%, which was not predicted and is the largest single move in the ladder.
-   **The interview floor is a clean win.** The boat pot closure reaches the sampler and PASSES the gate (35 divergences, 0.58%, R-hat 1.0003, n_eff 5,207), reporting 850 (95% CI 274 - 2,693) against a PE of 400. Rungs 3 and 4 are numerically identical on the three fits they share, so the floor did exactly one thing. Roughly 18% of the port total previously carried no uncertainty at all.
-   **RETRACTION.** The 2026-08-25 claim that the fixed shore I/E stream "becomes genuinely informative about `tau_shore`" is wrong as the model is written. `crab_bss_pooled.stan:370,418` declares `vector[D * estimate_L] L_raw` with `L[d] = L_data[d] * exp(L_prior_sigma[d] * L_raw[d])`; there is no shared `tau_shore` parameter, so four I/E days can inform four days out of 289. The season median `L` is 1.6998 against a prior centre of 1.7000. Making that claim true is a model change, not a config change (plan item 2.1).
-   **Two criteria were wrong and are repaired.** The gear exactness criterion demanded `delta == 0` on the port total and FAILED at -19 while the model was bit-identical; the port total is assembled from `rstan::extract(permuted = TRUE)` draws and an unseeded `sample.int()`, which this document had already recorded as RNG-sensitive. Exactness is now tested on `convergence_report.csv` byte equality plus full-precision equality of every shared parameter row. The tau criterion is retired to INFO. A per-rung convergence criterion is added, because nothing in the original set noticed that rung 2 had lost a fit.
-   **Six reporting defects fixed.** The second copy of the shore day-length weighting in `.srd_monthly_share()` (defect (B) was fixed in `pe_monthly_effort_share.R` only); the empty-effort-stratum report, which never reached a pooled run because that PE chunk is `results='hide'` and is now a CSV from both drivers; I/E unit provenance in `fit_data_summary.csv` plus `ie_trips` / `turnover` / `hours_per_trip` in the two I/E files; a `decoupled` flag on `structural_params_*.csv`; `production_arm` split per estimator; and an explicit `status = "skipped"` row when a CPUE regression falls below its 30-interview minimum.
-   **The empty-stratum fix produced a finding immediately.** On the shipped config the zeroed-day shares are shore all-gear 44/289 (15.2%), boat pot closure 9/76 (11.8%), boat all-gear 47/289 (16.3%). Three of four components are above the 5% warning threshold at the default `zero` fill, and the figure previously on the record was the boat pot closure alone.
-   **The largest open item is pre-existing and not in this batch: `osp_scale_is_tau` does not do what the notes say.** With it on, the OSP mean uses `L`, which is the same per-day independent construction as the retraction above, so 148 days of OSP data leave the posterior median at 1.201 against a prior centre of 1.200. The overlap calibration and the Phase-1 free `kappa_OSP` both put the real turnover at 2.0 to 3.0, and that conflict is absorbed by `r_OSP` (1.58 to 1.62) rather than moving `L`. Corroborating: the boat trailer PIT mean is 0.420 to 0.425 in all six runs against a nominal 0.50. The toggle roughly doubles the private-boat harvest, and as implemented the size of the doubling is set by the `tau_boat` prior rather than by OSP.
-   Files changed: `03_R_functions/save_run_diagnostics.R`, `model_diagnostics.R`, `bss_day_length.R`, `bss_cpue_diagnostics.R`, `diagnose_incomplete_trips.R`, `run_pe_pooled.R`, `run_pe_gear.R`, `prep_bss_crab_pooled.R`, `prep_bss_crab_gear.R`; both drivers; `06_diagnostics/run_patch_validation_2026-08-25.R`, `test_improvements_2026-08-25.R`; new `development_notes/improvement-plan-2026-08-27.md` and `ladder-validation-review-2026-08-27.md`; `PIPELINE_STATUS.md`; `README-R-functions.md`; this history.

### 2026-08-26, Blocking defect in the 2026-08-25 batch: five Stan data variables declared but never passed (branch `OSP-boat-count-incorporation`)

The 2026-08-25 batch was run through its own validation ladder (`06_diagnostics/run_patch_validation_2026-08-25.R`) and **every one of the six model-runs failed on its first BSS fit**, on both tracks and in every configuration, with `dim(X) must have a positive length`. No fitted results were produced; the partial folders under `05_output/20260825/` are scratch.

-   **Cause.** `crab_fraction_stan_data()` returns 17 fields. `prep_bss_crab_pooled()` and `prep_bss_crab_gear()` copy them into the Stan data list one at a time, and five were never copied: `OSPF_n`, `osp_f_stratum`, `osp_f_total`, `osp_f_crab`, `osp_f_kappa_prior_mu`. Improvement 8 puts the per-day OSP likelihood behind `if (osp_crab_lower == 1)`, but the DECLARATIONS in the `data` block are unconditional and cannot be otherwise, so Stan threw `Exception: variable does not exist; processing stage=data initialization; variable name=OSPF_n; base type=int` on every fit regardless of `use_osp_crab_lower`. That is why the pre-patch-equivalent baseline rung died identically to the shipped-config rung.
-   **Why the reported error named something else.** rstan does not raise a data-initialization failure as an R condition: it writes it to the MESSAGE stream, returns an EMPTY `stanfit` (mode 2), and returns normally. The driver then degraded silently through three layers, `summary(fit, pars=)$summary` returning `invisible(NULL)` (hence the 3-byte `bss_summary_*.csv`), `quantile(NULL, ...)` returning `NA NA NA`, and finally `apply(rstan::extract(fit, "E")$E[, 1, , 1], 2, median)` on a `NULL`, which is what threw. The `bss-fits` chunk is `results='hide'`, so the one line naming the real variable was the only diagnostic emitted and the only one not displayed.
-   **Fix, part 1: forward the five fields** in both preps. No model, prior or likelihood change: with `use_osp_crab_lower = FALSE` they are `OSPF_n = 0` plus three zero-length integer arrays, which is what the neutral path of `crab_fraction_stan_data()` always returned. The 2026-08-25 batch's intended behaviour is unchanged; it simply becomes runnable.
-   **Fix, part 2: `03_R_functions/bss_stan_fit.R` (new), called by both drivers in place of `rstan::stan()`.** Same arguments, same seed, same RNG stream, so a fit that succeeds is byte-unchanged and the gear track's exact-reproduction test still means what it meant. It adds `bss_assert_stan_data()`, which parses the `data` block of the `.stan` file in use and refuses to call the sampler when a declared variable is missing, non-numeric, or non-finite; `bss_assert_fit_usable()`, which rejects an empty `stanfit` and quotes what Stan said rather than letting `NULL` propagate; and a per-fit `stan_console_<label>_att<n>.log` written to `output_dir` from a NON-muffling `withCallingHandlers()`, so sampler progress still reaches knitr while a chain failure stays diagnosable after a `results='hide'` knit.
-   **Fix, part 3: the regression test that was missing.** Section 9 of `06_diagnostics/test_improvements_2026-08-25.R` asserts, for BOTH models, that every variable declared in the Stan `data` block is built in the corresponding prep, and that every crab-fraction field is returned by EVERY return path of `crab_fraction_stan_data()` (shore/off, `f` pinned, `f` estimated). Verified to fail on the pre-fix files and pass on the fixed ones. The harness is now 80 assertions and still needs no rstan.
-   **The process point.** The 2026-08-25 batch was reported as static-checked: both Stan files parsed under stanc 2.32.2, every R module and both drivers parsed, 67 unit assertions passed. None of those crossed the R-to-Stan boundary, which is the one seam that batch changed on both sides at once. A parse check on each side of an interface is not a check of the interface.
-   **Status.** The validation ladder has NOT been run. Every number the 2026-08-25 batch claims remains unconfirmed by a fitted run.
-   Files changed: `03_R_functions/prep_bss_crab_pooled.R`, `03_R_functions/prep_bss_crab_gear.R` (the five fields); new `03_R_functions/bss_stan_fit.R`; both drivers (`bss_stan_fit()` call site); `06_diagnostics/test_improvements_2026-08-25.R` (section 9); `README-R-functions.md`; `development_notes/PIPELINE_STATUS.md`; this history.

### 2026-08-25, Post-FWC improvement batch: eight requested items plus two defects found in review (branch `OSP-boat-count-incorporation`)

Eight improvements requested after the 2026-08-18 FWC presentation, plus two defects surfaced while auditing items 1 and 2. Everything behaviour-changing ships OFF except the three changes that were explicitly requested. Coded and static-checked only; the validation ladder that would confirm it failed on a separate defect (see the 2026-08-26 entry) and has not yet been re-run, so **nothing below is confirmed by a fitted run**. Full rationale, evidence and the run order live in `development_notes/PIPELINE_STATUS.md`.

-   **(A) DEFECT, inference-changing: the shore I/E likelihood was on the wrong unit.** `IE_obs ~ lognormal(log(lambda_E * L), sigma_IE)` was correct while shore ran on crabber-hours, where `L` was `L_effective` in hours. The v7.7 move to gear-deployments replaced `L` with `tau_shore` (a dimensionless turnover, ~1.7) but left the OBSERVATION as `ie_crabber_hours`, so since v7.7 the model has compared crabber-HOURS against a predicted crabber-TRIPS, roughly a 4x scale mismatch (~331 observed against ~80 predicted on a typical survey day). This is the most likely mechanism behind the standing unexplained shore all-gear `sigma_IE` ~1.07 (GR-9). Fixed by routing the observation through `bss_effort_spec()$ie_obs_col` (deployments -> `ie_trips`; time units -> `ie_crabber_hours`) in both preps, so the pair cannot drift again. `ie_shore_obs_unit = "crabber_hours"` reproduces the old behaviour. **This moves the shore number and must be isolated in its own run.**
-   **(B) DEFECT: shore monthly PE effort share still on crabber-hours.** `pe_monthly_effort_share.R` used the pre-v7.7 `mean_count * crabbers_per_gear * day_length` formula, re-weighting the shore monthly PE split toward long-day months. Totals were never affected (the share is normalised); the monthly split was. Now routed through `bss_effort_spec()`.
-   **(1) Gear deployments audited everywhere effort is formed**, not just the CPUE denominator: the opener-spillover diagnostic now measures CPUE per `number_of_gear`; the gear-resolved daily-combined series no longer applies the shore crabber-hours formula to the boat; the by-gear incomplete-trip bias is reported in deployments; the BSS effort plot axis and the `bss_L_effective_*.csv` `L_prior_mu` column are corrected (now `L_prior_center` + `L_unit` + a separate hours column). "Pot lift" is struck from both method documents: a deployment is a piece of gear in the water on that trip, and slot re-use is carried by `tau`.
-   **(2) Effective day length documented for what it is.** Production expands on a TURNOVER, not a day length; `L_effective` (~5.26 h) sets only a diagnostic column. Their ratio, 5.26 / 1.72 = 3.07 h against an interview-reported 3.23 h, is the method's free internal consistency check and is now stated in both documents.
-   **(3) Weekend is Saturday and Sunday.** Friday leaves `days_wkend`. Paired within month: shore log(Fri/Sat) = -0.844 (t = -8.88, p = 2.4e-06), boat -0.507 (t = -3.81, p = 0.003); against Mon-Thu, Friday is 1.21x (p = 0.10) shore and 0.99x (p = 0.96) boat. Residual variance the AR must absorb falls 15% (shore) / 5% (boat). Known residual: the boat's Sunday mean sits below its weekday mean on 5 sampled Sundays, which is a field-plan question, not a config edit.
-   **(4) Other-fishery openers as auto-selected effort covariates.** The razor-only `B3` term is generalised into a `K_open` / `X_open` / `B_open` block in both Stan models, with per-population candidate lists. `K_open = 0` is production and reproduces the previous model. `opener_covariate_mode = "auto"` screens on the spillover diagnostic's adjusted p-values after a Benjamini-Hochberg adjustment across the eight effort tests, and only where the indicator is identifiable inside the fit's window; the arbiter remains effort-stream `elpd_loo`, because selecting on a p-value then fitting the term inflates it. CPUE gets no opener covariate (all eight opener-vs-CPUE tests were null). The interaction that must not be missed: a halibut term legitimately fits the ALL-boat trailer series, but a constant `f` then converts the fitted surge to crab effort at the same rate as a closed-fishery day, so `crab_fraction_strata` gained `"opener"` / `"month_opener"` and both drivers warn when a boat opener is active without them.
-   **(5) Incomplete trips: a four-arm diagnostic; production unchanged.** `sensitivity_incomplete_trips.csv` reports `exclude` (production), `gear_only`, `impute_mean_cpue` and `keep`, with the gear ratio and a length-bias test under each. **Direct answer to the question asked: no, option 2 is not already how the model functions.** `prep_bss_crab_*()` derives `intA` from the already-filtered frame, so an incomplete trip's fully observed gear count is discarded with its truncated catch; and the two estimators disagree, since `run_pe_*()` takes the boat gear-per-group from the UNFILTERED set, so the boat PE already behaves like `gear_only` while the boat BSS behaves like `exclude`. Option 1 is a pooled no-op by arithmetic (a ratio-of-sums over complete-plus-imputed returns the complete-trip rate identically), and in the BSS it would be worse than a no-op because it adds fabricated observations that tighten the CPUE interval without adding information. The framing that bounds all four arms: effort does not come from interviews, so no treatment of incomplete trips can add effort to the expansion.
-   **(6) `bss_min_interviews` 20 -> 15.** The only component this changes is the private-boat POT CLOSURE (17 interviews), previously never fitted and entering the port total as an interval-free PE point. A second floor `bss_min_interviews_fitted` is applied AFTER zero-denominator and incomplete-trip filtering, because the pre-existing guard counts unfiltered interviews and reads looser than what the likelihood sees. Both floors moved out of each driver's `params_model` into `run_config.R`, where they belong: `params_model` is merged ON TOP of `run_config` and was silently overriding it. Honest expectation: 17 interviews over 76 days may still fail the gate, which is a better outcome than a threshold decision taken before any data were looked at.
-   **(7) AR resolution escalation ladder (`ar_escalate`, ships FALSE).** Each component starts at the finest rung, goes through the same convergence gate that decides PE-vs-BSS, and is refit one rung coarser on failure, stopping at the first pass; `ar_escalation_log.csv` records every attempt and the verdict that triggered the next. Cost stated rather than buried: each rung is a real multi-hour fit and the capped components burn their known-bad rungs first, so expect roughly 2-3x wall clock.
-   **(8) OSP crab-only counts as a HARD LOWER BOUND on `f` (`use_osp_crab_lower`, ships FALSE).** OSP does not record combo trips, so its crab-only share bounds `f` from below and is not an estimate of it; treating it as an estimate would bias the boat DOWN by exactly the combo rate, on the halibut and tuna days where that rate is highest. Structurally: `f_lower ~ Beta(1,1)`, `osp_crab_only[i] ~ BetaBinomial(osp_total[i], f_lower*kappa, (1-f_lower)*kappa)` per OSP DAY, and `f = f_lower + (1 - f_lower) * theta`. The likelihood is per day and beta-binomial, not a binomial on the stratum sum, which would treat ~7,500 boat-days as independent trials and give `f_lower` an SD near 0.005 that ~150 correlated daily observations do not support. **Identifiability, stated plainly: OSP alone identifies `f_lower`, never `theta`**; `theta` rests on the `crab_fraction_combo_share` placeholder until egress classification covers the same stratum, which is the operational reason to schedule egress days on days OSP is in port. Degradation is exact: with no OSP classification in a stratum `f_lower` is pinned to 0, `f = theta`, and R hands Stan the ordinary `f` prior. Both new terms sit on OBSERVED boat counts, so `f` stays in generated quantities only and the validated Phase 2/3 properties survive.
-   **Also in the batch.** `B2_C`, `gamma_C`, `B_open`, `kappa_OSP`, `r_OSP`, `f_crab` and `f_lower` are added to the curated `structural_params_*` table (closing the standing Tier-4 item). The opener design matrix is passed FLAT (`X_open_flat`) and rebuilt in `transformed data`. New `06_diagnostics/test_improvements_2026-08-25.R` (standalone, no rstan) and `06_diagnostics/run_patch_validation_2026-08-25.R` (the five-rung ladder, with its pass criteria written in code before the runs).
-   Files changed: `02_stan_models/crab_bss_pooled.stan` and `crab_bss_gear_resolved.stan`; new `03_R_functions/bss_opener_covariates.R` and `diagnose_incomplete_trips.R`; rewritten `03_R_functions/crab_fraction.R`; `bss_effort_spec.R`, `bss_ar_resolution.R`, `prep_bss_crab_pooled.R`, `prep_bss_crab_gear.R`, `pe_monthly_effort_share.R`, `diagnose_fishery_spillover.R`; both drivers; `run_config.R`; new `06_diagnostics/test_improvements_2026-08-25.R` and `run_patch_validation_2026-08-25.R`; the method documents; `development_notes/PIPELINE_STATUS.md`; this history.

### 2026-08-01, Merged the gear-resolved reader `fetch_crab_data_v2` into the shared `fetch_crab_data` (branch `OSP-boat-count-incorporation`)

Behavior-preserving. The gear-resolved reader `fetch_crab_data_v2.R` was near-identical to the pooled `fetch_crab_data.R`, differing only by one filter (the pooled reader drops private_boat interviews with no positive `gear_time_total`; v2 kept them). The two are now one shared `fetch_crab_data(params)`; this driver calls it with `boat_require_gear_time = FALSE`, reproducing the former v2 behavior exactly. `fetch_crab_data_v2.R` is deleted. The per-gear CPUE classification the v2 header advertised was never in this reader; it lives in `prep_bss_crab_gear.R`. Validate by run (expect byte-identical to the pre-merge v2 reader).
-   Files changed: `03_R_functions/fetch_crab_data.R` (now shared) and `fetch_crab_data_v2.R` (deleted); this driver (call site + `boat_require_gear_time = FALSE` in `params`) and the pooled driver; `README.md`, `README-R-functions.md`, `07_documentation/CLAUDE.md`, this method document, `development_notes/PIPELINE_STATUS.md`, and this history.

### 2026-07-31, OSP boat-count incorporation: second boat effort stream, crabbing fraction f, OSP-informs-tau, and two interview filters (branch `OSP-boat-count-incorporation`)

The gear-resolved track gains the same boat-effort features as the pooled track (built as Phase 0 through Phase 3, validated by a 14-run batch across both models), on the branch `OSP-boat-count-incorporation`. Changes the boat estimate materially, so confirm by a production run; full review in `development_notes/osp-validation-review-2026-07-31.md`.

-   **OSP second boat effort stream (Phase 1b, the gear-resolved mirror).** The OSP Westport Boat Launch daily boat total (`WBL_boat_counts.xlsx`, `fetch_osp_boat_counts`) enters `crab_bss_gear_resolved.stan` as a second boat effort observation on the same latent boat effort, scaled by `kappa_OSP`; the gear-resolved OSP mean sums `lambda_E_S` over the gear dimension before scaling. Toggle `use_osp_boat_counts`; FALSE reproduces the pre-Phase-1b boat exactly. Validation Step 2 raised the boat about 10% and tightened boat-effort CV 11.9% to 11.0%; the gear-resolved boat tracks the pooled boat within 1 to 2% at every step.
-   **Crabbing fraction f (Phase 2 + 2b + 3).** Trailer and OSP counts are all boats, so `crab_fraction.R` multiplies boat effort and catch by f in the boat generated quantities only (decoupled from sampling; CPUE invariant; the boat is exactly linear in f), Beta prior centered on `crab_fraction_set` (0.3), updated by the `boats_crabbing` / `boats_total` classification once the WBL egress pilot lands; the PE is f-adjusted too. Per-stratum via `crab_fraction_strata`. 0.3 is a placeholder, not a measured estimate, and is the largest lever on the boat harvest.
-   **OSP-informs-tau (`osp_scale_is_tau`, Phase 3), production ON.** Lets the dense OSP series identify `tau_boat` (the OSP mean uses `L` = `tau_boat` instead of the free `kappa_OSP`), closing GR-12. Resolved 2026-07-31: the trailer count is an instantaneous snapshot, so the OSP/trailer turnover (posterior `kappa_OSP` = 3.15, 95% [2.50, 3.91]) is real and the old `tau_boat` ~1.2 was a ~2x under-count; validation Step 6 puts the gear-resolved boat at about 27.5k vs Step 3b's 13.8k. Caveat: assumes the snapshot is timed representatively (not a fixed daily peak).
-   **Two interview filters (`fetch_crab_data_v2.R`, matching the pooled `fetch_crab_data.R`).** Non-crabbing interviews (any `number_of_gear == 0` row, regardless of trip status) and gear-tampered interviews (`gear_tampered == 1`) are dropped. Net effect on the validated 2024-25 set is about 2 interviews (the rest were already excluded by the existing `crabbers > 0` / `min_fishing_time` filters); `gear_tampered` is all blank today. Note: v2 has no `private_boat` gear-time filter (the pooled reader does), so the explicit `number_of_gear == 0` rule partly harmonizes the two readers.
-   **Production configuration (adopted 2026-07-31).** `use_osp_boat_counts = TRUE`, `use_crab_fraction = TRUE` (`crab_fraction_set = 0.3`), `osp_scale_is_tau = TRUE`; production private-boat harvest about 27,500 Dungeness kept. All 14 validation runs converged; open decisions (pilot f_hat; confirm snapshot timing) are in the validation review.
-   Files changed: `02_stan_models/crab_bss_gear_resolved.stan` (and the pooled Stan file); new `03_R_functions/fetch_osp_boat_counts.R`, `diagnose_osp_trailer_overlap.R`, `crab_fraction.R`; `fetch_crab_data_v2.R` (and `fetch_crab_data.R`) filters; `bss_day_length.R`; `prep_bss_crab_gear.R` / `prep_bss_crab_pooled.R`; `run_pe_gear.R` / `run_pe_pooled.R`; both drivers; `run_config.R`; new `04_input_files/WBL_boat_counts.xlsx`; new `06_diagnostics/run_osp_validation.R`; this history; the method documents; `development_notes/osp-validation-review-2026-07-31.md`.

### v5.6 (2026-07-12), run_config base-parameter restructure (P5), parity with pooled v7.9

Config-architecture parity with the pooled track's v7.9. No Stan change, no estimate change, and no gear-resolved run was required.

-   **`run_config.R` is the base parameter set.** The driver merge was inverted to `params <- modifyList(run_config, params_model)`, so `run_config` is the base and this model layers only its internal tuning (Stan file, per-fit sampler settings, gate thresholds) on top. The two key sets are disjoint, so behavior is identical; the inversion encodes intent (run_config is authoritative). The gear-resolved AR resolution cap map now lives in `run_config.R` as `ar_max_resolution$gear_resolved = list(shore = "weekly", private_boat = "monthly")`, selected after the merge. That map is dormant in production (`ar_adaptive = FALSE`, fixed per-sub-season `period_bss`); it binds only if `ar_adaptive` is turned on.
-   The divergence-fraction backstop was already 0.05 here, so the pooled P7 tightening (0.15 to 0.05) brought the pooled model into line with this one rather than changing anything in the gear-resolved track.
-   Files changed: `01_BSS_models/BSS-GH-gear-type-CPUE-model.Rmd` (merge inversion, AR map read from `run_config`, header to v5.6); this history.

### v5.5 (2026-07-11), Shore moved to the gear-deployment effort unit

The shore component is moved onto the gear-deployment effort unit (`shore_effort_unit = "gear-deployments"`), matching the boat, so both fitted populations now run on gear-deployments. The unit was chosen from the 2026-07-10 shore LOO comparison (shore all-gear, n=1649):

-   **Linearity, not fit, decided it.** Gear-deployments is the only shore unit whose CPUE-linearity slope `beta_h` covers 1 (1.05, 95% CI 0.94-1.15), against crabber-hours 0.57 (0.50-0.64) and gear-hours 0.73 (0.67-0.80). A `beta_h` that covers 1 means catch scales one-for-one with the effort unit, so the constant-CPUE season expansion is unbiased; a `beta_h` below 1 means the unit over-counts effort at the high end and the expansion is biased. Gear-deployments is therefore the only harvest-unbiased shore unit.
-   **No estimator-triad drift.** Gear-deployments is also the only shore unit with no drift across the three CPUE estimators: ratio-of-sums 0.87, mean-of-ratios 0.85, and model 0.85 crab/deployment all agree. When the three disagree, the unit is mis-specified.
-   **The LOO edge was a red herring.** Gear-hours had a marginally higher catch-stream `elpd_loo` (-3131, versus -3190 for deployments and -3228 for crabber-hours), but that edge is `lambda_C` absorbing the sub-linearity into the fitted rate, which is exactly what biases the season expansion. Predictive fit and harvest-unbiasedness diverge here, and the unit was chosen on harvest-unbiasedness.
-   Resolves backlog item GR-16. Sets `loo_effort_unit_comparison = FALSE` for production (the comparison is done). Harvest-unbiasedness is the operative criterion because the reported number is a constant-CPUE season expansion: if `beta_h` is below 1 the fitted rate is pulled toward the high-effort interviews and the expansion to unsampled days over-counts, so a unit with `beta_h` covering 1 is the one whose season total is not systematically biased.
-   **Retroactive record.** This entry also, belatedly, records that the boat had already moved to gear-deployments (`h = number_of_gear`, `L = tau_boat`) via the shared `03_R_functions/bss_effort_spec.R` plus the Stan `effort_scale_gear` / `E_scale` machinery. That is the F2 and P1 work; it predates v5.5 but was never carried in this framework log. Documentation Section 6.2 is corrected accordingly.
-   Files changed: gear RMD (`shore_effort_unit`, `loo_effort_unit_comparison`); gear Stan (via the shared effort module); documentation (Section 6.2, Section 13).

### v5.4, R-hat convergence gate tightened to 1.01

-   Tightened the convergence gate from R-hat < 1.05 to R-hat < 1.01 (Vehtari et al. 2021), in step with the pooled track's v6.4 so both gates use one threshold. Outcome-neutral for the 2024-25 season: every passing fit has R-hat near 1.00, and every failing fit fails on divergences or n_eff, not on an R-hat between 1.01 and 1.05.
-   Files changed: gear RMD (convergence gate); documentation.

### v5.3, R-side data prep aligned with the (then) gear-hours Stan formulation

-   Aligned the R-side data preparation with the gear-hours boat formulation the Stan model then expected. For boat fits the model expects `Gear_A_boat[IntA_trailer]` (observed gear per boat group, replacing `T_A_int` / `A_A_trailer`), `h = gear-hours`, and `L[d] = 24`. The v5.2 data list still passed the obsolete `T_A_int` / `A_A_trailer` variables and used crabber-hours / day_length for boats, which produced the runtime error `Gear_A_boat[IntA_trailer] is missing` and, if patched alone, an approximately 2x downward bias in boat catch. `prep_bss_crab()` now branches by `is_shore` for `L`, `h`, and the gear-expansion block; boat interviews lacking a valid `gear_time_total` are dropped before the Stan data list is built.
-   NOTE: this gear-hours (`L = 24`) boat formulation was itself later superseded by the gear-deployment scale (see fix-marker F2 and framework v5.5). Documentation Section 6.2 now calls the v5.3 gear-hours choice an over-correction: it fixed the missing-variable crash but replaced a wrong unit (crabber-hours) with another wrong unit (gear-hours), and the right unit is gear-deployments.
-   Files changed: gear RMD (`prep_bss_crab`, boat data list); documentation.

### v5.2 (Stan v3.2), Expected-value catch, incomplete-trip filter, divergence gate

-   **Expected-value daily catch `C_gear`** (no Poisson noise) is emitted for the CPUE trajectories, alongside a separate `C_gear_pred` (Dirichlet plus Poisson) for the season totals and prediction intervals. Trajectories read the smooth expected value; totals carry full predictive noise.
-   **Dirichlet-sampled `pi_gear`** in generated quantities propagates gear-proportion uncertainty into the per-gear catch. Raw `n_weighted_gear` is passed as data.
-   **Incomplete-trip filter** (`filter_incomplete_trips`, default TRUE) removes the roughly -20% CPUE bias for pots and traps, whose soak-time gear is often not yet retrieved at interview.
-   **Divergent transitions added to the convergence pass/fail** (`max_divergences`, default 5), matching the pooled v6.1 gate. (Reconciled 2026-07-13 to the code and the method documentation, both of which use 5; this entry previously said 0. A3.)
-   **Regulatory gear exclusion per sub-season** (`gear_exclude`) prevents fitting a CPUE for a gear type prohibited in that sub-season.
-   **Minimum effective-N per gear raised 3 -> 15** (`bss_min_gear_effective_n`) as a guard against phantom multi-gear mentions.
-   Trip-completion sensitivity output added.
-   Files changed: gear Stan (v3.2); gear RMD.

### v5.1 (Stan v3.1), Empirical gear proportions and per-gear process error

-   **Weighted fractional gear assignment** for multi-gear interviews (equal split across mentioned gear), replacing the Pot > Ring Net > Trap > Snare priority hierarchy that had assigned each interview to a single gear.
-   **`pi_gear` becomes DATA** (empirical proportions with Laplace smoothing) rather than a sampled parameter; the categorical likelihood is removed. `pi_gear` varies by period x day_type.
-   **Per-gear-type process-error SD** `sigma_eps_C_gear[G_gear]`, so each gear's CPUE process has its own volatility.
-   **`R_G` conditionally estimated** (shore) or fixed at 1.3 (boat).
-   **Cauchy prior scales tightened 5 -> 2.**
-   `bss_max_interviews` removed (the model now fits the full dataset); biweekly BSS periods added for ring-net; extended convergence checks; word-boundary regex for gear classification; day-length cap raised 16h -> 17h (46.9N summer).
-   Files changed: gear Stan (v3.1); gear RMD.

### v5.0 (Stan v3.0), Initial gear-resolved model

-   Initial gear-resolved release. Per-gear-type AR(1) CPUE processes with a shared `phi` and a shared `sigma`; Dirichlet-distributed `pi_gear` per period; the priority hierarchy for multi-gear interviews (later replaced in v5.1); the holiday effect `B2` in the effort process; sparse per-observation overdispersion.
-   Files changed: gear Stan (v3.0); gear RMD (branched from the shared pooled/gear sequence).

### Shared-sequence milestones (v1 to v4)

Versions v1 through v4 predate the gear-resolved branch and are a single shared milestone sequence, documented in `README.md`: v1 the single-population dock-only prototype, v2 the shared bug fixes (CSV columns, Stan dimensions, output folders), v3 the three-population two-sub-season pooled model with convergence tuning, and v4 the dawn/dusk day length, stat-week PE, census dates, and team review. The gear-resolved model forked at v5.0; everything above this line is gear-resolved-specific.

### Run-driven fix-markers (F and P series)

The Stan file `crab_bss_gear_resolved.stan` carries no vX.Y tag; it records its changes as fix-markers in the header comment block. The 20260709, 20260710, and 20260711 runs drove the markers below. These are the changes that the framework v5.x series does not name, and they include the single largest quantitative move in the model's history (F2).

-   **F1 (= B1.10), proper unconditional prior on `R_G_boat`.** Before: `R_G_boat` was declared `real<lower=0>` with no prior and no likelihood in the shore fits (it is a boat-only parameter). The `<lower=0>` constraint adds a Jacobian to the target, so with nothing else touching the parameter the log-posterior increased without bound, `exp(z)` plus the Jacobian drove it to +inf, `R_G_boat` ran to about 1e307, and 97.6% of shore transitions diverged. After: a proper unconditional prior `R_G_boat ~ lognormal(log(4), 0.5)`, applied in every fit. Shore_ring_net divergences 7818/8000 (97.7%) -> 598/8000 (7.5%); shore_all_gear 7807/8000 (97.6%) -> 213/8000 (2.7%); R-hat 1.0002-1.0026, n_eff 2937-9313. See Appendix A.
-   **F2, boat effort measured in gear DEPLOYMENTS, not gear-hours.** Boat catch is not linear in soak time. Binned by hours-per-gear, crab per gear-HOUR falls 43x (2.88 -> 0.068) across a 64x soak range, while crab per gear per trip rises only 1.8x; a log-log fit over 1,532 interviews gives `crab_per_gear ~ h^0.133`. Gear-hours forces that 43x per-hour gradient onto one constant CPUE, biasing the season expansion. The fix: `h = number_of_gear`, `L = tau` (turnover, a parameter with a lognormal prior), `E = lambda_E * tau`, and `lambda_C` becomes crab per deployment (stable, 4.0-7.6). Boat catch 155,038 -> 43,268; `R_G_boat = 3.554` (3.244-3.874); boat CPUE 2.930 crab/deployment; `beta_h` 0.754 (now covers 1). See Appendix A. This scale is what the pooled track later ported as POOL-1 and POOL-3.
-   **F4, CPUE diagnostics wired.** `write_cpue_diagnostics`, `bss_effort_spec`, and the linearity / saturation / estimator-triad checks plus the effort-unit assertion. The linearity check regresses log-catch on log-effort to estimate `beta_h`; the saturation check bins by effort and reports the per-unit rate gradient; the estimator-triad check compares ratio-of-sums, mean-of-ratios, and the model CPUE. These are the diagnostics that surfaced the shore effort-unit question resolved in v5.5 and are the model-independent evidence that gear-hours is invalid for pots (the F2 finding).
-   **F5, reporting.** `ar_resolution` populated; `divergence_fraction` / `impact` / `pass` columns added to the convergence report; `expansion_ratios` reports `R_G_boat` (replacing the removed `R_T`); `port_total` composition reported so the assembled total is auditable against its components. These are report-only and change no estimate.
-   **P0 (2026-07-10, commit d1968f3), PE population-argument bugfix and ratio-of-sums strata.** `run_pe` received the component label (for example `"shore_all_gear"`) in the slot where it expected the population, so every shore component silently ran the boat branch. Combined with a switch to ratio-of-sums PE strata (stratum CPUE = `sum(catch)/sum(h)`, replacing the unstable weighted mean-of-ratios) and a permanent PE implied-CPUE-vs-ratio-of-sums assertion. The PE had been internally inconsistent: implied CPUE was 2.88x the ratio-of-sums for shore_all_gear, 2.61x for shore_ring_net, and 0.55x for private_boat. Effect: shore PE 45,112 -> 19,767; PE total 80,160 -> 54,598. See Appendix B.
-   **P1 (2026-07-10), configurable effort unit with LOO comparison.** `bss_effort_spec.R`, `shore_effort_unit`, and `loo_effort_unit_comparison`; the Stan `effort_scale_gear` / `E_scale` machinery so `E` always carries `h`'s unit, which makes the cross-unit `elpd_loo` comparison valid. This is the lever the v5.5 shore decision was read off.
-   **P2 (2026-07-10), mu-hierarchy collapse at `G*S == 1`.** `use_mu_hier = (G*S > 1) ? 1 : 0` drops the redundant single-cell hierarchical level that funnels. The pooled track attempted the identical change (B1.7 in v6.9) and reverted it, because its shore all-gear DAILY AR (289 states) hung for more than 24 hours once the decoupled level was removed. The gear-resolved track is safe because its shore AR is monthly or biweekly, not daily, so removing the level does not force the effort level to reconcile against a long daily AR. P2 produced two new backlog items: GR-17 (the collapsed `sigma_mu` posteriors look non-converged but are inert) and GR-10 (the collapse is hardcoded and needs a config lever). The pooled track carries the equivalent lever as POOL-4's `collapse_mu_hier` (default off, so its production posterior is unchanged); the gear-resolved track applies the collapse unconditionally at `G*S == 1` and still needs the same config treatment.
-   **Parity ports already in the Stan file.** B1.3 non-centered AR(1) initial state; B1.5 effort overdispersion marginalized to NB2; B1.6 `sigma_IE` proper unconditional prior; B1.8 `C_expected_sum` deterministic gate total; B1.9 weekend CPUE effect `B1_C` (`estimate_B1_C`, default TRUE). These carry the pooled convergence-work conclusions into the gear-resolved model without re-deriving them.

### Outstanding backlog (GR-7 to GR-17)

The GR-series is maintained in `07_documentation/development_notes/20260710-OUTSTANDING_ISSUES.md`, which carries a per-item "Status after the 2026-07-10 run" block. An important caveat when reading the status column: the 2026-07-10 run's only code changes were P0, P1, and P2, so where a GR item's symptom cleared, the movement is usually a side-effect of P0 or P2 rather than a targeted GR fix. GR-8 is the clearest case: its funnel symptom cleared because P2 collapsed a separate `sigma_mu` level and pulled total divergences under the gate backstop, while the item's own proposed guard had not yet landed as of that run. (The `ie_min_obs_shore = 3` guard landed later, 2026-07-13, as batch item 5, resolving GR-8; the `sigma_IE` prior was deliberately left `exponential(5)`, so no prior retune was made. See the GR-8 row below.)

| ID | Item | Status |
| --- | --- | --- |
| GR-7 | Option A / genuine per-gear CPUE unbuilt: `G = 1`, the per-gear machinery is inert, gear catch is PE-apportioned rather than modeled | OPEN |
| GR-8 | `sigma_IE` funnel in shore_ring_net | RESOLVED 2026-07-13 (item 5): the `ie_min_obs_shore = 3` guard landed in both models, dropping the I/E stream and decoupling `sigma_IE` for thin shore fits. The `sigma_IE` prior was deliberately left `exponential(5)` (per batch item 4), so the guard, not a prior retune, is the fix. |
| GR-9 | shore_all_gear `sigma_IE = 0.603` unexplained | OPEN |
| GR-10 | No `collapse_mu_hier` config lever (`use_mu_hier` is hardcoded) | OPEN |
| GR-11 | Gate policy: the hard 5% divergence backstop overrides the scale-aware impact test | OPEN |
| GR-12 | `tau_boat = 1.2` rests on only 2 WBL I/E days | OPEN |
| GR-13 | Dead `stan_data` entries plus dot-prefixed metadata passed into `rstan::stan(data=)` | OPEN (low) |
| GR-14 | Latent AR carries only 4-13% of effort variance; NB overdispersion carries 81-91% | TRUE BY DESIGN (coarse fixed AR) |
| GR-15 | `private_boat_ring_net` never fits (about 17 interviews) | OPEN (low) |
| GR-16 | Shore effort unit | RESOLVED 2026-07-11 (see v5.5) |
| GR-17 | Collapsed `sigma_mu` posteriors look non-converged but are inert | NEW / OPEN (low) |

------------------------------------------------------------------------

## Appendix A: F1 and F2 working notes, the boat improper prior and the deployment-scale correction

These are the consolidated working notes behind fix-markers F1 and F2, the two changes that made the shore fits converge and corrected the boat effort scale. Together they are the single largest quantitative change in the gear-resolved model's history, and F2 is the reference implementation the pooled track later ported as POOL-1 and POOL-3.

**F1, the improper direction. What went wrong.** `R_G_boat` (gear per boat group) is a boat-only parameter, used only in the boat trailer-expansion likelihood. It was declared `real<lower=0>` with no prior. In a shore fit it appears in no likelihood at all. A `<lower=0>` declaration means Stan samples an unconstrained `z` and maps `R_G_boat = exp(z)`, adding the Jacobian `log|dR/dz| = z` to the target. With no prior and no likelihood, the target along that direction is just the Jacobian `+z`, which is unbounded above, so the sampler pushed `z` and hence `R_G_boat` toward positive infinity. `R_G_boat` ran to about 1e307 (the floating-point ceiling), the runaway direction wrecked mass-matrix adaptation, and 97.6% of shore transitions diverged. This is structurally the same bug as the pooled B1.6 `sigma_IE` improper direction (a constrained parameter with a Jacobian that no prior or likelihood ever touches), which is why it is filed as B1.10 in the shared marker sequence.

**F1, the fix and outcome.** Give `R_G_boat` a proper unconditional prior `R_G_boat ~ lognormal(log(4), 0.5)`, applied in every fit rather than only when the population is boat. In a shore fit it now enters no likelihood but is proper, so it sits near 4 and cannot run away; in a boat fit the likelihood updates it as before. The effect on the shore fits was decisive: shore_ring_net divergences 7818/8000 (97.7%) -> 598/8000 (7.5%), shore_all_gear 7807/8000 (97.6%) -> 213/8000 (2.7%), with R-hat 1.0002-1.0026 and n_eff 2937-9313. The shore components became reportable BSS fits rather than near-total divergence failures forced to PE.

**F2, the wrong unit. Why gear-hours is invalid for pots.** With the shore fits converging, the remaining error was the boat effort unit. The boat CPUE denominator was gear-hours (`h = gear_time_total`, `L = 24`), which assumes boat catch is linear in soak time. It is not. Binned by hours-per-gear, crab per gear-HOUR falls 43x (from 2.88 to 0.068) across a 64x soak range, while crab per gear per trip rises only 1.8x. A log-log fit over 1,532 interviews gives `crab_per_gear ~ h^0.133`, an exponent near zero. The mechanism is saturation: a pot or trap catches most of what it will catch soon after it is set, and the per-hour rate collapses as it soaks. Gear-hours forces a 43x per-hour gradient onto a single constant CPUE, so a fit that matches short soaks over-predicts long ones and the constant-CPUE season expansion inherits the bias.

**F2, the fix and outcome.** Measure boat effort in gear DEPLOYMENTS, not gear-hours: `h = number_of_gear` (the count of gear set), `L = tau` (turnover, a parameter with a lognormal prior, roughly how many times gear is pulled and reset per day), `E = lambda_E * tau`, and `lambda_C` becomes crab per deployment, which is stable across the soak range (4.0-7.6). The reported quantities moved substantially: boat catch 155,038 -> 43,268 (about a 3.6x reduction), `R_G_boat = 3.554` (3.244-3.874), boat CPUE 2.930 crab/deployment, and the linearity slope `beta_h = 0.754` now covers 1 where gear-hours had held it far from 1. This is the number that supplied the pooled track its missing validation and moved the pooled boat from gear-hours (about 56,266) onto deployments (about 43,314). The two fixes reinforce each other: F1 made the shore fits reportable and F2 put the boat on the harvest-unbiased scale, so both fitted populations now sit on BSS posteriors that the gate can accept.

**The standing lesson.** F1 is the same class of bug as the pooled B1.6 `sigma_IE` improper direction, and it was caught the same way, by a divergence-localization diagnostic that pointed at a single parameter pinned at the floating-point ceiling. That both tracks independently grew an improper boat-only direction in a shore fit is the argument for the shared parity ports and the shared effort module: a fix derived once should be carried, not re-discovered. F2 is the mirror-image lesson on the R side. A denominator that looks like a reasonable effort measure (gear-hours) can be quantitatively wrong by a factor of three on the harvest total, and the only reliable test is an empirical one (the saturation and linearity diagnostics of F4), not a plausibility argument. Neither fix was adopted on reasoning alone; both were confirmed by a run and by the before/after numbers recorded above.

------------------------------------------------------------------------

## Appendix B: P0 working notes, the PE population bug and the ratio-of-sums estimator

These are the consolidated working notes behind fix-marker P0 (2026-07-10, commit d1968f3). P0 is a point-estimator fix, not a Stan change, but it matters because the PE is both the fallback estimate when a BSS fit is gated out and the independent cross-check on every BSS fit. A badly biased PE corrupts the gate comparison and, when a component falls back, ships a wrong number.

**The population-argument bug.** `run_pe()` is called once per component. It received the component label (for example `"shore_all_gear"`) in the argument slot where it expected the population (`"shore"` or `"private_boat"`). Downstream, the branch that selects the shore-versus-boat effort and CPUE construction tested that argument against the boat population, and since `"shore_all_gear"` is not `"private_boat"` the test never matched for shore, so every shore component silently fell through to the boat branch and was computed with the boat's effort construction and denominator. The symptom was not a crash; it was a plausible-looking but wrong shore PE. The fix passes the population explicitly, separately from the label.

**The estimator inconsistency.** Independently, the PE stratum CPUE was a weighted mean-of-ratios, the average of per-interview `catch/effort` ratios. That estimator is unstable when effort varies across interviews within a stratum, because a low-effort interview with a few crab produces a large ratio that the mean over-weights relative to its share of the catch. The result was an internally inconsistent PE: the implied CPUE (PE catch divided by PE effort) was 2.88x the ratio-of-sums for shore_all_gear, 2.61x for shore_ring_net, and 0.55x for private_boat. The fix replaces mean-of-ratios with ratio-of-sums (stratum CPUE = `sum(catch)/sum(h)`), the harvest-consistent estimator whose implied CPUE equals itself by construction, and adds a permanent assertion that the PE implied CPUE equals the ratio-of-sums within tolerance, so this class of inconsistency cannot silently return.

**Effect and significance.** Together the two changes dropped the shore PE from 45,112 to 19,767 and the PE total from 80,160 to 54,598. Because the corrected PE is the cross-check the BSS is read against, P0 is also what made the shore effort-unit question legible: once the PE and the ratio-of-sums agreed, the linearity and estimator-triad diagnostics (F4) could show cleanly that no shore unit except gear-deployments produced a consistent, unbiased rate, which is the finding that GR-16 and framework v5.5 later resolved. P0 restored the PE as a trustworthy fallback and a trustworthy cross-check in one commit.

**Why the assertion is permanent.** The population-argument bug was silent: it produced a plausible number, not an error, so it survived until the ratio-of-sums cross-check exposed a 2.88x discrepancy that no correct estimator could produce. The permanent implied-CPUE-vs-ratio-of-sums assertion is the guard against that class of failure returning under a later refactor. It halts the run rather than shipping a PE whose implied CPUE and stratum CPUE disagree, which is the signature of either a mis-routed population or a re-introduced mean-of-ratios. This is the point-estimator analogue of the effort-unit assertion added in F4: a cheap invariant that converts a silent, plausible-looking bias into a loud, immediate failure.

------------------------------------------------------------------------

## Appendix C: Numbers of record

The two runs that bracket the F and P series, plus the 2026-07-13 gear-resolved validation run (Run 2). These are the component-level numbers the totals were assembled from; they are preserved here because the fit objects are not saved and the run outputs are the only durable record.

**20260709 run (commit 564071b).** Before the P0 PE fix and before the shore effort-unit decision.

| Component | PE | BSS | Reported basis |
| --- | --- | --- | --- |
| shore_ring_net | 12,174 | 6,930 (pooled BSS 6,733) | gated PE |
| shore_all_gear | 32,937 | 19,680 | gated BSS |
| private_boat_all | 22,070 | 43,268 | gated BSS |
| comm / charter | - | - | 12,007 census |

Reported port total 88,238 (95% CI 75,656-106,266), using the PE for shore_ring_net. The BSS-reconstituted interim total (substituting the shore ring-net BSS for its inflated PE) was about 82,856.

**20260710 run (commit d1968f3).** After P0, F1, and F2.

-   Convergence improved from 2/3 to 3/3 fits passing.
-   Shore ring-net: the inflated PE 12,174 was replaced by the BSS 6,936 (the fit now passes the gate).
-   Shore all-gear: BSS about 19,684.
-   Boat all-gear: BSS about 43,314, on validated deployments (the F2 scale).
-   PE totals dropped from the P0 fix: shore PE 45,112 -> 19,767; PE total 80,160 -> 54,598.

The 20260710 run is the first in which all three fitted components report on their BSS posteriors on the corrected effort scales, with the PE serving as a consistent cross-check rather than an inflated fallback. Framework v5.5 (2026-07-11) then moved the shore component onto gear-deployments to match the boat and closed GR-16.

**20260713 run (Run 2), gear-resolved default.** The independent cross-check on the pooled Run 1.

-   Reported port total BSS 82,995 (95% CI 70,238-101,111); all 3 fitted components pass the gate.
-   Boat all-gear BSS 43,314, matching the pooled Run 1 boat (43,180) to 0.3% and confirming cross-model boat consistency on the deployment scale.
-   Shore ring-net (the pot-closure sub-season) passes cleanly at biweekly AR (22 divergences, 0.27%), where the pooled ring-net failed at daily AR (1,165 divergences); this is the evidence that coarser shore AR is the fix for that pooled regression.
-   Carries the 2026-07-13 gear-touching batch items: the stale crabber-hours labels fixed to gear-deployments (item 8), the PE monthly-share consolidated into `pe_monthly_effort_share.R` with the gear boat PE-fallback moved onto the deployment formula (item 9), and the PE empty-stratum pooled fallback applied to both PE tracks (item 2).

------------------------------------------------------------------------

## Current production configuration

For a reader running the model today, the production state as of framework v5.6 (2026-07-12) is:

-   **Effort units.** Shore and boat both on gear-deployments (`h = number_of_gear`, `L = tau`); `loo_effort_unit_comparison = FALSE`. Both units have a linearity `beta_h` covering 1, so the season expansion is harvest-unbiased.
-   **Fitted components.** Shore ring-net, shore all-gear, and private boat all-gear are fit as BSS and, in the latest run, all pass the convergence gate; commercial and charter is a census tally. `private_boat_ring_net` never fits and falls back to PE (GR-15).
-   **Gates and filters.** R-hat < 1.01, n_eff > 400, a divergence-fraction backstop at 5%, and the scale-aware impact test (GR-11 notes the backstop can still override the impact test). `filter_incomplete_trips = TRUE`. `filter` and gate helpers are the shared modules, so pooled and gear-resolved cannot drift.
-   **Known open dependencies on the reported number.** The boat catch rests on the `tau_boat = 1.2` prior until the boat I/E stream activates (GR-12), and per-gear catch is PE-apportioned rather than modeled because `G = 1` (GR-7). State both in any co-management number.
