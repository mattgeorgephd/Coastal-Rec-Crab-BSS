# Change register: the OSP boat-count branch

**Last updated:** 2026-09-11 (the charter component made an expansion, three holidays added: entries A26-A27, B18, C 2026-09-11, D16 closed by Matt, D17 closed, D19-D20)
**Branch:** `OSP-boat-count-incorporation`. **`main` is the pre-FW-creel-meeting, pre-OSP state.**
**Authoritative run:** `05_output/20260904/pooled-CPUE-AD-A1-adopted`, port total **72,027 [53,018, 101,364]**, 4 of 4 components fitted.

**WDFW has published no estimate from this pipeline.** Every "adopted" below means adopted into the working model on this branch, not released. There is no published figure a change has to stay consistent with.

Status vocabulary: **ADOPTED** (shipping in `run_config.R`, validated by a run) / **BUILT, INERT** (coded and tested, off by default, waiting on data or a decision) / **OPEN** (not resolved) / **REJECTED** (tried, not kept) / **BLOCKED** (waiting on something outside the code).

---

## A. Model and estimator changes

| # | Change | Status | Evidence | Effect on the number |
|---|---|---|---|---|
| A1 | **Gear-deployments as the effort unit** (`h = number_of_gear`, `L = tau`), shore and boat, both tracks | **ADOPTED** (v7.7) | `cpue_linearity_*.csv`: `beta_h` covers 1 on both units; catch is sub-linear in soak time (saturation 0.13-0.27), so crabber-hours fails the linearity test | Structural; the basis of every number since |
| A2 | **Shared turnover** `shared_tau`, boat-only via `shared_tau_min_obs = 15` | **ADOPTED** 2026-09-01 | Stage S2 bit-identity gate; `tau_bar` agrees across the two independent tracks to **0.05%** (2.6128 vs 2.6114) as of 2026-09-08 | Replaced 289 per-day draws with one estimated turnover |
| A3 | **PE / BSS incomplete-trip arm alignment** (`pe_gear_ratio_arm = "match_bss"`) | **ADOPTED** 2026-09-02 | Stage P1: largest change **-0.059%** on the boat PE, shore unchanged (the control) | A consistency fix, not an accuracy one |
| A4 | **Shore all-gear AR: daily -> weekly** (`ar_max_resolution$pooled$shore$all_gear`) | **ADOPTED** 2026-09-07 | Ladder (C2) fitted all four rungs; adequacy went `p_loo` 35.2% -> 9.5% of `n_obs`, Pareto k>0.7 **41 -> 0**, `cov50` dev 0.204 -> 0.066, miscalibration flag cleared. Routing proven identical to the candidate across **10,253 parameter rows** | Shore all-gear 20,898 -> 21,489; port **+0.72%** |
| A5 | **Zero-inflated shore catch likelihood** (`estimate_catch_zi = TRUE`, scoped to shore) | **ADOPTED** 2026-09-07 | At the same resolution: zero bin z +3.7 -> +2.0, one bin z -6.1 -> -3.3, pot-closure replicate closes both; elpd **+11.6 nats at 2.30 paired SE** | Shore all-gear -58 crab at weekly (-0.27%) |
| A6 | **Gear-track boat all-gear sampler settings** moved from an experiment override into the driver | **ADOPTED** 2026-09-02 | Divergences **554 -> 2**, `tau_bar` n_eff 49 -> 19,292, gear port 51,385 -> 70,953 | Restored a cross-check that had been unavailable |
| A7 | **OSP boat-count stream** (`osp_scale_is_tau = TRUE`, `kappa_OSP` decoupled) | **ADOPTED** | 130 in-window days ingested; the OSP mean uses `L[day]` rather than a free scale | The reason this branch exists |
| A8 | **OSP crabbing-only lower bound on `f`** (`use_osp_crab_lower`, `f_lower`) | **BUILT, INERT** | The column `WestportCrabOnlyEffort` is absent from the workbook, so the bound never binds and `f` sits on the set value 0.30 | None until the data arrives |
| A9 | **Other-fishery opener effort covariates** (`K_open` / `X_open` / `B_open`) | **BUILT, INERT** | `opener_covariate_mode = "off"`; the razor-dig predecessor `B3` bought no predictive gain (elpd within 1 SE) and moved the port +0.6% | None |
| A10 | **Same-day-effort density term** `gamma_C` (`estimate_cpue_density`) | **REJECTED** | Run 4 was pathological (one chain ~17x slower, treedepth saturation); `gamma_C` indistinguishable from zero where it did fit | None |
| A11 | **GR-7 Phase 2 Dirichlet gear shares** (`gear_share_dirichlet`) | **BUILT, INERT** | Coded 2026-07-21, parses under stanc 2.32.5, byte-identical to Phase 1 when off; sampled in stages V2/V3 | None at `G = 1` |
| A12 | **Weather / tide covariate module** | **REJECTED and STALE** | Excluded on its own evidence; the fork is now missing ~40 data variables the production model declares | None; do not cite its boat number |

| A13 | **Season portability** (2026-09-09): non-intersecting closure windows yield a single all-gear sub-season instead of an error; `season_filter` accepts a vector for multi-season spans; `validate_season_window()` stops loudly on a stale season/window pairing; SEASON-DERIVED tags + NEW SEASON CHECKLIST in `run_config.R`; `ar_force` reframed as the per-fit resolution pin in the new-season workflow | **ADOPTED** 2026-09-09 | Functional tests in the harness (window cases, vector filter, validator stop); the program goal recorded in `CLAUDE.md` and Section 1n | None on 2024-25 outputs (full-season configs unaffected); part-season windows go from ERROR to running |

| A14 | **Multi-season spans** (2026-09-10): `pot_closures` list gives one closure window per season (`build_subseasons()` emits `ring_net_only_<season>` / `all_gear_<season>` per closure, `all_gear_pre_<season1>` before the first; 0/1 closure falls through to the byte-identical scalar path); `census_windows` named list gives a per-season commercial census (recursive expansion, summed, `by_season` kept); the pooled report writes `season_totals.csv` always and a season table on multi-season runs (caption corrected 2026-09-08: sub-seasons are independent fits, nothing is shared across seasons but the pooled I/E regression and the priors); the validator warns per season on interviews-with-zero-effort ("pure imputation"), warnings no longer silenced by `quiet`. **CORRECTED 2026-09-12**: this row used to claim a known approximation, that `pot_open_date` is "a single scalar feeding the `estimate_L_effective()` I/E regression split, exact only for the first season of a span". IT DOES NOT AND NEVER DID. `pot_open_date` was a formal argument of `estimate_L_effective()` that appeared nowhere in the body; the regression's only predictors are `yday` (quadratic) and day type. The dead argument is removed and every caller updated. THE REAL multi-season approximation, now documented at the function: the regression POOLS every season's I/E days into one `yday -> L` curve, so a per-season interaction is the fix if two seasons' I/E day lengths diverge (check the residuals by season in `L_effective_ie_detail.csv`). `pot_open_date`'s three surviving consumers are the "Pots open" plot marker, the gear driver's monthly-panel vline, and `build_subseasons()`'s fallback for a NULL `pot_closure_end`; a stale value is cosmetic, and `validate_season_window()` now warns when it or `pot_closure_start`/`end` falls outside the estimation window (which is what the 2026-09-11 ladder pin missed) | **BUILT, INERT** 2026-09-10 (architecture done; the two-season run is staged in `run_config.R` but BLOCKED on data, see D8) | Harness section 46 (18 assertions: exact 2023-25 four-sub-season shape, legacy fall-through incl. list form, out-of-window drop, overlap/label stops, census guard, validator warning under quiet, config self-consistency); both drivers purl-parse | None until a multi-season config is run; single-season runs byte-identical by the fall-through |
| A15 | **Boat turnover prior from the OSP/trailer overlap calibration; the boat PE on the same turnover** (2026-09-08, review items 3 and 5): `tau_boat_prior_mu = "calibration"` resolves per window to the overlap's mean-per-visit implied turnover (3.03 on 2024-25, 61 pairs) with `tau_boat_prior_sigma = 0.5`; `bss_resolve_tau_boat_prior()` replaces the value so the PE, the shared-turnover prior and the tau projection expand on one number | **ADOPTED in config, VALIDATION PENDING (ladder R2)** | The retired lognormal(log 1.2, 0.3) sat 3+ prior SDs below the calibration and shrank `tau_bar` by 13% (posterior 2.60 vs likelihood-only 2.97); the PE expanded on 1.2 while the BSS learned 2.6 (PE effort 3,709 vs BSS 11,118) | Boat about +12 to +15% (R2 measures it) |
| A16 | **Unit-aware fishing-time filters** (2026-09-08, review item 8): the 0.5 h threshold applies to time-denominated units only; under gear-deployments the sole guard is `drop_unfished_zero_catch` (no positive time AND zero catch) | **ADOPTED, VALIDATION PENDING (ladder R1)** | Restores 22 boat trips (14 with recorded catch) and ~140 shore rows; complete CPUE ratio-of-sums 3.26 -> 3.17 (boat), 0.979 -> 0.976 (shore); one commercial interview restored (census 11,821 -> 11,753) | Small; the boat CPUE a few percent lower |
| A17 | **Gear split by trip-level bootstrap, per population x sub-season** (2026-09-08, review item 6), replacing the Dirichlet on crab counts | **ADOPTED (reporting only)** | Crab arrive in trips; the Dirichlet(catch + 0.5) treated every crab as an independent draw and understated the share intervals several-fold; the season-wide shares attributed Pot catch to the pot closure | None on the totals; wider gear-share intervals |
| A18 | **Crabbing fraction f from the sampler boat contacts, month strata, DYNAMIC** (2026-09-08, review item 1A/1B): every private boat approached at the launch is a classification row (`dwg$boat_contacts`, 422 on 2024-25, 0.49 pooled, 0.9+ Dec-Feb, 0.26 Jul-Sep); f is a logit random walk across chronological month strata (learned step SD, Student-t(4) steps), observed per day by the contacts (beta-binomial) and, when OSP's crabbing-only column arrives, by the OSP counts through f(1 - c); f still enters generated quantities only | **ADOPTED in config, VALIDATION PENDING (ladder R3a/R3)** | Desk fit (boat all-gear, 4 x 1200): f tracks the monthly shares, thin months borrow (Feb 0.90 [0.73, 0.98] on 5 contacts), `sigma_f` 0.66 [0.34, 1.38], every non-f parameter within MC error of the legacy control, component +17.5% against Phase A (36.5k -> 42.9k), whose kappa = 20 prior held December at 0.78 | Boat up: f moves from a flat 0.30 to ~0.95 in the winter months that carry most of the boat catch |
| A19 | **Shore turnover at the count hours, derived from the I/E `time` column** (2026-09-08, review item 2): diel presence profile over the I/E days evaluated at the shore count hours (count-time-weighted ratio of sums, 2.48 on 2024-25; peak-based 1.67), bootstrap log-SE 0.044, `tau_shore_prior_mu = "derived"` adopts it and waives the shore shared-turnover floor | **ADOPTED in config 2026-09-09 (Matt: the count hours were not in the workbook when 1.7 was derived, so the old value was the wrong quantity); ladder R4 isolates it** | The shore counts are taken 9:00-14:00 when presence is ~0.7 of the peak, so the peak-based 1.7 under-expands the count by ~1.47x; weekday and weekend profiles agree (2.47 / 2.48) | Shore x ~1.47 if adopted: the largest single mover in the series |
| A20 | **Commercial/charter census: observed vs imputed, with the imputation SE** (2026-09-08, review item 4): exact on the 47 tally days (7,884 crab), imputed on 23 unsampled days (3,869 crab, day-type means), SE 426 (3.6%); `census_uncertainty = "imputed_days"` carries it into the port interval | **SUPERSEDED 2026-09-09 by A22 (kept as `census_expansion = "day_type"`)** | The tally is a census on the days it was taken and the roster left 23 of 70 days unsampled (9 Mondays, 8 Sundays); a day type with no sampled day used to expand to NA | None until switched on; the better fix is daily vessel counts for the unsampled days (D10) |
| A21 | **Trip types in the interview workbook; the combo-trip share c observed** (2026-09-09): `interview_combined.xlsx` is rebuilt from the creel-database export (`04_input_files/build_interview_combined.R`, raw export kept in `raw/`) with `trip_type` / `trip_type_class` (crab_only, combo, other_fishery, non_fishing), `creel_area`, `interview_time`, and `gear_tampered` filled; the contact builder reads the trip type (crabbing = crab_only + combo) and restricts to the launch sites (`crab_fraction_contact_areas`); c gets its own per-stratum logit walk in both Stan models, observed per day by the combos among the typed crabbing boats (beta-binomial, `CFC_n` rows); the OSP stream reads f(1 - c[k]) and `f_lower_out` = f(1 - c) is the prediction of OSP's column | **ADOPTED in config, VALIDATION PENDING (ladder R3a/R3)** | 2024-25 at the launches: 300 contacts, 143 crabbing, 43 combos (none in Dec-Feb, half or more in Mar-Sep). Desk fit (boat all-gear, 4 x 1200): c 0.02 in Dec-Feb rising to 0.4-0.8, `sigma_c` 1.05 [0.39, 2.40], every non-f parameter within MC error of the Phase A control (max abs z 1.8), component 46,511 against 35,698 for Phase A. The tampered filter now live (13 GH interviews of 2024-25) | c itself does not move the boat (f does); the launch-site restriction moves the summer f (July 0.47 against 0.31 with the docks), about +8% on the boat all-gear component against the all-areas contacts, pending the protocol question D13 |
| A22 | **The census is the exact sum over the tally days** (2026-09-09, item 4 settled): samplers are scheduled on the days the charter and commercial (recreational) vessels are confirmed to operate, so a window day without a tally is a day with no operation; `census_expansion = "none"` (shipped) puts the unsampled days at zero, `"day_type"` keeps the A20 expansion; `census_uncertainty = "sampling"` (alias `"imputed_days"`) carries the remaining per-vessel-mean variance | **ADOPTED** | 2024-25: 7,884 crab on 47 tally days (A20's expansion gave 11,753); the per-vessel-mean SE is 131 (1.7%), reported and not carried | Census -3,869 against the 2026-09-04 baseline's 11,821 (-33% on the component, about -5% on the port), by design |
| A23 | **Sampler shifts as an input; the shift-coverage diagnostic** (2026-09-09): `sampler_shifts.xlsx` (built from the survey export by `build_sampler_shifts.R`: check-in / check-out per survey, 11 flagged rows held out), `fetch_sampler_shifts()`, `diagnose_shift_coverage()` (union of a day's windows, share of the day's boat returns inside them from the boat I/E return profile, contact hour by trip type, crabbing share by hour bin); `crab_fraction_shift_weighting = "none"` is the documented hook for a return-time weighting once OSP's all-day profile exists | **ADOPTED (diagnostic only)** | 2024-25: 199 shift days, in port 09:44 to 15:52 (6.1 h), 75% of a day's boat returns inside the shift (9% before, 15% after), contact-hour medians 12:59-13:18 for combo / crab-only / finfish boats, non-fishing 11:41; crabbing share of typed contacts flat by hour bin (0.42-0.58) | None; it measures the exposure behind D11 |
| A24 | **Every input workbook built from the per-season creel workbooks** (2026-09-10): `04_input_files/raw/<YYYY><YY>_rec_crab_harvest_data.xlsx` (the creel database exported one season at a time, every sheet) replace the two pasted exports; six builders plus `build_helpers.R` and `build_all_inputs.R` write `interview_combined.xlsx` (38,962 rows, 31 columns, 2022-23 through 2026-09-08), `effort_combined.xlsx` (12,056 counts, four seasons, every count column, a `qc_flag`), `sampler_shifts.xlsx` (3,077 surveys with the sampler's on-site conditions), `wes_commercial_tally.xlsx` (2024-25 and 2025-26), `charter_trips.xlsx` (new), `crabbing_holidays.xlsx` (2022-23 through 2026-27 by one rule); readers go through `read_input_workbook()` (guess_max over the whole column) | **ADOPTED** | 2024-25 rows reproduce the earlier workbooks (interviews: 0 differences on the legacy columns beyond database corrections; effort: 0 differences on 3,256 rows; tally: identical); 2023-24 gains its gear count (D15) and 4,116 effort counts (D8); the 2022-24 gear labels are harmonised to the 2024-25 vocabulary (the gear-resolved regex classed 1,906 single-gear 2023-24 "Collapsible trap or ring" rows as TRAP; they are ring nets) | None on the 2024-25 fitted components; the 2023-24 and 2025-26 seasons become fittable |
| A25 | **The charter trip roster as the charter census frame** (2026-09-10, `charter_frame = "roster"`): the season workbooks' "charter trips" sheet lists every charter crab trip the operators reported (interviewed / missed / canceled); `estimate_comm_charter()` takes, per day, the larger of the roster's trips and the tally's charter column on every day of the window, exact and never expanded; the commercial vessels stay on the tally days; `"tally"` reproduces A22 | **ADOPTED** | 2024-25: 31 sailed Westport charter trips in the window, 8 of them (all "missed") on days WITHOUT a tally, 3 tally days where the tally has one charter and the roster two; census 7,884 -> 8,592 (+708, of which 514 on the 8 roster-only days); SE 131 -> 171 (2.0%). 2025-26: 2 roster-only trips, census 3,419 on Dec 1 to Jan 3 | +708 crab on the 2024-25 census (about +1% on the port). The roster contradicts the A22 premise for CHARTERS (a quarter of their trips fell on unsampled days); nothing checks it for the commercial vessels, which have no roster |
| A26 | **The commercial/charter component split into a CENSUS and an EXPANSION** (2026-09-11, Matt: "a complete census without error applies to the commercial boats; the charter vessels are not 100% sampled and need expansion"). Commercial: unchanged, the exact sum over the tally days (`census_expansion = "none"`), with the per-vessel catch mean reported as a near-census sample mean and not carried. Charter: `N x` (mean catch per interviewed trip) over the charter TRIP frame, stratified by vessel (`charter_expansion = "vessel"`), with the SRS-with-FPC variance `N^2 (1 - n/N) s^2 / n`; a vessel with fewer than two interviews borrows the pooled charter mean and SD. `census_uncertainty = "charter"` (new default) carries the charter expansion variance into the port interval and nothing else; `"none"` and `"sampling"` unchanged. `carried_var` / `carried_se` are what the drivers draw with; `Dungeness_Kept_var` stays the total | **ADOPTED** | 2024-25 Westport, Dec 1 to Feb 8: commercial 6,405 crab on 164 vessel-trips (141 interviews, SE 123 = 1.9%); charter 34 trips in the frame, 20 interviewed (59%), 1,286 crab observed, **2,133** expanded (Ultimate 25 trips x 70.2, Outta Line 6 x 30.7, 3 unattributed x 64.3), SE 73 = 3.4%. Total 8,538. `charter_expansion = "pooled"` gives 2,186 (SE 118); `charter_frame = "tally"` reproduces the 2026-09-09 7,884; tally + `census_expansion = "day_type"` reproduces the 2026-09-08 11,753 exactly. 2025-26: commercial 2,651 + charter 768 = 3,419. The harness recomputes the FPC variance by hand on a fixture | Census 7,884 -> **8,538** on 2024-25 (+654, about +0.9% on the port) and its SE 131 -> 143, of which 73 now ENTERS the port interval. The charter expansion agrees with the spreadsheet's own per-vessel method to about 1% (its window total ~2,156) |
| A27 | **Thanksgiving Day, Veterans Day (observed) and Juneteenth added to the crabbing-holiday calendar** (2026-09-11, Matt), on the same federal-observed-day rule as Independence Day; `build_crabbing_holidays.R` applies them to every season (2022-23 through 2026-27, 69 rows) | **ADOPTED** | The samplers' own Holiday? flag plus the Float 20 gear count against the same month's weekday mean: Thanksgiving 2.4x, Veterans Day observed 2.3x, Juneteenth 1.7x and 2.3x. Three days re-typed per season; in 2023-24 all three were sampled (27, 28, 50 gear against weekday means of 11.7, 11.7, 29), in 2024-25 none were. Still excluded, because the counts do not support them: Christmas Eve (1.1x, 0.8x), Presidents Day (0.8x), MLK Day (1.3x) | In the BSS each re-typed day gains the weekend (B1) and holiday (B2) effort effects: 3 of 365 days per season. In the PE the shipped empty-stratum fill ZEROES an unsampled holiday, so the 2024-25 PE port total falls 0.5% (72,588 -> 72,224) and the 2023-24 PE rises 0.2%; see D19 |

## B. Diagnostics and infrastructure

| # | Change | Status | Why it exists |
|---|---|---|---|
| B1 | **Scale-aware convergence gate** (`bss_convergence_gate.R`) | **ADOPTED** | Decides PE vs BSS per fit on R-hat, n_eff, divergence fraction and SD-normalized divergence impact. It answers "did this fit sample", never "is this model right" |
| B2 | **Model adequacy reported beside the gate** (`bss_model_adequacy.R`) | **ADOPTED** | Six configurations once passed every gate criterion while spanning 44% on the boat. `p_loo_frac`, Pareto k count, `cov50_worst_dev`, `pit_sd_worst_dev`, `flag_miscalibrated` |
| B3 | **Randomized-PIT coverage** in both the aggregate and per-observation PPC | **ADOPTED** 2026-09-01/02 | The quantile-interval form over-covers small counts by construction and produced a phantom "trailer over-coverage" finding |
| B4 | **Decoupled-parameter flag** on `structural_params_*.csv` | **ADOPTED** | Several parameters carry a proper prior unconditionally, so an unused one reports its PRIOR in the same columns as an estimate |
| B5 | **AR escalation ladder** (`ar_escalate`, scoped per population and sub-season) | **ADOPTED as a toggle**, off by default | The FW creel team's idea. Every rung is a multi-hour fit, so it ships off; it settled A4 |
| B6 | **Per-rung adequacy** (`bss_rung_adequacy.R`, `ar_rung_adequacy = TRUE`) | **ADOPTED** 2026-09-06 | The first ladder fitted two rungs for 174 minutes and recorded nothing about either, because only the kept fit reaches `write_bss_diagnostics()` |
| B7 | **Paired-difference elpd** (`loo_elpd_paired.R`) | **ADOPTED** 2026-09-05 | A model comparison needs the SE of the paired difference, not of either total. On the ZINB that was 5.5 nats against a naive 46, turning an apparent non-effect into a 2.69 SE effect |
| B8 | **Mixture-aware PPC** (`zinb_ppc.R`) | **ADOPTED** 2026-09-05 | The R-side PPC scored a ZINB fit as plain NB2 and reported the opposite of the truth on the zero bin |
| B9 | **Posterior draw persistence** (`save_bss_ppc_draws.R`, `save_ppc_draws = TRUE`) | **ADOPTED, HALF-BUILT** | Three diagnostic defects in five weeks each forced a multi-hour re-fit. The draws are written; **nothing reads them back yet**, so the feature is still write-only |
| B10 | **Untruncated config dump** (`str(params, list.len = 1000, ...)`) | **ADOPTED** 2026-09-04 | `str()` capped at 99 keys while `run_config` carried 120+, so every `run_parameters.txt` before that date is missing ~21 keys and every config comparison built on one was unreliable |
| B11 | **Shared batch helpers** (`batch_verdict_helpers.R`) | **ADOPTED** 2026-09-04 | Four runners had four copies. `merge_csv_by` exists because an ungated overwrite once destroyed a batch's verdicts; `config_delta` because an exactness comparison was got wrong by hand twice in three weeks |
| B12 | **Regression harness** (`test_improvements_2026-08-25.R`) | **ADOPTED**, **789 assertions** (2026-09-12; sections 47-59 cover the review-item patches, the follow-up decisions, the rebuilt inputs, the census split and the PE's unsampled/singleton cells) | Runs in seconds without rstan. Sections 35-44 read driver SOURCE and assert the property that failed, because most defects here have been in code that validates, not code that models |
| B13 | **`fit_agreement()`** in `batch_verdict_helpers.R` (2026-09-08) | **ADOPTED** | The dynamic f changes the parameter vector, so bit-identity (`fit_exactness`) is impossible on the rung that adds it; agreement within Monte Carlo error (z on every shared full-summary row, the touched parameters excluded) is the factorization proof the ladder needs |
| B14 | **Improvement ladder runner** `run_improvements_2026-09-08.R` (R0 desk, R1-R4 pooled, R5 gear) | **BUILT, NOT RUN** | One change per rung on the single 2024-25 season against `20260904/pooled-CPUE-AD-A1-adopted`; ships `DRY_RUN <- TRUE`, resumable, every verdict block wrapped; the harness (628 assertions) covers the deltas and the helpers |
| B15 | **Input builders** (`04_input_files/build_interview_combined.R`, `build_sampler_shifts.R`; raw exports under `04_input_files/raw/`) | **ADOPTED** 2026-09-09 | The model workbook is derived from the export by one documented rule and a printed comparison with its predecessor, so a new season is a new export plus a script run and the workbook cannot drift by hand-editing; the 2026-07-16 trim had dropped three fields the crabbing fraction now needs |
| B16 | **Effort-count QC flags** (`effort_combined.xlsx` `qc_flag`; `run_config$effort_qc_drop`) | **ADOPTED** 2026-09-10 | During the L&I PFD stand-down of Feb 3 to 13, 2024 the Float 20 / 17-21 gear "counts" are the day's TOTAL gear from interviews (the notes say so), not instantaneous counts; fed to the count model they would inflate the shore effort by about the turnover. 20 rows flagged `gear_count_from_interviews` and held out by the reader; `missing_count_time` (30) and `implausible_count_time` (3, clock times before 05:00 on real counts) are informational |
| B17 | **`read_input_workbook()`** (`03_R_functions/read_input_workbook.R`) | **ADOPTED** 2026-09-10 | readxl guesses a column's type from its first 1,000 rows; in the multi-season workbooks several columns are blank for a whole early season (no tampered flag or vehicle count in 2022-23) and were typed LOGICAL, which turns a count of 2, 3, 4 into TRUE and text into NA. The 2026-09-09 `gear_tampered` column was read that way and survived only because its values are 0/1. Every reader now guesses over the whole column; the harness asserts no reader calls readxl directly |
| B18 | **`pe_empty_effort_stratum = "local_day_type"`** (2026-09-11) | **ADOPTED 2026-09-12 (Matt: "Ship with local_day_type option on")** | The 2026-08-25 note assumed the empty (week x day-type) cells were a small thin-component problem. On the rebuilt 2024-25 inputs they are 15.6% of shore all-gear days (D19). `"day_type"` uses a sub-season-wide day-type mean, the wrong scale for a 20-fold seasonal swing; this uses the same day type in the same MONTH, falling back to the sub-season day type and then the sub-season mean. Shipped together with B19 and B20, which is what makes it defensible: alone it imputes 29% more effort at zero variance and multiplies it by a season-average CPUE |
| B19 | **`pe_empty_stratum = "local"`: the empty-stratum CPUE on the same scale as the effort fill** (2026-09-12) | **ADOPTED** | Found while shipping B18. The effort of an unsampled cell could be filled month-locally while its CPUE was always the sub-season-wide ratio-of-sums, so the seasonal gradient was counted once in the effort and again in the rate. The imputed BOAT cells are in the summer and the pooled boat CPUE is dominated by the high-CPUE winter: boat all-gear PE catch 42,841 (`"pooled"`) against 37,018 (`"local"`), -13.6%. The shore barely moves (29,705 -> 29,737). **This corrects the +26% in the 2026-09-11 D19 note to +17.8%**: 72,224 -> 90,861 from the effort fill, -5,785 from the CPUE fill, 85,076. `"pooled"` reproduces the 2026-07-13 to 2026-09-11 behaviour, `"zero"` the pre-2026-07-13 one |
| B20 | **`pe_variance = "impute_aware"`: an unsampled or singleton PE cell is no longer free** (2026-09-12) | **ADOPTED** | See D21. A singleton cell's `sd()` is NA, replaced by 0, so 37 of 93 shore all-gear cells and 34 of 93 boat cells contributed their full point estimate and no variance; so did every imputed cell. A singleton now borrows its donor's spread with the divisor still 1 (the collapsed-stratum estimator: Hansen, Hurwitz & Madow 1953; Cochran 1977 sec. 5A.12; Wolter 2007 ch. 2) and an imputed cell carries the donor mean's variance plus a between-cell surrogate `s^2 (1/n_donor + 1)`. The POINT ESTIMATE does not move. 2024-25 effort SE: shore all-gear 410 -> 1,379 (1.5% -> 3.9%), boat all-gear 573 -> 1,310 (5.5% -> 9.4%), shore pot-closure 238 -> 550, boat pot-closure 29 -> 79. `"donor_mean_only"` is the lower bound (1,184 / 1,103); `"sampled_only"` reproduces the old arithmetic exactly and every run reports `effort_se_sampled_only` regardless |
| B21 | **One shared PE stratification** (`03_R_functions/pe_effort_strata.R`, 2026-09-12) | **ADOPTED** | `run_pe_pooled()` and `run_pe_gear()` each carried their own copy of the (week x day-type) stratification, the fill and the SE formula, so all three defects above lived in two places and a fix to one would have made the two tracks' PEs disagree. Both now delegate; the harness asserts neither file still contains the old fill or SE code, and on 2024-25 the two tracks' PEs are now numerically IDENTICAL component by component (shore all-gear 29,737 / SE 1,379; boat all-gear 37,018 / SE 1,310) |
| B22 | **The ladder's comparability is proven before the MCMC, not after** (`run_improvements_2026-09-08.R`, 2026-09-12) | **ADOPTED** | Four changes, none of them a new rung: `F_METHOD` ("new_throughout" shipped, per Matt "the run will use the new method for estimating f"; "ladder" restores the cumulative-attribution design) with the mode in every run_tag and output filename; `WINDOW` completed with the four per-season keys it was missing (`pot_closure_start`/`end`, `pot_open_date`, `census_start_date`/`end`, `commercial_opener`) plus the three PE levers and the sampler seed; a preflight that resolves every rung's config and FAILS on any non-delta key differing between them, writing `improvements_2026-09-08_manifest*.csv` before anything runs; and a `RESUME` that requires a matching config digest (`IMP_STAGE.txt`) instead of reusing any finished-looking folder. `R2f` is a new optional control rung: R2 with the f block rolled back, which is the R3-vs-R3a factorization proof at one fit instead of two. R0 now runs the PE under all four unsampled-cell arms, so D19 needs no refit |

## C. Defects found and fixed, with what each cost

| Date | Defect | Cost | Fixed by |
|---|---|---|---|
| 2026-08-25 | rstan reports a data-init failure on the message stream and returns an empty `stanfit` rather than raising | six model-runs | `bss_stan_fit.R` data-contract guard |
| 2026-08-28 | `ar_force` keyed per population silently forced a second sub-season | 3,025 crab on a component | per-sub-season resolution and a scope control in every batch |
| 2026-08-31 | PPC coverage from a quantile interval over-covers small counts | a phantom open item, retracted | B3 |
| 2026-09-01 | `tau_bar` registered under its bare name while rstan writes `tau_bar[1]`; the lookup threw inside a `tryCatch` | whole `prior_vs_posterior` files silently dropped | row-key helper |
| 2026-09-02 | The gear boat all-gear fit had never sampled in production | a 27% understatement of the cross-check | A6 |
| 2026-09-03 | `force_res` gated on `isTRUE(params$ar_escalate)`, which is FALSE for the scoped form | **14.7 h**: four ladder rungs all refit `daily` | resolve through the one helper; harness asserts no driver contains the expression |
| 2026-09-03 | The ZINB PPC computed under NB2 | the feature was nearly rejected on a diagnostic that reported the opposite of the truth | B8 |
| 2026-09-03 | The elpd criterion used `se_elpd_loo` | "NOT WORTH IT" on a 2.69 SE effect | B7 |
| 2026-09-04 | `run_parameters.txt` truncated at 99 keys | one false caveat, one latent missing flag | B10 |
| 2026-09-06 | The runner never moved the rendered HTML | **stage L1's report destroyed** by the next stage; numbers survived in CSV | move it, gitignore `01_BSS_models/*.html`, assert over every runner |
| 2026-09-06 | `bss_rung_adequacy` reseeded inside the fitting loop | a diagnostic-only toggle moved a committed diagnostic on a **bit-identical** fit | save and restore `.Random.seed` |
| 2026-09-06 | `p_loo_frac` summed streams where `model_adequacy` takes the worst | two statistics under one name in one run | match the existing definition |
| 2026-09-08 | The two drivers label the port-total row differently; `is.finite(numeric(0)) && ...` errors | the adoption run's verdicts lost after 4.1 h of fitting | pattern match; every verdict block wrapped |
| 2026-09-10 | The 2023-24 gear count was declared absent (D15, 2026-09-09) on a misreading: the pasted export carried it under the header "Number Of Gear" (capital O, the 2023-24 sheet's spelling) and the builder read only "Number of Gear"; the column was described as 0/1 flags, which it never was | a false blocker on the 2023-24 fits for one day; the July workbook had the same NA column for two months | the builders read from the season workbooks and take the count from either header (A24) |
| 2026-09-10 | Multi-season workbooks read with readxl's 1,000-row type guess | latent: a late-starting numeric column would be read as logical | B17 |
| 2026-09-11 | The harness read the improvement ladder's `D_R1..D_R4` deltas by grepping for lines that look like delta keys; adding a comment line inside a delta made the extracted text unparseable | the harness aborted mid-run (it had passed 638 of 725 assertions and reported nothing about the rest) | take the whole contiguous `D_R1..D_R4` block and extend it until it parses; the block-parses check is itself an assertion now |
| 2026-09-11 | `observed_dung` (the census draw floor) was the joint tally-day estimate, which counted un-interviewed charter trips as observed | latent: it would have clamped the census draws above the observed catch | the floor is now the commercial census plus the charter crab actually observed |
| recurring | `DRY_RUN <- FALSE` committed | a fresh clone starts real fits | asserted since 2026-09-03; caught every time, **five occurrences** |

## D. Open items, ordered by how much they could move the number

| # | Item | Status | What would settle it |
|---|---|---|---|
| D1 | **The boat is 43% of the port and 79.8% extrapolated** (66 sampled days of 289) | **OPEN, not a code problem** | More boat interviews in 2025-26. No modelling change manufactures uncollected information |
| D2 | **OSP crabbing-only daily counts** | **BLOCKED on a data request** | One column in `WBL_boat_counts.xlsx`. Machinery built and inert (A8). Note it is a LOWER BOUND on crabbing vessels, not `f` |
| D3 | **The gear track's shore cap is still monthly** | **OPEN** | Now the largest single contributor to the -1.39% cross-track gap. A gear-track ladder, about 3 h. At the same resolution the tracks agree to **0.08%** |
| D4 | **The catch stream is under-covered at every AR resolution** (-3.5 to -4.6 SD) | **OPEN, bounded** | A hurdle or two-component NB mixture. The ZINB halved both count bins and did not close the shore all-gear one bin (still 3.3 SD). Gain bounded at ~6% of the catch |
| D5 | **`ppc_draws_*.rds` are write-only** | **HALF-BUILT** | A recompute script. Code, no run |
| D6 | **The ZINB is pooled-only** | **OPEN, low priority** | The two tracks now differ in the shore catch likelihood (~-0.3%). A Stan edit plus a recompile restores symmetry |
| D8 | **One closure window per run** | **ARCHITECTURE DONE 2026-09-10 (A14); DATA UNBLOCKED 2026-09-10 (A24) except the 2023-24 census** | `effort_combined.xlsx` now carries 2023-24 (4,116 counts on 360 days), `interview_combined.xlsx` the 2023-24 gear count, `crabbing_holidays.xlsx` the 2023-24 calendar, and the 2023-24 census window is Dec 1, 2023 to Jan 31, 2024 (the commercial fishery opened Feb 1, 2024, WDFW bulletin). What cannot be supplied: a 2023-24 vessel tally or charter roster (none was kept), so the 2023-24 census component is 0 and `estimate_comm_charter()` warns that its frame is missing; a two-season port total is short by the 2023-24 commercial/charter catch. The pre-fit driver code and every Stan data set build for the staged config (smoke-tested 2026-09-10); the fits have not been run
| D9 | **New-season workflow documented** | **DONE 2026-09-09** | `07_documentation/NEW_SEASON_GUIDE.md`: naive run -> ladder -> per-rung adequacy -> pin (`ar_force`) -> cap -> production -> cross-check |
| D7 | **Method of record is frozen at v1.0** (pooled code v7.4); the code is past v7.9 | **OPEN, documentation debt** | The two method documents carry pre-refresh 2024-25 reference numbers that have never been regenerated |
| D10 | **The 23 unsampled census days** (A20) | **CLOSED 2026-09-09 (A22)** | They are days with no commercial or charter operation by the sampling design (samplers are scheduled on confirmed operating days); the census is the exact sum over the tally days |
| D11 | **The contact stream sees boats during sampler shifts only** (A18, A23) | **OPEN, now MEASURED** | The shifts cover about 75% of a day's boat returns (boat I/E profile, all launch sites pooled; WBL alone has 5 days) and the trip-type mix does not drift with the hour inside them (2024-25). The 15% of returns after the last check-out are unclassified; only OSP's all-day crabbing-only count can say whether their mix differs, and the comparison is now direct: `f_lower_out` = f(1 - c) is the model's prediction of that column |
| D12 | **The improvement ladder has not run** (B14) | **BLOCKED on ~22 h of fitting** | R1-R5 in `run_improvements_2026-09-08.R`; adoption after R4 (decision: derived shore turnover) and R5 is the usual one-render gate against the last rung and moves the authoritative run |
| D13 | **Which interviews are the f classification** (A21) | **CLOSED 2026-09-10 (Matt)** | Private boats interviewed at Float 20 / Float 17-21 are docked at the float, not trailered; their trailers sit in a different lot that is not censused, and the censused lot holds the trailers of the boats launched at the ramp. The launch-site restriction (`crab_fraction_contact_areas = NULL`) is therefore the right default and stays; the dock and marina rows are outside the counted population (D14)
| D14 | **Moored private boats are outside the boat effort counts** (found 2026-09-09; confirmed 2026-09-10 by D13's answer) | **OPEN, structural** | Private boats interviewed at the marina (34 in 2024-25, all crab-only) and at the docks (88 in 2024-25; 221 dock boat interviews in 2025-26) are docked at the floats; their catch rates enter the CPUE but their effort is in neither the trailer count nor the OSP ramp total, so the boat component omits them. The 2022-24 effort protocol had a "Boats Entering Marina Count" column, but it was recorded at Ilwaco Marina (2,209 in 2023-24) and never at Westport (0 in every season). Sizing it needs a Westport moorage count (slips occupied, or a marina boat count series)
| D15 | **2023-24 has no gear count** (declared 2026-09-09) | **CLOSED 2026-09-10: it was a misreading, corrected** | The count was in the export all along under the 2023-24 sheet's header spelling "Number Of Gear"; the builder read only "Number of Gear". Rebuilt from the season workbooks, every 2023-24 interview carries its gear count (0 to 23, matching the workbook exactly), and the 2023-24 CPUE denominator exists
| D16 | **The census premise for the commercial vessels** (A25) | **CLOSED 2026-09-11 (Matt)** | "My statement of a complete census without error applies to the commercial boats. The charter vessels are not 100% sampled and need expansion." So the commercial vessel COUNT is a census on the tally days and the unsampled days had no commercial operation; the charter part is now an expansion (A26). The residual, reported not carried: the commercial per-vessel catch MEAN is a sample over 141 of 164 vessel-trips in 2024-25 (86%, SE 123 = 1.9%) and 44 of 67 in 2025-26 (66%, SE 146 = 5.5%), so "without error" is a statement about the count, not about the catch. `census_uncertainty = "sampling"` carries that term if it is ever wanted |
| D17 | **Holiday rule candidates** (A24) | **CLOSED 2026-09-11 (Matt): the three added** | Thanksgiving Day, Veterans Day (observed) and Juneteenth are in the calendar (A27). Christmas Eve, Presidents Day and MLK Day stay out on the effort evidence; a future addition is one name in `build_crabbing_holidays.R` |
| D18 | **OSP boat counts stop at 2025-10-18** | **BLOCKED on a data request (Erica)** | `WBL_boat_counts.xlsx` covers 2024-03-09 to 2025-10-18, so a 2025-26 boat fit runs on the trailer counts and the I/E alone (the shared turnover is refused for want of OSP overlap days) and the 2023-24 boat has OSP from March 2024 only. The 2025-26 season workbook is otherwise complete through 2026-09-08 |
| D19 | **The PE zeroed 15% of the biggest components' days, and the fill choice moves the PE by 18%** (found 2026-09-11 while adding the holidays; the mechanism was flagged 2026-08-25; the CPUE half found 2026-09-12) | **PARTLY CLOSED 2026-09-12 (Matt: ship `local_day_type`); the comparison against the post-ladder BSS is still open** | The PE builds (week x day-type) strata from sampled days and left-joins the calendar, so a cell with calendar days but no sampled day contributed ZERO effort under the retired `pe_empty_effort_stratum = "zero"`. On the rebuilt 2024-25 inputs that is 45 of 289 shore all-gear days (15.6%) and 48 of 289 boat all-gear days, and the empty cells sit in the HIGH months. **The 2026-09-11 figure of +26% was wrong in a specific way**: it paired a month-local EFFORT fill with a sub-season-pooled CPUE, counting the seasonal gradient twice (B19). At the shipped settings the 2024-25 PE port total is 72,224 (`"zero"`), 81,160 (`"day_type"`) and **85,076** (`"local_day_type"`, +17.8%); the effort totals are 45,175 / 53,846 / 56,793, so `"zero"` was omitting a fifth of the season's effort. **AND IT IS NOT PE-ONLY**, contrary to what this row said until 2026-09-12: a component whose convergence gate FAILS reports its PE point in the port total as a CONSTANT (section 7.7 of either driver), and the thin boat pot closure is the component most likely to fall back, so the fill can reach the headline. What remains open is only the CHOICE: read the arm spread against R4's BSS rather than against `20260904/pooled-CPUE-AD-A1-adopted`, which predates the derived shore turnover and the boat recentring. The ladder's R0 desk rung now prints all four arms, so that comparison costs no MCMC. Adding a holiday makes `"zero"` marginally worse (the -0.5% in A27); under the shipped fill that interaction is gone |
| D20 | **The 2022-24 gear label "Collapsible trap or ring" is one option where the current form has two** (found 2026-09-11, answering "is there not a ring net option?") | **OPEN, low priority; affects the gear-resolved 2022-24 fits only** | There was NO ring-net option before 2024-25: the 2022-23 and 2023-24 forms offered "Collapsible trap or ring" and, separately, "FISHING ROD WITH foldable trap" and "Star trap", so a hand-held foldable trap had no home but the "or ring" option. The 2023-24 workbook's own `gear_key` sheet maps "Collapsible trap or ring" -> "Ring net", which is what `build_interview_combined.R` implements, but the mapping is one-to-many in truth. The share evidence: the combined ring-plus-trap share of gear tokens is stable across the vocabulary change (43% in 2023-24, 45% in 2024-25) while its internal split moves from 66/34 ring:trap to 47/53, which is what a moved boundary looks like rather than a change in gear use. The pooled model is unaffected (it does not split by gear) and so is the gear-resolved 2024-25 cross-check; only a gear-resolved fit of 2022-24 would inherit the ambiguity. What would settle it: whether the pre-2024-25 iForm's "Collapsible trap or ring" was ever chosen for a non-rod foldable trap in practice, which is a question for the samplers of those seasons |
| D21 | **The PE's effort SE was computed over less than half the effort it reported** (found 2026-09-12 while shipping B18) | **FIXED (B20); two residuals remain, D22** | The SE was `sqrt(N^2 sd^2 / max(n, 1))` with `sd` from the cell's own sampled days. `sd()` of ONE observation is NA and was replaced by 0, so a cell resting on a single sampled day contributed its full point estimate and no variance. With weekly strata and ~50% day coverage that is not rare: 37 of 93 shore all-gear cells (82 of 289 calendar days, 14,816 of 27,345 effort units) and 34 of 93 boat cells (77 days, 5,164 of 10,482). **54% of the shore all-gear effort and 49% of the boat's had no uncertainty attached**, and the component SE read 410 on 27,345 (1.5%) for a design with a within-cell CV of 0.2 to 0.7. Imputed cells were treated the same way, so the shore all-gear effort SE was BIT-IDENTICAL at 410 whether the fill added 0, 6,240 or 7,947 effort units. Note which half is bigger: fixing the singletons alone takes the shore SE 410 -> 1,099 under the retired `"zero"` fill, so most of the understatement was never about imputation |
| D22 | **Two PE variance residuals not addressed in B20** (2026-09-12) | **OPEN, both deliberate** | (1) NO FINITE-POPULATION CORRECTION anywhere in the PE. A stratum's `n_h` sampled days are drawn without replacement from its `N_h` calendar days, so `Var(ybar_h)` should carry `(1 - n_h/N_h)`; the PE has never applied one. Adding it would REDUCE the SE and would have mixed an opposite-signed change into B20, making neither attributable. `estimate_comm_charter()` does apply an FPC (A26), so the two estimators are currently inconsistent on this point. (2) THE PE CATCH HAS NO SE AT ALL, only effort does: `run_pe_*()` returns `effort_se` and no catch variance, so the PE side of `pe_vs_bss_comparison.csv` is a point against a posterior interval, and a gate-failed component enters the port total as a constant. The honest version is a ratio-estimator variance combining the effort SE with the stratum CPUE variance, which is a larger piece of work than everything in this patch combined |
